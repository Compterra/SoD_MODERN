"""Optional local visual surface for the SoD Modern DevKit.

The Studio is intentionally a thin loopback-only adapter over the existing
LLM-first Module Atlas, dialogue, presentation, and Workbench APIs.  It owns
no alternate parsing or write path.
"""
