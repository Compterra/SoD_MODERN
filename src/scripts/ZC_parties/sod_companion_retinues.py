SCRIPTS = [
("sod_companion_retinue_get_identity",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, sod_retinue_pref_general),
   (assign, reg1, 0),
   (try_begin),
     (eq, ":companion", "trp_npc1"),
     (assign, reg0, sod_retinue_pref_scout_irregular),
     (assign, reg1, 1),
   (else_try),
     (eq, ":companion", "trp_npc2"),
     (assign, reg0, sod_retinue_pref_trade_guard),
     (assign, reg1, 1),
   (else_try),
     (eq, ":companion", "trp_npc3"),
     (assign, reg0, sod_retinue_pref_mercy_guard),
     (assign, reg1, -1),
   (else_try),
     (eq, ":companion", "trp_npc4"),
     (assign, reg0, sod_retinue_pref_noble_guard),
     (assign, reg1, 2),
   (else_try),
     (eq, ":companion", "trp_npc5"),
     (assign, reg0, sod_retinue_pref_horse_archer),
     (assign, reg1, 3),
   (else_try),
     (eq, ":companion", "trp_npc6"),
     (assign, reg0, sod_retinue_pref_redeemed_infantry),
     (assign, reg1, 3),
   (else_try),
     (eq, ":companion", "trp_npc7"),
     (assign, reg0, sod_retinue_pref_archer_tracker),
     (assign, reg1, 2),
   (else_try),
     (eq, ":companion", "trp_npc8"),
     (assign, reg0, sod_retinue_pref_shield_wall),
     (assign, reg1, 3),
   (else_try),
     (eq, ":companion", "trp_npc9"),
     (assign, reg0, sod_retinue_pref_field_captain),
     (assign, reg1, 3),
   (else_try),
     (eq, ":companion", "trp_npc10"),
     (assign, reg0, sod_retinue_pref_crossbow_veteran),
     (assign, reg1, 3),
   (else_try),
     (eq, ":companion", "trp_npc11"),
     (assign, reg0, sod_retinue_pref_household_guard),
     (assign, reg1, -1),
   (else_try),
     (eq, ":companion", "trp_npc12"),
     (assign, reg0, sod_retinue_pref_healer_escort),
     (assign, reg1, -1),
   (else_try),
     (eq, ":companion", "trp_npc13"),
     (assign, reg0, sod_retinue_pref_glory_cavalry),
     (assign, reg1, 3),
   (else_try),
     (eq, ":companion", "trp_npc14"),
     (assign, reg0, sod_retinue_pref_drilled_infantry),
     (assign, reg1, 3),
   (else_try),
     (eq, ":companion", "trp_npc15"),
     (assign, reg0, sod_retinue_pref_engineer_support),
     (assign, reg1, 1),
   (else_try),
     (eq, ":companion", "trp_npc16"),
     (assign, reg0, sod_retinue_pref_skirmisher),
     (assign, reg1, 1),
   (try_end),
 ]),

("sod_companion_retinue_describe_identity_to_s27",
 [
   (store_script_param, ":companion", 1),
   (str_store_string, s27, "@general company troops"),
   (try_begin),
     (call_script, "script_sod_companion_retinue_get_identity", ":companion"),
     (assign, ":identity", reg0),
     (try_begin),
       (eq, ":identity", sod_retinue_pref_scout_irregular),
       (str_store_string, s27, "@scouts and road irregulars"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_trade_guard),
       (str_store_string, s27, "@trade guards and mercantile escorts"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_mercy_guard),
       (str_store_string, s27, "@protectors and mercy-minded escorts"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_noble_guard),
       (str_store_string, s27, "@prestige troops and self-styled noble guards"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_horse_archer),
       (str_store_string, s27, "@mounted scouts and horse archers"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_redeemed_infantry),
       (str_store_string, s27, "@disciplined infantry and redeemed lowborn soldiers"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_archer_tracker),
       (str_store_string, s27, "@archers, trackers, and village ambushers"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_shield_wall),
       (str_store_string, s27, "@shield troops and hard infantry"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_field_captain),
       (str_store_string, s27, "@disciplined noble infantry and cavalry"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_crossbow_veteran),
       (str_store_string, s27, "@crossbowmen and veteran infantry"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_household_guard),
       (str_store_string, s27, "@household troops and practical militia"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_healer_escort),
       (str_store_string, s27, "@guards, recovery escorts, and defensive orderlies"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_glory_cavalry),
       (str_store_string, s27, "@cavalry, shock troops, and glory volunteers"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_drilled_infantry),
       (str_store_string, s27, "@drilled infantry and recruits under formation training"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_engineer_support),
       (str_store_string, s27, "@engineers, crossbowmen, and siege-support troops"),
     (else_try),
       (eq, ":identity", sod_retinue_pref_skirmisher),
       (str_store_string, s27, "@skirmishers, knife-fighters, and light irregulars"),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_describe_voice_to_s29",
 [
   (store_script_param, ":companion", 1),
   (str_store_string, s29, "@Give me soldiers suited to the work and I will keep them in hand."),
   (try_begin),
     (eq, ":companion", "trp_npc1"),
     (str_store_string, s29, "@Road men do best when they can move fast, sleep light, and know when not to be seen."),
   (else_try),
     (eq, ":companion", "trp_npc2"),
     (str_store_string, s29, "@A guard who understands the value of a pack train is worth two who only understand glory."),
   (else_try),
     (eq, ":companion", "trp_npc3"),
     (str_store_string, s29, "@I will not waste lives for pride. Give me people who can protect the hurt and still hold a line."),
   (else_try),
     (eq, ":companion", "trp_npc4"),
     (str_store_string, s29, "@Men follow a name if the name is carried like a banner. I can give them that."),
   (else_try),
     (eq, ":companion", "trp_npc5"),
     (str_store_string, s29, "@Give me riders with good eyes and better horses. A slow command is a dead command."),
   (else_try),
     (eq, ":companion", "trp_npc6"),
     (str_store_string, s29, "@I know what a man can become after shame. Give me soldiers who need discipline more than speeches."),
   (else_try),
     (eq, ":companion", "trp_npc7"),
     (str_store_string, s29, "@The woods teach patience. Archers and trackers under me will learn to wait until waiting hurts."),
   (else_try),
     (eq, ":companion", "trp_npc8"),
     (str_store_string, s29, "@Put shields in my hands and I will make a wall. Put fools there and I will make fewer fools."),
   (else_try),
     (eq, ":companion", "trp_npc9"),
     (str_store_string, s29, "@A proper command needs standards, watches, and men who know why they stand where they stand."),
   (else_try),
     (eq, ":companion", "trp_npc10"),
     (str_store_string, s29, "@Crossbows, veterans, steady infantry. Give me troops who can hold when the brave ones start shouting."),
   (else_try),
     (eq, ":companion", "trp_npc11"),
     (str_store_string, s29, "@I can keep practical folk fed, paid, and pointed the right way. That is not nothing."),
   (else_try),
     (eq, ":companion", "trp_npc12"),
     (str_store_string, s29, "@If they are under my care, I will count bandages before boasts and still see them ready."),
   (else_try),
     (eq, ":companion", "trp_npc13"),
     (str_store_string, s29, "@Give me a few riders with nerve and I will make them feel like the front of a parade."),
   (else_try),
     (eq, ":companion", "trp_npc14"),
     (str_store_string, s29, "@Recruits become soldiers when someone stops forgiving sloppy feet. I can do that."),
   (else_try),
     (eq, ":companion", "trp_npc15"),
     (str_store_string, s29, "@Sieges are won by hands that know tools as well as weapons. I can use those hands."),
   (else_try),
     (eq, ":companion", "trp_npc16"),
     (str_store_string, s29, "@Give me quiet feet, quick hands, and people who understand that clean work is still work."),
   (try_end),
 ]),

("sod_companion_retinue_describe_dialogue_to_s28",
 [
   (store_script_param, ":companion", 1),
   (str_store_string, s28, "@I have no command to speak of just now."),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_describe_identity_to_s27", ":companion"),
     (call_script, "script_sod_companion_retinue_describe_voice_to_s29", ":companion"),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, reg20, reg0),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (assign, reg21, reg0),
     (call_script, "script_sod_companion_retinue_get_target_size", ":companion"),
     (assign, reg22, reg0),
     (troop_get_slot, reg23, ":companion", slot_troop_sod_retinue_treasury),
     (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
     (assign, reg24, reg0),
     (call_script, "script_sod_companion_retinue_update_warning_state", ":companion"),
     (troop_get_slot, ":retinue_warning", ":companion", slot_troop_sod_retinue_warning_state),
     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (troop_get_slot, ":companion_warning", ":companion", slot_troop_companion_warning_state),
     (try_begin),
       (eq, ":retinue_warning", sod_retinue_warning_over_capacity),
       (str_store_string, s28, "@This command is overfilled. I can keep them together for a little while, but not without discipline fraying. Reclaim some, or give me fewer mouths to answer for."),
     (else_try),
       (eq, ":retinue_warning", sod_retinue_warning_full_refused),
       (str_store_string, s28, "@I will take responsibility for soldiers, but not a full command while trust is this strained. Give me a steadier footing first."),
     (else_try),
       (this_or_next|eq, ":companion_warning", sod_companion_warning_pending),
       (this_or_next|eq, ":companion_warning", sod_companion_warning_final),
       (eq, ":companion_warning", sod_companion_warning_broken),
       (str_store_string, s28, "@You can assign troops to me, but hear this plainly: a retinue follows the captain as much as the banner. Strained trust makes every order cost more than coin."),
     (else_try),
       (lt, ":approval", 45),
       (str_store_string, s28, "@I can command {s27}, but I will not pretend the men are blind. If you put valuable troops under me while things stand like this, they will feel the tension."),
     (else_try),
       (gt, reg20, 0),
       (str_store_string, s28, "@{s29}^My rolls stand at {reg20} of {reg21}, with orders aiming for {reg22}. The purse holds {reg23} denars; wages due this week are {reg24}."),
     (else_try),
       (str_store_string, s28, "@{s29}^Give me {s27}, and I will make a proper command of them. I do not need a crowd. I need soldiers who fit the work."),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_sanitize_state",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),

     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (val_clamp, ":approval", 0, 101),
     (troop_set_slot, ":companion", slot_troop_companion_approval, ":approval"),
     (call_script, "script_sod_companion_get_approval_band_to_reg", ":companion"),
     (troop_set_slot, ":companion", slot_troop_companion_trust_tier, reg0),
     (troop_get_slot, ":warning_state", ":companion", slot_troop_companion_warning_state),
     (try_begin),
       (this_or_next|lt, ":warning_state", sod_companion_warning_none),
       (gt, ":warning_state", sod_companion_warning_broken),
       (assign, ":warning_state", sod_companion_warning_none),
       (troop_set_slot, ":companion", slot_troop_companion_warning_state, ":warning_state"),
     (try_end),
     (troop_get_slot, ":quest_stage", ":companion", slot_troop_companion_personal_quest_stage),
     (try_begin),
       (this_or_next|lt, ":quest_stage", sod_companion_quest_none),
       (gt, ":quest_stage", sod_companion_quest_failed),
       (troop_set_slot, ":companion", slot_troop_companion_personal_quest_stage, sod_companion_quest_none),
     (try_end),

     (troop_get_slot, ":state", ":companion", slot_troop_sod_retinue_state),
     (try_begin),
       (this_or_next|lt, ":state", sod_retinue_state_inactive),
       (gt, ":state", sod_retinue_state_pending_cleanup),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_inactive),
     (try_end),
     (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_policy),
     (try_begin),
       (this_or_next|lt, ":policy", sod_retinue_policy_balanced),
       (gt, ":policy", sod_retinue_policy_guard_companion),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_policy, sod_retinue_policy_balanced),
     (try_end),
     (troop_get_slot, ":strength_order", ":companion", slot_troop_sod_retinue_strength_order),
     (try_begin),
       (this_or_next|lt, ":strength_order", sod_retinue_strength_none),
       (gt, ":strength_order", sod_retinue_strength_full),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
     (try_end),
     (troop_get_slot, ":recruit_policy", ":companion", slot_troop_sod_retinue_recruit_policy),
     (try_begin),
       (this_or_next|lt, ":recruit_policy", sod_retinue_recruit_policy_none),
       (gt, ":recruit_policy", sod_retinue_recruit_policy_eager),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
     (try_end),
     (troop_get_slot, ":post_battle_policy", ":companion", slot_troop_sod_retinue_post_battle_policy),
     (try_begin),
       (this_or_next|lt, ":post_battle_policy", sod_retinue_post_battle_enabled),
       (gt, ":post_battle_policy", sod_retinue_post_battle_disabled),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_post_battle_policy, sod_retinue_post_battle_enabled),
     (try_end),

     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (val_max, ":treasury", 0),
     (val_min, ":treasury", sod_retinue_max_command_purse),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
     (troop_get_slot, ":last_shortage", ":companion", slot_troop_sod_retinue_last_shortage),
     (val_max, ":last_shortage", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_shortage, ":last_shortage"),
     (troop_get_slot, ":wage_reserve", ":companion", slot_troop_sod_retinue_wage_reserve),
     (val_max, ":wage_reserve", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_wage_reserve, ":wage_reserve"),
     (troop_get_slot, ":last_morale", ":companion", slot_troop_sod_retinue_last_morale),
     (val_clamp, ":last_morale", 0, 101),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_morale, ":last_morale"),
     (troop_get_slot, ":supply_pressure", ":companion", slot_troop_sod_retinue_supply_pressure),
     (val_max, ":supply_pressure", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_supply_pressure, ":supply_pressure"),
     (troop_get_slot, ":last_training_xp", ":companion", slot_troop_sod_retinue_last_training_xp),
     (val_max, ":last_training_xp", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_training_xp, ":last_training_xp"),

     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (try_begin),
       (party_is_active, ":retinue_party"),
       (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_anchor_party, "p_main_party"),
       (party_set_ai_behavior, ":retinue_party", ai_bhvr_hold),
       (party_set_slot, ":retinue_party", slot_party_ai_state, spai_undefined),
       (party_set_slot, ":retinue_party", slot_party_ai_object, -1),
       (party_set_slot, ":retinue_party", slot_party_ai_substate, 0),
       (party_set_slot, ":retinue_party", slot_party_follow_me, 0),
       (party_get_num_prisoner_stacks, ":num_prisoner_stacks", ":retinue_party"),
       (try_for_range_backwards, ":stack_no", 0, ":num_prisoner_stacks"),
         (party_prisoner_stack_get_troop_id, ":stack_troop", ":retinue_party", ":stack_no"),
         (party_prisoner_stack_get_size, ":stack_size", ":retinue_party", ":stack_no"),
         (gt, ":stack_size", 0),
         (party_remove_prisoners, ":retinue_party", ":stack_troop", ":stack_size"),
       (try_end),
       (party_get_num_companion_stacks, ":num_companion_stacks", ":retinue_party"),
       (try_for_range_backwards, ":stack_no", 0, ":num_companion_stacks"),
         (party_stack_get_troop_id, ":stack_troop", ":retinue_party", ":stack_no"),
         (party_stack_get_size, ":stack_size", ":retinue_party", ":stack_no"),
         (gt, ":stack_size", 0),
         (try_begin),
           (troop_is_hero, ":stack_troop"),
           (party_remove_members, ":retinue_party", ":stack_troop", ":stack_size"),
           (party_add_members, "p_main_party", ":stack_troop", ":stack_size"),
         (else_try),
           (neg|is_between, ":stack_troop", soldiers_begin, soldiers_end),
           (party_remove_members, ":retinue_party", ":stack_troop", ":stack_size"),
         (try_end),
       (try_end),
       (assign, reg0, 1),
     (else_try),
       (neq, ":retinue_party", 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_party, 0),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_get_capacity",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_sanitize_state", ":companion"),
     (store_skill_level, ":leadership", "skl_leadership", ":companion"),
     (store_attribute_level, ":charisma", ":companion", ca_charisma),
     (store_character_level, ":level", ":companion"),
     (assign, ":capacity", 4),
     (store_mul, ":leadership_bonus", ":leadership", 5),
     (val_add, ":capacity", ":leadership_bonus"),
     (store_div, ":charisma_bonus", ":charisma", 2),
     (val_add, ":capacity", ":charisma_bonus"),
     (store_div, ":level_bonus", ":level", 3),
     (val_add, ":capacity", ":level_bonus"),
     (call_script, "script_sod_companion_retinue_get_identity", ":companion"),
     (assign, ":identity", reg0),
     (assign, ":martial_grade", reg1),
     (try_begin),
       (ge, ":martial_grade", 3),
       (val_add, ":capacity", 4),
     (else_try),
       (ge, ":martial_grade", 2),
       (val_add, ":capacity", 3),
     (else_try),
       (ge, ":martial_grade", 1),
       (val_add, ":capacity", 1),
     (else_try),
       (lt, ":martial_grade", 0),
       (val_sub, ":capacity", 2),
     (try_end),
     (try_begin),
       (this_or_next|eq, ":identity", sod_retinue_pref_healer_escort),
       (this_or_next|eq, ":identity", sod_retinue_pref_mercy_guard),
       (this_or_next|eq, ":identity", sod_retinue_pref_household_guard),
       (eq, ":identity", sod_retinue_pref_engineer_support),
       (val_add, ":capacity", 1),
     (try_end),

     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (try_begin),
       (ge, ":approval", 80),
       (val_add, ":capacity", 8),
     (else_try),
       (ge, ":approval", 60),
       (val_add, ":capacity", 5),
     (else_try),
       (ge, ":approval", 45),
       (val_add, ":capacity", 2),
     (else_try),
       (lt, ":approval", 20),
       (val_sub, ":capacity", 8),
     (else_try),
       (lt, ":approval", 35),
       (val_sub, ":capacity", 4),
     (try_end),

     (troop_get_slot, ":grievance", ":companion", slot_troop_companion_grievance),
     (val_clamp, ":grievance", 0, 101),
     (troop_set_slot, ":companion", slot_troop_companion_grievance, ":grievance"),
     (try_begin),
       (ge, ":grievance", 80),
       (val_sub, ":capacity", 8),
     (else_try),
       (ge, ":grievance", 60),
       (val_sub, ":capacity", 5),
     (else_try),
       (ge, ":grievance", 40),
       (val_sub, ":capacity", 2),
     (else_try),
       (lt, ":grievance", 20),
       (val_add, ":capacity", 2),
     (try_end),

     (troop_get_slot, ":warning_state", ":companion", slot_troop_companion_warning_state),
     (try_begin),
       (eq, ":warning_state", sod_companion_warning_broken),
       (val_sub, ":capacity", 20),
     (else_try),
       (eq, ":warning_state", sod_companion_warning_final),
       (val_sub, ":capacity", 15),
     (else_try),
       (eq, ":warning_state", sod_companion_warning_acknowledged),
       (val_sub, ":capacity", 8),
     (else_try),
       (eq, ":warning_state", sod_companion_warning_pending),
       (val_sub, ":capacity", 5),
     (try_end),

     (troop_get_slot, ":quest_stage", ":companion", slot_troop_companion_personal_quest_stage),
     (try_begin),
       (eq, ":quest_stage", sod_companion_quest_resolved_good),
       (val_add, ":capacity", 5),
     (else_try),
       (eq, ":quest_stage", sod_companion_quest_resolved_hard),
       (val_add, ":capacity", 2),
     (else_try),
       (eq, ":quest_stage", sod_companion_quest_failed),
       (assign, ":capacity", 0),
     (try_end),

     (val_max, ":capacity", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_capacity, ":capacity"),
     (assign, reg0, ":capacity"),
   (try_end),
 ]),

("sod_companion_retinue_ensure_party",
 [
   (store_script_param, ":companion", 1),
   (assign, ":result", 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_sanitize_state", ":companion"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (assign, ":valid_retinue", 0),
     (try_begin),
       (party_is_active, ":retinue_party"),
       (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
       (party_slot_eq, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
       (assign, ":valid_retinue", 1),
     (try_end),
     (try_begin),
       (eq, ":valid_retinue", 0),
       (set_spawn_radius, 0),
       (spawn_around_party, "p_main_party", "pt_sod_companion_retinue"),
       (assign, ":retinue_party", reg0),
       (try_begin),
         (gt, ":retinue_party", 0),
         (party_is_active, ":retinue_party"),
         (str_store_troop_name, s19, ":companion"),
         (party_set_name, ":retinue_party", "@{s19}'s retinue"),
         (party_set_slot, ":retinue_party", slot_party_type, spt_companion_retinue),
         (party_set_slot, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
         (troop_set_slot, ":companion", slot_troop_sod_retinue_party, ":retinue_party"),
         (party_set_ai_behavior, ":retinue_party", ai_bhvr_hold),
         (party_attach_to_party, ":retinue_party", "p_main_party"),
         (party_set_slot, ":retinue_party", slot_party_ai_state, spai_undefined),
         (party_set_slot, ":retinue_party", slot_party_ai_object, -1),
         (party_set_slot, ":retinue_party", slot_party_ai_substate, 0),
         (party_set_slot, ":retinue_party", slot_party_follow_me, 0),
       (else_try),
         (assign, ":retinue_party", 0),
         (troop_set_slot, ":companion", slot_troop_sod_retinue_party, 0),
       (try_end),
     (try_end),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),

     (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_policy),
     (try_begin),
       (le, ":policy", 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_policy, sod_retinue_policy_balanced),
     (try_end),
     (troop_get_slot, ":strength_order", ":companion", slot_troop_sod_retinue_strength_order),
     (try_begin),
       (lt, ":strength_order", sod_retinue_strength_none),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
     (else_try),
       (gt, ":strength_order", sod_retinue_strength_full),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_full),
     (try_end),
     (troop_get_slot, ":recruit_policy", ":companion", slot_troop_sod_retinue_recruit_policy),
     (try_begin),
       (troop_slot_eq, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
       (eq, "$g_player_is_captive", 0),
       (neg|troop_slot_ge, ":companion", slot_troop_prisoner_of_party, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_inactive),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_state, sod_retinue_state_inactive),
     (try_end),
     (try_begin),
       (lt, ":recruit_policy", sod_retinue_recruit_policy_none),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
     (else_try),
       (gt, ":recruit_policy", sod_retinue_recruit_policy_eager),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_balanced),
     (try_end),
     (troop_get_slot, ":post_battle_policy", ":companion", slot_troop_sod_retinue_post_battle_policy),
     (try_begin),
       (lt, ":post_battle_policy", sod_retinue_post_battle_enabled),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_post_battle_policy, sod_retinue_post_battle_enabled),
     (else_try),
       (gt, ":post_battle_policy", sod_retinue_post_battle_disabled),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_post_battle_policy, sod_retinue_post_battle_enabled),
     (try_end),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (troop_get_slot, ":quest_stage", ":companion", slot_troop_companion_personal_quest_stage),
     (try_begin),
       (eq, ":quest_stage", sod_companion_quest_failed),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_state, sod_retinue_state_suspended),
     (else_try),
       (this_or_next|eq, "$g_player_is_captive", 1),
       (troop_slot_ge, ":companion", slot_troop_prisoner_of_party, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_state, sod_retinue_state_suspended),
     (else_try),
       (gt, reg0, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_active),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_state, sod_retinue_state_active),
     (else_try),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_inactive),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_state, sod_retinue_state_inactive),
     (try_end),
     (party_set_slot, ":retinue_party", slot_party_sod_retinue_anchor_party, "p_main_party"),
     (store_current_hours, ":cur_hours"),
     (party_set_slot, ":retinue_party", slot_party_sod_retinue_last_sync_hour, ":cur_hours"),
     (assign, ":result", ":retinue_party"),
   (try_end),
   # This is a data-returning helper, not a condition. Return zero when a
   # retinue cannot be created yet so callers can continue their main action.
   (assign, reg0, ":result"),
 ]),

("sod_companion_retinue_get_size",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (party_get_num_companions, ":size", ":retinue_party"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_size, ":size"),
     (assign, reg0, ":size"),
   (try_end),
 ]),

("sod_companion_retinue_get_free_capacity",
 [
   (store_script_param, ":companion", 1),
   (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
   (assign, ":capacity", reg0),
   (call_script, "script_sod_companion_retinue_get_size", ":companion"),
   (store_sub, reg0, ":capacity", reg0),
   (val_max, reg0, 0),
 ]),

("sod_companion_retinue_get_target_size",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_get_slot, ":order", ":companion", slot_troop_sod_retinue_strength_order),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (assign, ":capacity", reg0),
     (try_begin),
       (eq, ":order", sod_retinue_strength_full),
       (assign, reg0, ":capacity"),
     (else_try),
       (eq, ":order", sod_retinue_strength_half),
       (store_div, reg0, ":capacity", 2),
     (else_try),
       (assign, reg0, 0),
     (try_end),
   (try_end),
 ]),

("cf_sod_companion_retinue_accepts_strength_order",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":order", 2),
   (is_between, ":companion", companions_begin, companions_end),
   (is_between, ":order", sod_retinue_strength_none, sod_retinue_strength_full + 1),
   (assign, ":allowed", 1),
   (try_begin),
     (neq, ":order", sod_retinue_strength_none),
     (troop_slot_eq, ":companion", slot_troop_companion_personal_quest_stage, sod_companion_quest_failed),
     (assign, ":allowed", 0),
   (else_try),
     (eq, ":order", sod_retinue_strength_full),
     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (troop_get_slot, ":grievance", ":companion", slot_troop_companion_grievance),
     (val_clamp, ":grievance", 0, 101),
     (try_begin),
       (lt, ":approval", 45),
       (assign, ":allowed", 0),
     (else_try),
       (ge, ":grievance", 75),
       (assign, ":allowed", 0),
     (else_try),
       (troop_slot_eq, ":companion", slot_troop_companion_warning_state, sod_companion_warning_pending),
       (assign, ":allowed", 0),
     (else_try),
       (troop_slot_eq, ":companion", slot_troop_companion_warning_state, sod_companion_warning_final),
       (assign, ":allowed", 0),
     (else_try),
       (troop_slot_eq, ":companion", slot_troop_companion_warning_state, sod_companion_warning_broken),
       (assign, ":allowed", 0),
     (else_try),
       (troop_slot_eq, ":companion", slot_troop_companion_personal_quest_stage, sod_companion_quest_resolved_hard),
       (lt, ":approval", 55),
       (assign, ":allowed", 0),
     (try_end),
   (try_end),
   (eq, ":allowed", 1),
 ]),

("sod_companion_retinue_update_warning_state",
 [
   (store_script_param, ":companion", 1),
   (assign, ":warning_state", sod_retinue_warning_none),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, ":size", reg0),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (assign, ":capacity", reg0),
     (call_script, "script_sod_companion_retinue_get_target_size", ":companion"),
     (assign, ":target_size", reg0),
     (troop_get_slot, ":order", ":companion", slot_troop_sod_retinue_strength_order),
     (assign, ":order_ok", 0),
     (try_begin),
       (call_script, "script_cf_sod_companion_retinue_accepts_strength_order", ":companion", ":order"),
       (assign, ":order_ok", 1),
     (try_end),
     (try_begin),
       (eq, ":order", sod_retinue_strength_none),
       (gt, ":size", 0),
       (assign, ":warning_state", sod_retinue_warning_no_troops_returning),
     (else_try),
       (gt, ":size", ":capacity"),
       (assign, ":warning_state", sod_retinue_warning_over_capacity),
     (else_try),
       (eq, ":order", sod_retinue_strength_full),
       (eq, ":order_ok", 0),
       (assign, ":warning_state", sod_retinue_warning_full_refused),
     (else_try),
       (eq, ":order", sod_retinue_strength_half),
      (store_add, ":upper_band", ":target_size", sod_retinue_half_strength_tolerance),
       (gt, ":size", ":upper_band"),
       (assign, ":warning_state", sod_retinue_warning_above_target),
     (try_end),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_warning_state, ":warning_state"),
   (try_end),
   # Keep this maintenance helper total: invalid/deferred retinues simply have
   # no warning state instead of failing the caller's enclosing try block.
   (assign, reg0, ":warning_state"),
 ]),

("sod_companion_retinue_set_strength_order",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":order", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (is_between, ":order", sod_retinue_strength_none, sod_retinue_strength_full + 1),
     (assign, ":order_ok", 0),
     (try_begin),
       (call_script, "script_cf_sod_companion_retinue_accepts_strength_order", ":companion", ":order"),
       (assign, ":order_ok", 1),
     (try_end),
     (try_begin),
       (eq, ":order_ok", 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_warning_state, sod_retinue_warning_full_refused),
       (str_store_troop_name, s20, ":companion"),
       (display_message, "@{s20} will not take a full-strength command while trust is this strained.", 0xFFCC66),
     (else_try),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_strength_order, ":order"),
       (try_begin),
         (eq, ":order", sod_retinue_strength_none),
         (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
       (else_try),
         (troop_slot_eq, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
         (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_balanced),
       (try_end),
       (call_script, "script_sod_companion_retinue_update_warning_state", ":companion"),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_set_recruit_policy",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":policy", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (is_between, ":policy", sod_retinue_recruit_policy_none, sod_retinue_recruit_policy_eager + 1),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, ":policy"),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_companion_retinue_set_post_battle_policy",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":policy", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (is_between, ":policy", sod_retinue_post_battle_enabled, sod_retinue_post_battle_disabled + 1),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_post_battle_policy, ":policy"),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_companion_retinue_add_gold",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":amount", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (gt, ":amount", 0),
     (store_troop_gold, ":player_gold", "trp_player"),
     (ge, ":player_gold", ":amount"),
     (call_script, "script_sod_player_charge_gold", ":amount"),
     (eq, reg1, 1),
     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (val_max, ":treasury", 0),
     (val_add, ":treasury", ":amount"),
     (val_min, ":treasury", sod_retinue_max_command_purse),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_companion_retinue_remove_gold",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":amount", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (gt, ":amount", 0),
     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (val_max, ":treasury", 0),
     (ge, ":treasury", ":amount"),
     (val_sub, ":treasury", ":amount"),
     (val_max, ":treasury", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
     (call_script, "script_troop_add_gold", "trp_player", ":amount"),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_companion_retinue_get_surplus_gold",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
     (assign, ":reserve", reg0),
     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (val_max, ":treasury", 0),
     (val_min, ":treasury", sod_retinue_max_command_purse),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
     (store_sub, ":surplus", ":treasury", ":reserve"),
     (val_max, ":surplus", 0),
     (assign, reg0, ":surplus"),
   (try_end),
 ]),

("sod_companion_retinue_calculate_recruit_budget",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (neg|troop_slot_eq, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
     (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_recruit_policy),
     (is_between, ":policy", sod_retinue_recruit_policy_cautious, sod_retinue_recruit_policy_eager + 1),
     (call_script, "script_sod_companion_retinue_get_surplus_gold", ":companion"),
     (assign, ":budget", reg0),
     (try_begin),
       (eq, ":policy", sod_retinue_recruit_policy_cautious),
       (val_div, ":budget", 4),
     (else_try),
       (eq, ":policy", sod_retinue_recruit_policy_balanced),
       (val_div, ":budget", 2),
     (else_try),
       (eq, ":policy", sod_retinue_recruit_policy_eager),
       (val_mul, ":budget", 3),
       (val_div, ":budget", 4),
     (try_end),
     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (try_begin),
       (ge, ":approval", 80),
       (val_mul, ":budget", 80),
       (val_div, ":budget", 100),
     (else_try),
       (ge, ":approval", 60),
       (val_mul, ":budget", 90),
       (val_div, ":budget", 100),
     (else_try),
       (lt, ":approval", 20),
       (val_mul, ":budget", 120),
       (val_div, ":budget", 100),
     (else_try),
       (lt, ":approval", 35),
       (val_mul, ":budget", 110),
       (val_div, ":budget", 100),
     (try_end),
     (assign, reg0, ":budget"),
   (try_end),
 ]),

("sod_companion_retinue_calculate_upgrade_budget",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (neg|troop_slot_eq, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
     (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_recruit_policy),
     (is_between, ":policy", sod_retinue_recruit_policy_cautious, sod_retinue_recruit_policy_eager + 1),
     (call_script, "script_sod_companion_retinue_get_surplus_gold", ":companion"),
     (assign, ":budget", reg0),
     (try_begin),
       (eq, ":policy", sod_retinue_recruit_policy_cautious),
       (val_div, ":budget", 5),
     (else_try),
       (eq, ":policy", sod_retinue_recruit_policy_balanced),
       (val_mul, ":budget", 35),
       (val_div, ":budget", 100),
     (else_try),
       (eq, ":policy", sod_retinue_recruit_policy_eager),
       (val_div, ":budget", 2),
     (try_end),
     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (try_begin),
       (ge, ":approval", 80),
       (val_mul, ":budget", 80),
       (val_div, ":budget", 100),
     (else_try),
       (ge, ":approval", 60),
       (val_mul, ":budget", 90),
       (val_div, ":budget", 100),
     (else_try),
       (lt, ":approval", 20),
       (val_mul, ":budget", 120),
       (val_div, ":budget", 100),
     (else_try),
       (lt, ":approval", 35),
       (val_mul, ":budget", 110),
       (val_div, ":budget", 100),
     (try_end),
     (assign, reg0, ":budget"),
   (try_end),
 ]),

("cf_sod_companion_retinue_can_self_manage",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":mode", 2),
   (is_between, ":companion", companions_begin, companions_end),
   (main_party_has_troop, ":companion"),
   (neg|troop_slot_ge, ":companion", slot_troop_prisoner_of_party, 0),
   (store_troop_health, ":health", ":companion"),
   (ge, ":health", 35),
   (troop_get_slot, ":warning_state", ":companion", slot_troop_companion_warning_state),
   (neq, ":warning_state", sod_companion_warning_pending),
   (neq, ":warning_state", sod_companion_warning_final),
   (neq, ":warning_state", sod_companion_warning_broken),
   (neg|troop_slot_eq, ":companion", slot_troop_companion_personal_quest_stage, sod_companion_quest_failed),
   (troop_get_slot, ":retinue_state", ":companion", slot_troop_sod_retinue_state),
   (neq, ":retinue_state", sod_retinue_state_suspended),
   (neq, ":retinue_state", sod_retinue_state_detached),
   (neq, ":retinue_state", sod_retinue_state_pending_cleanup),
   (eq, "$g_player_is_captive", 0),
   (assign, ":mode_ok", 0),
   (try_begin),
     (eq, ":mode", 0),
     (map_free),
     (eq, "$g_sod_retinue_battle_bridge_active", 0),
     (assign, ":mode_ok", 1),
   (else_try),
     (eq, ":mode", 1),
     (eq, "$g_battle_result", 1),
     (assign, ":mode_ok", 1),
   (try_end),
   (eq, ":mode_ok", 1),
 ]),

("sod_companion_retinue_select_recruit_troop",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, "trp_watchman"),
   (try_begin),
     (eq, ":companion", "trp_npc1"),
     (assign, reg0, "trp_khergit_tribesman"),
   (else_try),
     (eq, ":companion", "trp_npc2"),
     (assign, reg0, "trp_caravan_guard"),
   (else_try),
     (eq, ":companion", "trp_npc3"),
     (assign, reg0, "trp_refugee"),
   (else_try),
     (eq, ":companion", "trp_npc4"),
     (assign, reg0, "trp_swadian_recruit"),
   (else_try),
     (eq, ":companion", "trp_npc5"),
     (assign, reg0, "trp_khergit_tribesman"),
   (else_try),
     (eq, ":companion", "trp_npc6"),
     (assign, reg0, "trp_swadian_recruit"),
   (else_try),
     (eq, ":companion", "trp_npc7"),
     (assign, reg0, "trp_vaegir_recruit"),
   (else_try),
     (eq, ":companion", "trp_npc8"),
     (assign, reg0, "trp_nord_recruit"),
   (else_try),
     (eq, ":companion", "trp_npc9"),
     (assign, reg0, "trp_swadian_recruit"),
   (else_try),
     (eq, ":companion", "trp_npc10"),
     (assign, reg0, "trp_rhodok_tribesman"),
   (else_try),
     (eq, ":companion", "trp_npc11"),
     (assign, reg0, "trp_caravan_guard"),
   (else_try),
     (eq, ":companion", "trp_npc12"),
     (assign, reg0, "trp_manhunter"),
   (else_try),
     (eq, ":companion", "trp_npc13"),
     (assign, reg0, "trp_sod_ade_light"),
   (else_try),
     (eq, ":companion", "trp_npc14"),
     (assign, reg0, "trp_sod_mar_conscript"),
   (else_try),
     (eq, ":companion", "trp_npc15"),
     (assign, reg0, "trp_sod_mar_crossbowman"),
   (else_try),
     (eq, ":companion", "trp_npc16"),
     (assign, reg0, "trp_watchman"),
   (try_end),
   (try_begin),
     (eq, reg0, "trp_watchman"),
     (troop_get_slot, ":companion_role", ":companion", slot_troop_companion_role),
     (try_begin),
       (eq, ":companion_role", sod_companion_role_quartermaster),
       (assign, reg0, "trp_caravan_guard"),
     (else_try),
       (eq, ":companion_role", sod_companion_role_surgeon),
       (assign, reg0, "trp_refugee"),
     (else_try),
       (eq, ":companion_role", sod_companion_role_scout),
       (assign, reg0, "trp_khergit_tribesman"),
     (else_try),
       (eq, ":companion_role", sod_companion_role_captain),
       (assign, reg0, "trp_swadian_recruit"),
     (else_try),
       (eq, ":companion_role", sod_companion_role_engineer),
       (assign, reg0, "trp_rhodok_tribesman"),
     (try_end),
   (try_end),
 ]),

("cf_sod_companion_retinue_troop_matches_preference",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":troop", 2),
   (is_between, ":troop", soldiers_begin, soldiers_end),
   (assign, ":matches", 0),
   (call_script, "script_sod_companion_retinue_select_recruit_troop", ":companion"),
   (try_begin),
     (eq, ":troop", reg0),
     (assign, ":matches", 1),
   (else_try),
     (call_script, "script_sod_companion_retinue_get_identity", ":companion"),
     (assign, ":identity", reg0),
     (try_begin),
       (this_or_next|eq, ":identity", sod_retinue_pref_crossbow_veteran),
       (eq, ":identity", sod_retinue_pref_engineer_support),
       (this_or_next|eq, ":troop", "trp_rhodok_tribesman"),
       (this_or_next|eq, ":troop", "trp_sod_mar_crossbowman"),
       (eq, ":troop", "trp_caravan_guard"),
       (assign, ":matches", 1),
     (else_try),
       (this_or_next|eq, ":identity", sod_retinue_pref_horse_archer),
       (this_or_next|eq, ":identity", sod_retinue_pref_scout_irregular),
       (eq, ":identity", sod_retinue_pref_glory_cavalry),
       (this_or_next|eq, ":troop", "trp_khergit_tribesman"),
       (this_or_next|eq, ":troop", "trp_sod_ade_light"),
       (eq, ":troop", "trp_watchman"),
       (assign, ":matches", 1),
     (else_try),
       (this_or_next|eq, ":identity", sod_retinue_pref_mercy_guard),
       (this_or_next|eq, ":identity", sod_retinue_pref_healer_escort),
       (eq, ":identity", sod_retinue_pref_household_guard),
       (this_or_next|eq, ":troop", "trp_refugee"),
       (this_or_next|eq, ":troop", "trp_caravan_guard"),
       (eq, ":troop", "trp_manhunter"),
       (assign, ":matches", 1),
     (else_try),
       (this_or_next|eq, ":identity", sod_retinue_pref_field_captain),
       (this_or_next|eq, ":identity", sod_retinue_pref_redeemed_infantry),
       (this_or_next|eq, ":identity", sod_retinue_pref_drilled_infantry),
       (eq, ":identity", sod_retinue_pref_noble_guard),
       (this_or_next|eq, ":troop", "trp_swadian_recruit"),
       (this_or_next|eq, ":troop", "trp_sod_mar_conscript"),
       (eq, ":troop", "trp_manhunter"),
       (assign, ":matches", 1),
     (else_try),
       (this_or_next|eq, ":identity", sod_retinue_pref_archer_tracker),
       (eq, ":identity", sod_retinue_pref_skirmisher),
       (this_or_next|eq, ":troop", "trp_vaegir_recruit"),
       (this_or_next|eq, ":troop", "trp_watchman"),
       (eq, ":troop", "trp_sod_mar_crossbowman"),
       (assign, ":matches", 1),
     (else_try),
       (eq, ":identity", sod_retinue_pref_shield_wall),
       (this_or_next|eq, ":troop", "trp_nord_recruit"),
       (eq, ":troop", "trp_swadian_recruit"),
       (assign, ":matches", 1),
     (try_end),
   (try_end),
   (eq, ":matches", 1),
 ]),

("sod_companion_retinue_select_local_recruit_troop",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (assign, reg1, -1),
   (assign, reg2, 0),
   (assign, ":center", -1),
   (try_begin),
     (is_between, "$current_town", centers_begin, centers_end),
     (party_is_active, "$current_town"),
     (store_distance_to_party_from_party, ":dist_to_current", "p_main_party", "$current_town"),
     (le, ":dist_to_current", 4),
     (assign, ":center", "$current_town"),
   (else_try),
     (assign, ":best_dist", 5),
     (try_for_range, ":village", villages_begin, villages_end),
       (party_is_active, ":village"),
       (party_slot_ge, ":village", slot_center_volunteer_troop_amount, 1),
       (party_slot_ge, ":village", slot_center_volunteer_troop_type, 1),
       (store_distance_to_party_from_party, ":dist", "p_main_party", ":village"),
       (lt, ":dist", ":best_dist"),
       (assign, ":best_dist", ":dist"),
       (assign, ":center", ":village"),
     (try_end),
   (try_end),
   (try_begin),
     (is_between, ":center", centers_begin, centers_end),
     (party_is_active, ":center"),
     (assign, ":recruit_troop", 0),
     (assign, ":available", 0),
     (try_begin),
       (party_get_slot, ":volunteer_troop", ":center", slot_center_volunteer_troop_type),
       (party_get_slot, ":volunteer_amount", ":center", slot_center_volunteer_troop_amount),
       (is_between, ":volunteer_troop", soldiers_begin, soldiers_end),
       (gt, ":volunteer_amount", 0),
       (assign, ":recruit_troop", ":volunteer_troop"),
       (assign, ":available", ":volunteer_amount"),
     (else_try),
       (party_slot_eq, ":center", slot_party_type, spt_town),
       (store_faction_of_party, ":center_faction", ":center"),
       (try_begin),
         (eq, ":center_faction", "fac_kingdom_1"),
         (assign, ":recruit_troop", "trp_swadian_recruit"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_2"),
         (assign, ":recruit_troop", "trp_vaegir_recruit"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_3"),
         (assign, ":recruit_troop", "trp_khergit_tribesman"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_4"),
         (assign, ":recruit_troop", "trp_nord_recruit"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_5"),
         (assign, ":recruit_troop", "trp_rhodok_tribesman"),
       (else_try),
         (call_script, "script_sod_companion_retinue_select_recruit_troop", ":companion"),
         (assign, ":recruit_troop", reg0),
       (try_end),
       (assign, ":available", 2),
     (else_try),
       (party_slot_eq, ":center", slot_party_type, spt_castle),
       (store_faction_of_party, ":center_faction", ":center"),
       (try_begin),
         (eq, ":center_faction", "fac_kingdom_1"),
         (assign, ":recruit_troop", "trp_swadian_recruit"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_2"),
         (assign, ":recruit_troop", "trp_vaegir_recruit"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_3"),
         (assign, ":recruit_troop", "trp_khergit_tribesman"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_4"),
         (assign, ":recruit_troop", "trp_nord_recruit"),
       (else_try),
         (eq, ":center_faction", "fac_kingdom_5"),
         (assign, ":recruit_troop", "trp_rhodok_tribesman"),
       (else_try),
         (call_script, "script_sod_companion_retinue_select_recruit_troop", ":companion"),
         (assign, ":recruit_troop", reg0),
       (try_end),
       (assign, ":available", 1),
     (try_end),
     (is_between, ":recruit_troop", soldiers_begin, soldiers_end),
     (gt, ":available", 0),
     (assign, reg0, ":recruit_troop"),
     (assign, reg1, ":center"),
     (assign, reg2, ":available"),
   (try_end),
 ]),

("sod_companion_retinue_get_recruit_cost",
 [
   (store_script_param, ":troop", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (store_character_level, ":level", ":troop"),
     (store_mul, ":cost", ":level", 12),
     (val_add, ":cost", 20),
     (val_max, ":cost", 25),
     (assign, reg0, ":cost"),
   (try_end),
 ]),

("sod_companion_retinue_select_upgrade_troop",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":troop", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":troop"),
     (troop_get_upgrade_troop, ":upgrade1", ":troop", 0),
     (troop_get_upgrade_troop, ":upgrade2", ":troop", 1),
     (try_begin),
       (le, ":upgrade1", 0),
       (troop_get_slot, ":upgrade1", ":troop", slot_troop_sod_upgrade1),
     (try_end),
     (try_begin),
       (le, ":upgrade2", 0),
       (troop_get_slot, ":upgrade2", ":troop", slot_troop_sod_upgrade2),
     (try_end),
     (assign, ":selected_upgrade", ":upgrade1"),
     (try_begin),
       (this_or_next|eq, ":companion", "trp_npc7"),
       (this_or_next|eq, ":companion", "trp_npc10"),
       (this_or_next|eq, ":companion", "trp_npc15"),
       (eq, ":companion", "trp_npc16"),
       (is_between, ":upgrade2", soldiers_begin, soldiers_end),
       (assign, ":selected_upgrade", ":upgrade2"),
     (try_end),
     (try_begin),
       (neg|is_between, ":selected_upgrade", soldiers_begin, soldiers_end),
       (assign, ":selected_upgrade", ":upgrade1"),
     (try_end),
     (is_between, ":selected_upgrade", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":selected_upgrade"),
     (neq, ":selected_upgrade", ":troop"),
     (call_script, "script_sod_troop_get_elite_tier", ":selected_upgrade"),
     (lt, reg0, sod_elite_tier_noble),
     (assign, reg0, ":selected_upgrade"),
   (try_end),
 ]),

("sod_companion_retinue_get_upgrade_cost",
 [
   (store_script_param, ":troop", 1),
   (store_script_param, ":upgrade", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (is_between, ":upgrade", soldiers_begin, soldiers_end),
     (neq, ":troop", ":upgrade"),
     (store_character_level, ":old_level", ":troop"),
     (store_character_level, ":new_level", ":upgrade"),
     (store_sub, ":level_gain", ":new_level", ":old_level"),
     (val_max, ":level_gain", 1),
     (store_mul, ":cost", ":new_level", 15),
     (store_mul, ":gain_cost", ":level_gain", 25),
     (val_add, ":cost", ":gain_cost"),
     (try_begin),
       (troop_is_mounted, ":upgrade"),
       (val_mul, ":cost", 3),
       (val_div, ":cost", 2),
     (try_end),
     (val_max, ":cost", 35),
     (assign, reg0, ":cost"),
   (try_end),
 ]),

("sod_companion_retinue_can_accept_troop",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":troop", 2),
   (store_script_param, ":amount", 3),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (gt, ":amount", 0),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":troop"),
     (call_script, "script_sod_companion_retinue_sanitize_state", ":companion"),
     (party_count_members_of_type, ":available", "p_main_party", ":troop"),
     (ge, ":available", ":amount"),
     (call_script, "script_sod_companion_retinue_get_free_capacity", ":companion"),
     (ge, reg0, ":amount"),
     (store_troop_health, ":health", ":companion"),
     (ge, ":health", 35),
     (troop_get_slot, ":retinue_state", ":companion", slot_troop_sod_retinue_state),
     (neq, ":retinue_state", sod_retinue_state_suspended),
     (neq, ":retinue_state", sod_retinue_state_detached),
     (neq, ":retinue_state", sod_retinue_state_pending_cleanup),
     (neg|troop_slot_ge, ":companion", slot_troop_prisoner_of_party, 0),
     (neg|troop_slot_eq, ":companion", slot_troop_companion_personal_quest_stage, sod_companion_quest_failed),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_companion_retinue_add_troops",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":troop", 2),
   (store_script_param, ":amount", 3),
   (assign, reg0, 0),
   (try_begin),
     (gt, ":amount", 0),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":troop"),
     (call_script, "script_sod_companion_retinue_can_accept_troop", ":companion", ":troop", ":amount"),
     (eq, reg0, 1),
     (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (assign, ":retinue_party", reg0),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (party_count_members_of_type, ":available_now", "p_main_party", ":troop"),
     (ge, ":available_now", ":amount"),
     (party_remove_members, "p_main_party", ":troop", ":amount"),
     (party_add_members, ":retinue_party", ":troop", ":amount"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_active),
     (party_set_slot, ":retinue_party", slot_party_sod_retinue_state, sod_retinue_state_active),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_companion_retinue_remove_troops",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":troop", 2),
   (store_script_param, ":amount", 3),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (gt, ":amount", 0),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":troop"),
     (call_script, "script_sod_companion_retinue_sanitize_state", ":companion"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (troop_get_slot, ":retinue_state", ":companion", slot_troop_sod_retinue_state),
     (neq, ":retinue_state", sod_retinue_state_suspended),
     (neq, ":retinue_state", sod_retinue_state_detached),
     (neq, ":retinue_state", sod_retinue_state_pending_cleanup),
     (party_count_members_of_type, ":available", ":retinue_party", ":troop"),
     (ge, ":available", ":amount"),
     (party_get_free_companions_capacity, ":free_capacity", "p_main_party"),
     (ge, ":free_capacity", ":amount"),
     (party_remove_members, ":retinue_party", ":troop", ":amount"),
     (party_add_members, "p_main_party", ":troop", ":amount"),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_companion_retinue_add_troops_up_to_capacity",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":troop", 2),
   (store_script_param, ":requested", 3),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (gt, ":requested", 0),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":troop"),
     (party_count_members_of_type, ":available", "p_main_party", ":troop"),
     (gt, ":available", 0),
     (call_script, "script_sod_companion_retinue_get_free_capacity", ":companion"),
     (assign, ":transfer", reg0),
     (val_min, ":transfer", ":available"),
     (val_min, ":transfer", ":requested"),
     (gt, ":transfer", 0),
     (call_script, "script_sod_companion_retinue_add_troops", ":companion", ":troop", ":transfer"),
     (try_begin),
       (eq, reg0, 1),
       (assign, reg0, ":transfer"),
     (else_try),
       (assign, reg0, 0),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_remove_troops_up_to_capacity",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":troop", 2),
   (store_script_param, ":requested", 3),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (gt, ":requested", 0),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":troop"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (party_count_members_of_type, ":available", ":retinue_party", ":troop"),
     (gt, ":available", 0),
     (party_get_free_companions_capacity, ":free_capacity", "p_main_party"),
     (gt, ":free_capacity", 0),
     (assign, ":transfer", ":available"),
     (val_min, ":transfer", ":free_capacity"),
     (val_min, ":transfer", ":requested"),
     (gt, ":transfer", 0),
     (call_script, "script_sod_companion_retinue_remove_troops", ":companion", ":troop", ":transfer"),
     (try_begin),
       (eq, reg0, 1),
       (assign, reg0, ":transfer"),
     (else_try),
       (assign, reg0, 0),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_select_main_party_troop",
 [
   (store_script_param, ":current_troop", 1),
   (store_script_param, ":advance", 2),
   (assign, reg0, 0),
   (assign, reg1, 0),
   (assign, ":first_troop", 0),
   (assign, ":first_count", 0),
   (assign, ":found_current", 0),
   (try_begin),
     (le, ":current_troop", 0),
     (assign, ":advance", 0),
   (try_end),
   (party_get_num_companion_stacks, ":num_stacks", "p_main_party"),
     (try_for_range, ":stack_no", 0, ":num_stacks"),
     (party_stack_get_troop_id, ":stack_troop", "p_main_party", ":stack_no"),
     (is_between, ":stack_troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":stack_troop"),
     (party_stack_get_size, ":stack_size", "p_main_party", ":stack_no"),
     (gt, ":stack_size", 0),
     (try_begin),
       (eq, ":first_troop", 0),
       (assign, ":first_troop", ":stack_troop"),
       (assign, ":first_count", ":stack_size"),
     (try_end),
     (try_begin),
       (eq, ":advance", 0),
       (eq, ":stack_troop", ":current_troop"),
       (assign, reg0, ":stack_troop"),
       (assign, reg1, ":stack_size"),
     (else_try),
       (eq, ":advance", 1),
       (eq, ":found_current", 1),
       (eq, reg0, 0),
       (assign, reg0, ":stack_troop"),
       (assign, reg1, ":stack_size"),
     (try_end),
     (try_begin),
       (eq, ":stack_troop", ":current_troop"),
       (assign, ":found_current", 1),
     (try_end),
   (try_end),
   (try_begin),
     (eq, reg0, 0),
     (assign, reg0, ":first_troop"),
     (assign, reg1, ":first_count"),
   (try_end),
 ]),

("sod_companion_retinue_select_retinue_troop",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":current_troop", 2),
   (store_script_param, ":advance", 3),
   (assign, reg0, 0),
   (assign, reg1, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (assign, ":first_troop", 0),
     (assign, ":first_count", 0),
     (assign, ":found_current", 0),
     (try_begin),
       (le, ":current_troop", 0),
       (assign, ":advance", 0),
     (try_end),
     (party_get_num_companion_stacks, ":num_stacks", ":retinue_party"),
       (try_for_range, ":stack_no", 0, ":num_stacks"),
       (party_stack_get_troop_id, ":stack_troop", ":retinue_party", ":stack_no"),
       (is_between, ":stack_troop", soldiers_begin, soldiers_end),
       (neg|troop_is_hero, ":stack_troop"),
       (party_stack_get_size, ":stack_size", ":retinue_party", ":stack_no"),
       (gt, ":stack_size", 0),
       (try_begin),
         (eq, ":first_troop", 0),
         (assign, ":first_troop", ":stack_troop"),
         (assign, ":first_count", ":stack_size"),
       (try_end),
       (try_begin),
         (eq, ":advance", 0),
         (eq, ":stack_troop", ":current_troop"),
         (assign, reg0, ":stack_troop"),
         (assign, reg1, ":stack_size"),
       (else_try),
         (eq, ":advance", 1),
         (eq, ":found_current", 1),
         (eq, reg0, 0),
         (assign, reg0, ":stack_troop"),
         (assign, reg1, ":stack_size"),
       (try_end),
       (try_begin),
         (eq, ":stack_troop", ":current_troop"),
         (assign, ":found_current", 1),
       (try_end),
     (try_end),
     (try_begin),
       (eq, reg0, 0),
       (assign, reg0, ":first_troop"),
       (assign, reg1, ":first_count"),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_describe_transfer_to_s1",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":mode", 2),
   (store_script_param, ":selected_troop", 3),
   (store_script_param, ":selected_count", 4),
   (call_script, "script_sod_companion_retinue_describe_focus_to_s1", ":companion"),
   (str_store_string_reg, s98, s1),
   (try_begin),
     (gt, ":selected_troop", 0),
     (str_store_troop_name_by_count, s4, ":selected_troop", ":selected_count"),
     (assign, reg25, ":selected_count"),
     (try_begin),
       (eq, ":mode", 1),
       (str_store_string_reg, s97, s98),
       (str_store_string, s98, "@{s97}^^Selected from your company: {reg25} {s4}."),
       (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
       (store_character_level, ":troop_level", ":selected_troop"),
       (call_script, "script_sod_companion_retinue_get_recruit_cost", ":selected_troop"),
       (assign, ":troop_value", reg0),
       (try_begin),
         (this_or_next|ge, ":troop_level", 12),
         (ge, ":troop_value", 140),
         (lt, ":approval", 45),
         (str_store_string_reg, s97, s98),
         (str_store_string, s98, "@{s97}^Warning: loyalty is strained. Assigning costly troops to this command may worsen unrest if the retinue is later underpaid, overfilled, or pushed too hard."),
       (try_end),
     (else_try),
       (str_store_string_reg, s97, s98),
       (str_store_string, s98, "@{s97}^^Selected from this retinue: {reg25} {s4}."),
       (party_get_free_companions_capacity, ":free_capacity", "p_main_party"),
       (assign, reg26, ":free_capacity"),
       (try_begin),
         (gt, ":selected_count", ":free_capacity"),
         (str_store_string_reg, s97, s98),
         (str_store_string, s98, "@{s97}^Warning: your personal party can currently receive only {reg26}. Reclaiming the whole stack would exceed your direct command capacity."),
       (try_end),
     (try_end),
   (else_try),
     (try_begin),
       (eq, ":mode", 1),
       (str_store_string_reg, s97, s98),
       (str_store_string, s98, "@{s97}^^No regular troop stacks in your company are eligible for assignment."),
     (else_try),
       (str_store_string_reg, s97, s98),
       (str_store_string, s98, "@{s97}^^This retinue has no regular troop stacks to reclaim."),
     (try_end),
   (try_end),
   (str_store_string_reg, s1, s98),
 ]),

("sod_companion_retinue_get_weekly_wage",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (call_script, "script_calculate_weekly_party_wage", ":retinue_party"),
     (assign, ":weekly_wage", reg0),
     (store_skill_level, ":leadership", "skl_leadership", ":companion"),
     (store_mul, ":leadership_discount", ":leadership", 3),
     (val_min, ":leadership_discount", 30),
     (store_sub, ":wage_percent", 100, ":leadership_discount"),
     (val_mul, ":weekly_wage", ":wage_percent"),
     (val_div, ":weekly_wage", 100),
     (val_max, ":weekly_wage", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_wage, ":weekly_wage"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_invoice, ":weekly_wage"),
     (assign, reg0, ":weekly_wage"),
   (try_end),
 ]),

("sod_companion_retinue_update_wage_reserve",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (assign, reg1, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
     (assign, ":weekly_wage", reg0),
     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (val_max, ":treasury", 0),
     (assign, ":reserve", 0),
     (try_begin),
       (gt, ":weekly_wage", 0),
       (store_mul, ":target_reserve", ":weekly_wage", 2),
       (assign, ":reserve", ":treasury"),
       (val_min, ":reserve", ":target_reserve"),
       (store_div, reg1, ":treasury", ":weekly_wage"),
     (try_end),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_wage_reserve, ":reserve"),
     (assign, reg0, ":reserve"),
   (try_end),
 ]),

("sod_companion_retinue_pay_weekly_wages",
 [
   (assign, ":total_paid_by_purses", 0),
   (assign, ":total_retinue_wages", 0),
   (assign, ":total_shortage", 0),
   (assign, ":player_covered_shortage", 0),
   (assign, ":unpaid_shortage", 0),
   (assign, ":shortage_policy", sod_retinue_wage_shortage_player_auto_cover),
   (try_begin),
     (eq, "$g_sod_retinue_wage_shortage_policy", sod_retinue_wage_shortage_purse_only),
     (assign, ":shortage_policy", sod_retinue_wage_shortage_purse_only),
   (try_end),
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (neg|troop_slot_eq, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
     (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
     (assign, ":weekly_wage", reg0),
     (gt, ":weekly_wage", 0),
     (val_add, ":total_retinue_wages", ":weekly_wage"),
     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (val_max, ":treasury", 0),
     (assign, ":paid", ":weekly_wage"),
     (val_min, ":paid", ":treasury"),
     (try_begin),
       (gt, ":paid", 0),
       (val_sub, ":treasury", ":paid"),
       (val_max, ":treasury", 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
       (val_add, ":total_paid_by_purses", ":paid"),
     (try_end),
     (store_sub, ":shortage", ":weekly_wage", ":paid"),
     (val_max, ":shortage", 0),
     (val_add, ":total_shortage", ":shortage"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_shortage, ":shortage"),
     (try_begin),
       (gt, ":shortage", 0),
       (eq, ":shortage_policy", sod_retinue_wage_shortage_purse_only),
       (call_script, "script_sod_companion_retinue_apply_unpaid_wage_consequences", ":companion", ":shortage"),
     (try_end),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
   (try_end),
   (try_begin),
     (eq, ":shortage_policy", sod_retinue_wage_shortage_player_auto_cover),
     (assign, ":player_covered_shortage", ":total_shortage"),
   (else_try),
     (assign, ":unpaid_shortage", ":total_shortage"),
   (try_end),
   (assign, reg0, ":total_paid_by_purses"),
   (assign, reg1, ":total_retinue_wages"),
   (assign, reg2, ":total_shortage"),
   (assign, reg3, ":player_covered_shortage"),
   (assign, reg4, ":unpaid_shortage"),
 ]),

("sod_companion_retinue_apply_unpaid_wage_consequences",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":shortage", 2),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (gt, ":shortage", 0),
     (call_script, "script_sod_companion_retinue_update_supply_and_morale", ":companion"),
     (troop_get_slot, ":morale", ":companion", slot_troop_sod_retinue_last_morale),
     (store_div, ":morale_loss", ":shortage", 100),
     (val_add, ":morale_loss", 2),
     (val_clamp, ":morale_loss", 2, 12),
     (val_sub, ":morale", ":morale_loss"),
     (val_clamp, ":morale", 0, 101),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_morale, ":morale"),
     (try_begin),
       (le, ":morale", 20),
       (call_script, "script_sod_companion_retinue_apply_desertion_risk", ":companion", 1),
     (try_end),
     (str_store_troop_name, s20, ":companion"),
     (assign, reg20, ":shortage"),
     (display_message, "@{s20}'s retinue went unpaid by {reg20} denars. Cohesion in that command suffers.", 0xFFCC66),
   (try_end),
 ]),

("sod_companion_retinue_update_supply_and_morale",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (assign, reg1, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, ":size", reg0),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (assign, ":capacity", reg0),
     (store_skill_level, ":leadership", "skl_leadership", ":companion"),
     (troop_get_slot, ":companion_role", ":companion", slot_troop_companion_role),
     (call_script, "script_sod_companion_retinue_get_identity", ":companion"),
     (assign, ":identity", reg0),
     (assign, ":martial_grade", reg1),
     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (troop_get_slot, ":companion_warning_state", ":companion", slot_troop_companion_warning_state),
     (troop_get_slot, ":grievance", ":companion", slot_troop_companion_grievance),
     (val_clamp, ":grievance", 0, 101),
     (troop_get_slot, ":quest_stage", ":companion", slot_troop_companion_personal_quest_stage),
     (troop_get_slot, ":shortage", ":companion", slot_troop_sod_retinue_last_shortage),
     (val_max, ":shortage", 0),

     (store_div, ":supply_pressure", ":size", 5),
     (try_begin),
       (gt, ":size", 0),
       (val_add, ":supply_pressure", 1),
     (try_end),
     (try_begin),
       (gt, ":size", ":capacity"),
       (store_sub, ":over_capacity", ":size", ":capacity"),
       (store_mul, ":over_supply", ":over_capacity", 2),
       (val_add, ":supply_pressure", ":over_supply"),
     (try_end),
     (try_begin),
       (eq, ":companion_warning_state", sod_companion_warning_pending),
       (val_add, ":supply_pressure", 1),
     (else_try),
       (eq, ":companion_warning_state", sod_companion_warning_acknowledged),
       (val_add, ":supply_pressure", 1),
     (else_try),
       (eq, ":companion_warning_state", sod_companion_warning_final),
       (val_add, ":supply_pressure", 2),
     (else_try),
       (eq, ":companion_warning_state", sod_companion_warning_broken),
       (val_add, ":supply_pressure", 3),
     (try_end),
     (try_begin),
       (ge, ":grievance", 80),
       (val_add, ":supply_pressure", 2),
     (else_try),
       (ge, ":grievance", 60),
       (val_add, ":supply_pressure", 1),
     (try_end),
     (try_begin),
       (eq, ":companion_role", sod_companion_role_quartermaster),
       (val_sub, ":supply_pressure", 1),
       (val_max, ":supply_pressure", 0),
     (try_end),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_supply_pressure, ":supply_pressure"),

     (assign, ":morale", 60),
     (store_div, ":size_pressure", ":size", 3),
     (val_sub, ":morale", ":size_pressure"),
     (store_mul, ":leadership_relief", ":leadership", 3),
     (val_add, ":morale", ":leadership_relief"),
     (try_begin),
       (eq, ":companion_role", sod_companion_role_captain),
       (val_add, ":morale", 3),
     (try_end),
     (try_begin),
       (ge, ":martial_grade", 3),
       (val_add, ":morale", 4),
     (else_try),
       (ge, ":martial_grade", 2),
       (val_add, ":morale", 3),
     (else_try),
       (ge, ":martial_grade", 1),
       (val_add, ":morale", 1),
     (try_end),
     (try_begin),
       (this_or_next|eq, ":identity", sod_retinue_pref_mercy_guard),
       (this_or_next|eq, ":identity", sod_retinue_pref_healer_escort),
       (this_or_next|eq, ":identity", sod_retinue_pref_household_guard),
       (eq, ":identity", sod_retinue_pref_engineer_support),
       (val_add, ":morale", 2),
     (try_end),
     (try_begin),
       (ge, ":approval", 80),
       (val_add, ":morale", 5),
     (else_try),
       (ge, ":approval", 60),
       (val_add, ":morale", 3),
     (else_try),
       (lt, ":approval", 20),
       (val_sub, ":morale", 8),
     (else_try),
       (lt, ":approval", 35),
       (val_sub, ":morale", 4),
     (try_end),
     (try_begin),
       (ge, ":grievance", 80),
       (val_sub, ":morale", 8),
     (else_try),
       (ge, ":grievance", 60),
       (val_sub, ":morale", 5),
     (else_try),
       (ge, ":grievance", 40),
       (val_sub, ":morale", 2),
     (else_try),
       (lt, ":grievance", 20),
       (val_add, ":morale", 2),
     (try_end),
     (try_begin),
       (eq, ":companion_warning_state", sod_companion_warning_pending),
       (val_sub, ":morale", 6),
     (else_try),
       (eq, ":companion_warning_state", sod_companion_warning_acknowledged),
       (val_sub, ":morale", 4),
     (else_try),
       (eq, ":companion_warning_state", sod_companion_warning_final),
       (val_sub, ":morale", 12),
     (else_try),
       (eq, ":companion_warning_state", sod_companion_warning_broken),
       (val_sub, ":morale", 18),
     (try_end),
     (try_begin),
       (eq, ":quest_stage", sod_companion_quest_resolved_good),
       (val_add, ":morale", 3),
     (else_try),
       (eq, ":quest_stage", sod_companion_quest_resolved_hard),
       (val_sub, ":morale", 3),
     (else_try),
       (eq, ":quest_stage", sod_companion_quest_failed),
       (val_sub, ":morale", 20),
     (try_end),
     (try_begin),
       (gt, ":size", ":capacity"),
       (store_sub, ":over_capacity", ":size", ":capacity"),
       (store_mul, ":over_morale", ":over_capacity", 5),
       (val_sub, ":morale", ":over_morale"),
     (try_end),
     (try_begin),
       (gt, ":shortage", 0),
       (store_div, ":shortage_pressure", ":shortage", 75),
       (val_add, ":shortage_pressure", 1),
       (val_sub, ":morale", ":shortage_pressure"),
     (try_end),
     (try_begin),
       (eq, "$g_sod_company_ration_policy", sod_company_ration_policy_thin),
       (val_sub, ":morale", 3),
     (else_try),
       (eq, "$g_sod_company_ration_policy", sod_company_ration_policy_generous),
       (val_add, ":morale", 2),
     (try_end),
     (val_clamp, ":morale", 0, 101),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_morale, ":morale"),
     (assign, reg0, ":morale"),
     (assign, reg1, ":supply_pressure"),
   (try_end),
 ]),

("sod_companion_retinue_apply_desertion_risk",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":max_deserters", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (gt, ":max_deserters", 0),
     (store_current_day, ":cur_day"),
     (troop_get_slot, ":last_day", ":companion", slot_troop_sod_retinue_last_desertion_day),
     (neq, ":last_day", ":cur_day"),
     (call_script, "script_sod_companion_retinue_select_retinue_troop", ":companion", 0, 0),
     (assign, ":troop", reg0),
     (assign, ":available", reg1),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (is_between, ":troop", soldiers_begin, soldiers_end),
     (gt, ":available", 0),
     (assign, ":deserters", ":available"),
     (val_min, ":deserters", ":max_deserters"),
     (party_remove_members, ":retinue_party", ":troop", ":deserters"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_desertion_day, ":cur_day"),
     (str_store_troop_name, s20, ":companion"),
     (str_store_troop_name_by_count, s21, ":troop", ":deserters"),
     (assign, reg20, ":deserters"),
     (display_message, "@{reg20} {s21} slip away from {s20}'s retinue after poor pay, tight supplies, or over-capacity strain.", 0xFF6666),
     (assign, reg0, ":deserters"),
   (try_end),
 ]),

("sod_companion_retinue_apply_training",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (call_script, "script_cf_sod_companion_retinue_can_self_manage", ":companion", 0),
     (store_skill_level, ":trainer", "skl_trainer", ":companion"),
     (gt, ":trainer", 0),
     (store_current_hours, ":cur_hours"),
     (troop_get_slot, ":last_training_hour", ":companion", slot_troop_sod_retinue_last_training_hour),
     (store_sub, ":hours_since_training", ":cur_hours", ":last_training_hour"),
     (this_or_next|le, ":last_training_hour", 0),
     (ge, ":hours_since_training", 6),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (party_get_num_companions, ":retinue_size", ":retinue_party"),
     (gt, ":retinue_size", 0),
     (store_mul, ":xp_per_stack", ":trainer", 8),
     (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_policy),
     (try_begin),
       (eq, ":policy", sod_retinue_policy_training),
       (val_add, ":xp_per_stack", 10),
     (try_end),
     (troop_get_slot, ":quest_stage", ":companion", slot_troop_companion_personal_quest_stage),
     (try_begin),
       (eq, ":quest_stage", sod_companion_quest_resolved_good),
       (val_add, ":xp_per_stack", 4),
     (else_try),
       (eq, ":quest_stage", sod_companion_quest_resolved_hard),
       (val_add, ":xp_per_stack", 8),
     (try_end),
     (val_min, ":xp_per_stack", 60),
     (assign, ":total_xp", 0),
     (party_get_num_companion_stacks, ":num_stacks", ":retinue_party"),
     (try_for_range, ":stack_no", 0, ":num_stacks"),
       (party_stack_get_troop_id, ":stack_troop", ":retinue_party", ":stack_no"),
       (is_between, ":stack_troop", soldiers_begin, soldiers_end),
       (neg|troop_is_hero, ":stack_troop"),
       (assign, ":stack_xp", ":xp_per_stack"),
       (try_begin),
         (call_script, "script_cf_sod_companion_retinue_troop_matches_preference", ":companion", ":stack_troop"),
         (val_add, ":stack_xp", 6),
       (try_end),
       (val_add, ":stack_xp", 3),
       (val_div, ":stack_xp", 4),
       (val_max, ":stack_xp", 1),
       (party_add_xp_to_stack, ":retinue_party", ":stack_no", ":stack_xp"),
       (val_add, ":total_xp", ":stack_xp"),
     (try_end),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_training_hour, ":cur_hours"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_training_xp, ":total_xp"),
     (assign, reg0, ":total_xp"),
   (try_end),
 ]),

("sod_companion_retinue_get_account_totals_to_regs",
 [
   (assign, ":total_size", 0),
   (assign, ":total_wages", 0),
   (assign, ":total_purses", 0),
   (assign, ":total_shortage", 0),
   (assign, ":total_supply_pressure", 0),
   (assign, ":total_morale", 0),
   (assign, ":active_count", 0),
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, ":size", reg0),
     (val_add, ":total_size", ":size"),
     (troop_get_slot, ":purse", ":companion", slot_troop_sod_retinue_treasury),
     (val_max, ":purse", 0),
     (val_add, ":total_purses", ":purse"),
     (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
     (val_add, ":total_wages", reg0),
     (troop_get_slot, ":shortage", ":companion", slot_troop_sod_retinue_last_shortage),
     (val_max, ":shortage", 0),
     (val_add, ":total_shortage", ":shortage"),
     (call_script, "script_sod_companion_retinue_update_supply_and_morale", ":companion"),
     (val_add, ":total_morale", reg0),
     (val_add, ":total_supply_pressure", reg1),
     (try_begin),
       (gt, ":size", 0),
       (val_add, ":active_count", 1),
     (try_end),
   (try_end),
   (try_begin),
     (gt, ":active_count", 0),
     (val_div, ":total_morale", ":active_count"),
   (try_end),
   (assign, reg53, ":total_size"),
   (assign, reg54, ":total_wages"),
   (assign, reg55, ":total_purses"),
   (assign, reg56, ":total_shortage"),
   (assign, reg57, ":total_supply_pressure"),
   (assign, reg58, ":total_morale"),
   (assign, reg59, ":active_count"),
 ]),

("sod_companion_retinue_note_post_battle_hire_result",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":result", 2),
   (store_script_param, ":troop", 3),
   (store_script_param, ":amount", 4),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_result, ":result"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_troop, ":troop"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_amount, ":amount"),
   (try_end),
 ]),

("sod_companion_retinue_describe_post_battle_hire_to_s22",
 [
   (store_script_param, ":companion", 1),
   (str_store_string, s22, "@No battlefield hiring report yet."),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_get_slot, ":result", ":companion", slot_troop_sod_retinue_last_battle_hire_result),
     (try_begin),
       (eq, ":result", sod_retinue_battle_hire_hired),
       (troop_get_slot, reg26, ":companion", slot_troop_sod_retinue_last_battle_hire_amount),
       (troop_get_slot, ":troop", ":companion", slot_troop_sod_retinue_last_battle_hire_troop),
       (str_store_troop_name_by_count, s23, ":troop", reg26),
       (str_store_string, s22, "@Last battlefield hiring: took {reg26} {s23} from freed stragglers."),
     (else_try),
       (eq, ":result", sod_retinue_battle_hire_opted_out),
       (str_store_string, s22, "@Last battlefield hiring: skipped by your orders."),
     (else_try),
       (eq, ":result", sod_retinue_battle_hire_no_trust),
       (str_store_string, s22, "@Last battlefield hiring: wanted recruits, but command trust or captain readiness was not steady enough."),
     (else_try),
       (eq, ":result", sod_retinue_battle_hire_no_capacity),
       (str_store_string, s22, "@Last battlefield hiring: wanted recruits, but the retinue had no room under its current order."),
     (else_try),
       (eq, ":result", sod_retinue_battle_hire_no_gold),
       (str_store_string, s22, "@Last battlefield hiring: wanted recruits, but the command purse lacked surplus gold."),
     (else_try),
       (eq, ":result", sod_retinue_battle_hire_no_leftovers),
       (str_store_string, s22, "@Last battlefield hiring: no suitable freed troops were left after your own choices."),
     (else_try),
       (eq, ":result", sod_retinue_battle_hire_no_order),
       (str_store_string, s22, "@Last battlefield hiring: skipped because the retinue has no active recruiting order."),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_describe_status_to_s20",
 [
   (store_script_param, ":companion", 1),
   (str_store_string, s20, "@This companion has no retinue under command."),
   (assign, reg20, 0),
   (assign, reg21, 0),
   (assign, reg22, 0),
   (assign, reg23, 0),
   (assign, reg24, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, reg20, reg0),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (assign, reg21, reg0),
     (troop_get_slot, reg22, ":companion", slot_troop_sod_retinue_treasury),
     (call_script, "script_sod_companion_retinue_get_target_size", ":companion"),
     (assign, reg23, reg0),
     (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
     (assign, reg24, reg0),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
     (assign, reg25, reg1),
     (call_script, "script_sod_companion_retinue_update_supply_and_morale", ":companion"),
     (assign, reg26, reg0),
     (assign, reg27, reg1),
     (troop_get_slot, reg28, ":companion", slot_troop_sod_retinue_last_shortage),
     (troop_get_slot, reg29, ":companion", slot_troop_sod_retinue_last_training_xp),
     (call_script, "script_sod_companion_retinue_describe_identity_to_s27", ":companion"),
     (troop_get_slot, ":post_battle_policy", ":companion", slot_troop_sod_retinue_post_battle_policy),
     (try_begin),
       (eq, ":post_battle_policy", sod_retinue_post_battle_disabled),
       (str_store_string, s22, "@off"),
     (else_try),
       (gt, "$g_sod_retinue_post_battle_hiring_disabled", 0),
       (str_store_string, s22, "@off by company order"),
     (else_try),
       (str_store_string, s22, "@on"),
     (try_end),
     (str_store_string_reg, s23, s22),
     (call_script, "script_sod_companion_retinue_describe_post_battle_hire_to_s22", ":companion"),
     (troop_get_slot, ":recruit_policy", ":companion", slot_troop_sod_retinue_recruit_policy),
     (try_begin),
       (eq, ":recruit_policy", sod_retinue_recruit_policy_eager),
       (str_store_string, s21, "@eager"),
     (else_try),
       (eq, ":recruit_policy", sod_retinue_recruit_policy_balanced),
       (str_store_string, s21, "@balanced"),
     (else_try),
       (eq, ":recruit_policy", sod_retinue_recruit_policy_cautious),
       (str_store_string, s21, "@cautious"),
     (else_try),
       (str_store_string, s21, "@none"),
     (try_end),
     (troop_get_slot, ":order", ":companion", slot_troop_sod_retinue_strength_order),
     (call_script, "script_sod_companion_retinue_update_warning_state", ":companion"),
     (troop_get_slot, ":warning", ":companion", slot_troop_sod_retinue_warning_state),
     (try_begin),
       (eq, ":warning", sod_retinue_warning_no_troops_returning),
       (str_store_string, s24, "@Retinue is standing down and returning troops over time."),
     (else_try),
       (eq, ":warning", sod_retinue_warning_over_capacity),
       (str_store_string, s24, "@Retinue is over capacity; reclaim troops or reduce the command."),
     (else_try),
       (eq, ":warning", sod_retinue_warning_above_target),
       (str_store_string, s24, "@Retinue is above its half-strength band and will not replace losses."),
     (else_try),
       (eq, ":warning", sod_retinue_warning_full_refused),
       (str_store_string, s24, "@Trust is too strained for a full-strength command."),
     (else_try),
       (str_store_string, s24, "@Orders are steady."),
     (try_end),
     (try_begin),
       (gt, reg24, 0),
       (lt, reg22, reg24),
       (neq, ":order", sod_retinue_strength_none),
       (str_store_string, s24, "@Command purse cannot cover this week's wages; no further recruiting until funds are restored."),
     (try_end),
     (try_begin),
       (le, reg26, 20),
       (str_store_string, s25, "@fracturing"),
     (else_try),
       (le, reg26, 40),
       (str_store_string, s25, "@strained"),
     (else_try),
       (eq, ":warning", sod_retinue_warning_over_capacity),
       (str_store_string, s25, "@over capacity"),
     (else_try),
       (str_store_string, s25, "@steady"),
     (try_end),
     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (troop_get_slot, ":companion_warning_state", ":companion", slot_troop_companion_warning_state),
     (troop_get_slot, ":quest_stage", ":companion", slot_troop_companion_personal_quest_stage),
     (try_begin),
       (this_or_next|lt, ":approval", 20),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_final),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_broken),
       (eq, ":quest_stage", sod_companion_quest_failed),
       (str_store_string, s26, "@serious"),
     (else_try),
       (this_or_next|lt, ":approval", 45),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_pending),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_acknowledged),
       (eq, ":quest_stage", sod_companion_quest_resolved_hard),
       (str_store_string, s26, "@watchful"),
     (else_try),
       (str_store_string, s26, "@low"),
     (try_end),
     (try_begin),
       (eq, ":order", sod_retinue_strength_full),
       (str_store_string, s20, "@Retinue: {reg20}/{reg21} under command; target {reg23}.^Captain's focus: {s27}. Order: full strength. Recruitment: {s21}. Post-battle hiring: {s23}.^Command purse: {reg22} denars. Wages due this week: {reg24} denars. Wage reserve: {reg25} week(s). Last shortage: {reg28}.^Condition: cohesion {reg26}; health {s25}; loyalty risk {s26}; supply pressure {reg27}; last drill {reg29} xp.^Captain's note: {s24} {s22}"),
     (else_try),
       (eq, ":order", sod_retinue_strength_half),
       (str_store_string, s20, "@Retinue: {reg20}/{reg21} under command; target {reg23}.^Captain's focus: {s27}. Order: half strength. Recruitment: {s21}. Post-battle hiring: {s23}.^Command purse: {reg22} denars. Wages due this week: {reg24} denars. Wage reserve: {reg25} week(s). Last shortage: {reg28}.^Condition: cohesion {reg26}; health {s25}; loyalty risk {s26}; supply pressure {reg27}; last drill {reg29} xp.^Captain's note: {s24} {s22}"),
     (else_try),
       (str_store_string, s20, "@Retinue: {reg20}/{reg21} under command; target {reg23}.^Captain's focus: {s27}. Order: no troops. Recruitment: {s21}. Post-battle hiring: {s23}.^Command purse: {reg22} denars. Wages due this week: {reg24} denars. Wage reserve: {reg25} week(s). Last shortage: {reg28}.^Condition: cohesion {reg26}; health {s25}; loyalty risk {s26}; supply pressure {reg27}; last drill {reg29} xp.^Captain's note: {s24} {s22}"),
     (try_end),
   (try_end),
 ]),

("sod_get_player_effective_party_size",
 [
   (party_get_num_companions, ":personal_size", "p_main_party"),
   (assign, ":retinue_size", 0),
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (val_add, ":retinue_size", reg0),
   (try_end),
   (store_add, ":combined_size", ":personal_size", ":retinue_size"),
   (assign, reg0, ":personal_size"),
   (assign, reg1, ":retinue_size"),
   (assign, reg2, ":combined_size"),
 ]),

("sod_get_player_effective_party_capacity",
 [
   (party_get_num_companions, ":personal_size", "p_main_party"),
   (party_get_free_companions_capacity, ":personal_free", "p_main_party"),
   (store_add, ":personal_capacity", ":personal_size", ":personal_free"),
   (assign, ":retinue_capacity", 0),
   (assign, ":retinue_free", 0),
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (val_add, ":retinue_capacity", reg0),
     (call_script, "script_sod_companion_retinue_get_free_capacity", ":companion"),
     (val_add, ":retinue_free", reg0),
   (try_end),
   (store_add, ":combined_capacity", ":personal_capacity", ":retinue_capacity"),
   (assign, reg0, ":personal_capacity"),
   (assign, reg1, ":retinue_capacity"),
   (assign, reg2, ":combined_capacity"),
   (assign, reg3, ":personal_free"),
   (assign, reg4, ":retinue_free"),
 ]),

("sod_player_party_size_describe_to_s1",
 [
   (call_script, "script_sod_get_player_effective_party_size"),
   (assign, reg20, reg0),
   (assign, reg21, reg1),
   (assign, reg22, reg2),
   (call_script, "script_sod_get_player_effective_party_capacity"),
   (assign, reg23, reg0),
   (assign, reg24, reg1),
   (assign, reg25, reg2),
   (assign, reg26, reg3),
   (assign, reg27, reg4),
   (try_begin),
     (gt, reg26, 0),
     (str_store_string, s1, "@Personal command: {reg20}/{reg23}, free {reg26}. Companion retinues: {reg21}/{reg24}, free {reg27}. Combined company bodies: {reg22}/{reg25}. Retinue capacity is captain-commanded; it does not increase your personal party limit."),
   (else_try),
     (gt, reg27, 0),
     (str_store_string, s1, "@Personal command: {reg20}/{reg23}, no personal room. Companion retinues: {reg21}/{reg24}, free {reg27}. You can move existing soldiers to a trusted companion's retinue, but new hires still enter your personal party only when there is native capacity."),
   (else_try),
     (str_store_string, s1, "@Personal command: {reg20}/{reg23}, no personal room. Companion retinues: {reg21}/{reg24}, no retinue room. Retinue capacity is tracked separately and never raises your personal party limit."),
   (try_end),
 ]),

("sod_companion_retinue_describe_report_to_s1",
 [
   (call_script, "script_sod_player_party_size_describe_to_s1"),
   (str_store_string_reg, s97, s1),
   (str_store_string, s98, "@Companion retinues^^These are internal commands held by companions, like small garrisons inside the company. Retinue troops are commanded by their captain rather than by your personal party limit, but they still need wages, food, discipline, and trust.^^{s97}^^"),
   (str_store_string_reg, s97, s98),
   (str_store_string, s98, "@{s97}Command distinction: your personal party is the roster you command directly; companion retinues are captain-held internal troop groups; external follower parties are separate map companies ordered through dialogue; garrisons remain tied to towns and castles.^"),
   (try_begin),
     (gt, "$g_sod_retinue_post_battle_hiring_disabled", 0),
     (str_store_string_reg, s97, s98),
     (str_store_string, s98, "@{s97}Post-battle retinue hiring: disabled for the whole company.^"),
   (else_try),
     (str_store_string_reg, s97, s98),
     (str_store_string, s98, "@{s97}Post-battle retinue hiring: enabled for companions who allow it.^"),
   (try_end),
   (assign, ":listed", 0),
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, reg20, reg0),
     (call_script, "script_sod_companion_retinue_get_capacity", ":companion"),
     (assign, reg21, reg0),
     (call_script, "script_sod_companion_retinue_get_target_size", ":companion"),
     (assign, reg22, reg0),
     (troop_get_slot, reg23, ":companion", slot_troop_sod_retinue_treasury),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
     (assign, reg24, reg1),
     (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
     (assign, reg25, reg0),
     (troop_get_slot, reg26, ":companion", slot_troop_sod_retinue_last_shortage),
     (call_script, "script_sod_companion_retinue_update_supply_and_morale", ":companion"),
     (assign, reg27, reg0),
     (assign, reg28, reg1),
     (troop_get_slot, reg29, ":companion", slot_troop_sod_retinue_last_training_xp),
     (call_script, "script_sod_companion_retinue_update_warning_state", ":companion"),
     (troop_get_slot, ":warning", ":companion", slot_troop_sod_retinue_warning_state),
     (try_begin),
       (eq, ":warning", sod_retinue_warning_no_troops_returning),
       (str_store_string, s5, "@standing down"),
     (else_try),
       (eq, ":warning", sod_retinue_warning_over_capacity),
       (str_store_string, s5, "@over capacity"),
     (else_try),
       (eq, ":warning", sod_retinue_warning_above_target),
       (str_store_string, s5, "@above target"),
     (else_try),
       (eq, ":warning", sod_retinue_warning_full_refused),
       (str_store_string, s5, "@full command refused"),
     (else_try),
       (str_store_string, s5, "@steady"),
     (try_end),
     (try_begin),
       (le, reg27, 20),
       (str_store_string, s6, "@fracturing"),
     (else_try),
       (le, reg27, 40),
       (str_store_string, s6, "@strained"),
     (else_try),
       (eq, ":warning", sod_retinue_warning_over_capacity),
       (str_store_string, s6, "@over capacity"),
     (else_try),
       (str_store_string, s6, "@steady"),
     (try_end),
     (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
     (troop_get_slot, ":companion_warning_state", ":companion", slot_troop_companion_warning_state),
     (troop_get_slot, ":quest_stage", ":companion", slot_troop_companion_personal_quest_stage),
     (try_begin),
       (this_or_next|lt, ":approval", 20),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_final),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_broken),
       (eq, ":quest_stage", sod_companion_quest_failed),
       (str_store_string, s7, "@serious"),
     (else_try),
       (this_or_next|lt, ":approval", 45),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_pending),
       (this_or_next|eq, ":companion_warning_state", sod_companion_warning_acknowledged),
       (eq, ":quest_stage", sod_companion_quest_resolved_hard),
       (str_store_string, s7, "@watchful"),
     (else_try),
       (str_store_string, s7, "@low"),
     (try_end),
     (troop_get_slot, ":order", ":companion", slot_troop_sod_retinue_strength_order),
     (str_store_troop_name, s2, ":companion"),
     (call_script, "script_sod_companion_retinue_describe_identity_to_s27", ":companion"),
     (troop_get_slot, ":recruit_policy", ":companion", slot_troop_sod_retinue_recruit_policy),
     (try_begin),
       (eq, ":recruit_policy", sod_retinue_recruit_policy_eager),
       (str_store_string, s4, "@eager"),
     (else_try),
       (eq, ":recruit_policy", sod_retinue_recruit_policy_balanced),
       (str_store_string, s4, "@balanced"),
     (else_try),
       (eq, ":recruit_policy", sod_retinue_recruit_policy_cautious),
       (str_store_string, s4, "@cautious"),
     (else_try),
       (str_store_string, s4, "@none"),
     (try_end),
     (try_begin),
       (eq, ":order", sod_retinue_strength_full),
       (str_store_string, s3, "@full strength"),
     (else_try),
       (eq, ":order", sod_retinue_strength_half),
       (str_store_string, s3, "@half strength"),
     (else_try),
       (str_store_string, s3, "@no troops"),
     (try_end),
     (str_store_string_reg, s97, s98),
     (str_store_string, s98, "@{s97}^{s2}: {reg20}/{reg21}, target {reg22}, order {s3}, status {s5}. Focus: {s27}. Recruitment {s4}. Purse {reg23} denars; wages due this week {reg25}; reserve {reg24} week(s); shortage {reg26}. Cohesion {reg27}; health {s6}; loyalty risk {s7}; supply pressure {reg28}; last drill {reg29} xp."),
     (assign, ":listed", 1),
   (try_end),
   (try_begin),
     (eq, ":listed", 0),
     (str_store_string_reg, s97, s98),
     (str_store_string, s98, "@{s97}No companions in the company can currently hold a retinue."),
   (try_end),
   (str_store_string_reg, s1, s98),
 ]),

("sod_companion_retinue_describe_focus_to_s1",
 [
   (store_script_param, ":companion", 1),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (call_script, "script_sod_companion_retinue_describe_status_to_s20", ":companion"),
     (str_store_troop_name, s2, ":companion"),
     (call_script, "script_sod_companion_retinue_describe_voice_to_s29", ":companion"),
     (str_store_string, s1, "@{s2}'s command^^{s29}^^{s20}^^Orders here set the size {s2} should maintain. Command-purse gold is used for that retinue's wages and future autonomous hiring."),
   (else_try),
     (str_store_string, s1, "@No valid companion retinue is selected."),
   (try_end),
 ]),

("sod_companion_retinue_process_strength_order",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (assign, ":retinue_party", reg0),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (troop_get_slot, ":order", ":companion", slot_troop_sod_retinue_strength_order),
     (try_begin),
       (eq, ":order", sod_retinue_strength_none),
       (call_script, "script_sod_companion_retinue_get_size", ":companion"),
       (assign, ":size", reg0),
       (gt, ":size", 0),
       (call_script, "script_sod_companion_retinue_select_retinue_troop", ":companion", 0, 0),
       (assign, ":troop", reg0),
       (assign, ":available", reg1),
       (is_between, ":troop", soldiers_begin, soldiers_end),
       (gt, ":available", 0),
       (party_get_free_companions_capacity, ":player_free", "p_main_party"),
       (assign, ":amount", ":available"),
       (val_min, ":amount", 3),
       (try_begin),
         (gt, ":player_free", 0),
         (val_min, ":amount", ":player_free"),
         (party_remove_members, ":retinue_party", ":troop", ":amount"),
         (party_add_members, "p_main_party", ":troop", ":amount"),
         (str_store_troop_name, s20, ":companion"),
         (str_store_troop_name_by_count, s21, ":troop", ":amount"),
         (assign, reg20, ":amount"),
         (display_message, "@{s20} returns {reg20} {s21} from their retinue to your command.", 0x99CCFF),
         (assign, reg0, ":amount"),
       (else_try),
         (assign, ":amount", 1),
         (party_remove_members, ":retinue_party", ":troop", ":amount"),
         (str_store_troop_name, s20, ":companion"),
         (str_store_troop_name_by_count, s21, ":troop", ":amount"),
         (display_message, "@{s20} discharges {s21} because your party has no room to receive them.", 0xFFCC66),
         (assign, reg0, ":amount"),
       (try_end),
     (else_try),
       (call_script, "script_sod_companion_retinue_update_warning_state", ":companion"),
     (try_end),
     (call_script, "script_sod_companion_retinue_update_warning_state", ":companion"),
   (try_end),
 ]),

("sod_companion_retinue_try_autorecruit",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (call_script, "script_cf_sod_companion_retinue_can_self_manage", ":companion", 0),
     (troop_get_slot, ":order", ":companion", slot_troop_sod_retinue_strength_order),
     (neq, ":order", sod_retinue_strength_none),
     (call_script, "script_cf_sod_companion_retinue_accepts_strength_order", ":companion", ":order"),
     (neg|troop_slot_eq, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
     (store_current_hours, ":cur_hours"),
     (troop_get_slot, ":last_recruit_hour", ":companion", slot_troop_sod_retinue_last_recruit_hour),
     (store_sub, ":hours_since_recruit", ":cur_hours", ":last_recruit_hour"),
     (this_or_next|le, ":last_recruit_hour", 0),
     (ge, ":hours_since_recruit", 24),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, ":size", reg0),
     (call_script, "script_sod_companion_retinue_get_target_size", ":companion"),
     (assign, ":target_size", reg0),
     (assign, ":recruit_target", ":target_size"),
     (try_begin),
       (eq, ":order", sod_retinue_strength_half),
      (val_sub, ":recruit_target", sod_retinue_half_strength_tolerance),
       (val_max, ":recruit_target", 0),
     (try_end),
     (gt, ":recruit_target", ":size"),
     (call_script, "script_sod_companion_retinue_get_free_capacity", ":companion"),
     (assign, ":free_capacity", reg0),
     (gt, ":free_capacity", 0),
     (call_script, "script_sod_companion_retinue_calculate_recruit_budget", ":companion"),
     (assign, ":budget", reg0),
     (gt, ":budget", 0),
     (call_script, "script_sod_companion_retinue_select_local_recruit_troop", ":companion"),
     (assign, ":recruit_troop", reg0),
     (assign, ":recruit_center", reg1),
     (assign, ":local_available", reg2),
     (is_between, ":recruit_troop", soldiers_begin, soldiers_end),
     (gt, ":local_available", 0),
     (call_script, "script_sod_companion_retinue_get_recruit_cost", ":recruit_troop"),
     (assign, ":cost_per_recruit", reg0),
     (gt, ":cost_per_recruit", 0),
     (store_div, ":affordable", ":budget", ":cost_per_recruit"),
     (gt, ":affordable", 0),
     (store_sub, ":needed", ":recruit_target", ":size"),
     (assign, ":take", ":affordable"),
     (val_min, ":take", ":needed"),
     (val_min, ":take", ":free_capacity"),
     (val_min, ":take", ":local_available"),
     (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_recruit_policy),
     (try_begin),
       (eq, ":policy", sod_retinue_recruit_policy_cautious),
       (val_min, ":take", 1),
     (else_try),
       (eq, ":policy", sod_retinue_recruit_policy_balanced),
       (val_min, ":take", 2),
     (else_try),
       (eq, ":policy", sod_retinue_recruit_policy_eager),
       (val_min, ":take", 3),
     (try_end),
     (gt, ":take", 0),
     (store_mul, ":cost", ":take", ":cost_per_recruit"),
     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (ge, ":treasury", ":cost"),
     (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (assign, ":retinue_party", reg0),
     (party_is_active, ":retinue_party"),
     (val_sub, ":treasury", ":cost"),
     (val_max, ":treasury", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
     (party_add_members, ":retinue_party", ":recruit_troop", ":take"),
     (try_begin),
       (is_between, ":recruit_center", villages_begin, villages_end),
       (party_slot_ge, ":recruit_center", slot_center_volunteer_troop_amount, 1),
       (party_get_slot, ":remaining_volunteers", ":recruit_center", slot_center_volunteer_troop_amount),
       (val_sub, ":remaining_volunteers", ":take"),
       (val_max, ":remaining_volunteers", 0),
       (party_set_slot, ":recruit_center", slot_center_volunteer_troop_amount, ":remaining_volunteers"),
       (call_script, "script_spend_center_population_for_recruitment", ":recruit_center", ":take"),
     (try_end),
     (call_script, "script_sod_company_accounts_record_company_growth", sod_company_growth_recruit, ":take", ":cost"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_recruit_hour, ":cur_hours"),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
     (assign, reg0, ":take"),
   (try_end),
 ]),

("sod_companion_retinue_try_autoupgrade",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (call_script, "script_cf_sod_companion_retinue_can_self_manage", ":companion", 0),
     (neg|troop_slot_eq, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
     (store_current_hours, ":cur_hours"),
     (troop_get_slot, ":last_upgrade_hour", ":companion", slot_troop_sod_retinue_last_upgrade_hour),
     (store_sub, ":hours_since_upgrade", ":cur_hours", ":last_upgrade_hour"),
     (this_or_next|le, ":last_upgrade_hour", 0),
     (ge, ":hours_since_upgrade", 24),
     (call_script, "script_sod_companion_retinue_calculate_upgrade_budget", ":companion"),
     (assign, ":budget", reg0),
     (gt, ":budget", 0),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (assign, ":upgraded_count", 0),
     (party_get_num_companion_stacks, ":num_stacks", ":retinue_party"),
     (try_for_range, ":stack_no", 0, ":num_stacks"),
       (eq, ":upgraded_count", 0),
       (party_stack_get_troop_id, ":stack_troop", ":retinue_party", ":stack_no"),
       (is_between, ":stack_troop", soldiers_begin, soldiers_end),
       (neg|troop_is_hero, ":stack_troop"),
       (party_stack_get_size, ":stack_size", ":retinue_party", ":stack_no"),
       (gt, ":stack_size", 0),
       (call_script, "script_sod_companion_retinue_select_upgrade_troop", ":companion", ":stack_troop"),
       (assign, ":upgrade_troop", reg0),
       (is_between, ":upgrade_troop", soldiers_begin, soldiers_end),
       (call_script, "script_sod_companion_retinue_get_upgrade_cost", ":stack_troop", ":upgrade_troop"),
       (assign, ":cost_per_upgrade", reg0),
       (gt, ":cost_per_upgrade", 0),
       (store_div, ":affordable", ":budget", ":cost_per_upgrade"),
       (gt, ":affordable", 0),
       (assign, ":take", ":stack_size"),
       (val_min, ":take", ":affordable"),
       (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_recruit_policy),
       (try_begin),
         (eq, ":policy", sod_retinue_recruit_policy_cautious),
         (val_min, ":take", 1),
       (else_try),
         (eq, ":policy", sod_retinue_recruit_policy_balanced),
         (val_min, ":take", 2),
       (else_try),
         (eq, ":policy", sod_retinue_recruit_policy_eager),
         (val_min, ":take", 3),
       (try_end),
       (gt, ":take", 0),
       (store_mul, ":cost", ":take", ":cost_per_upgrade"),
       (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
       (ge, ":treasury", ":cost"),
       (val_sub, ":treasury", ":cost"),
       (val_max, ":treasury", 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
       (party_remove_members, ":retinue_party", ":stack_troop", ":take"),
       (party_add_members, ":retinue_party", ":upgrade_troop", ":take"),
       (call_script, "script_sod_company_accounts_record_company_growth", sod_company_growth_upgrade, ":take", ":cost"),
       (assign, ":upgraded_count", ":take"),
     (try_end),
     (gt, ":upgraded_count", 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_upgrade_hour, ":cur_hours"),
     (call_script, "script_sod_companion_retinue_update_wage_reserve", ":companion"),
     (assign, reg0, ":upgraded_count"),
   (try_end),
 ]),

("sod_companion_retinue_try_post_battle_hire",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_none, 0, 0),
     (try_begin),
       (this_or_next|gt, "$g_sod_retinue_post_battle_hiring_disabled", 0),
       (troop_slot_eq, ":companion", slot_troop_sod_retinue_post_battle_policy, sod_retinue_post_battle_disabled),
       (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_opted_out, 0, 0),
     (else_try),
       (this_or_next|troop_slot_eq, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
       (troop_slot_eq, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
       (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_no_order, 0, 0),
     (else_try),
       (assign, ":can_command", 0),
       (try_begin),
         (call_script, "script_cf_sod_companion_retinue_can_self_manage", ":companion", 1),
         (assign, ":can_command", 1),
       (try_end),
       (try_begin),
         (eq, ":can_command", 0),
         (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_no_trust, 0, 0),
      (else_try),
        (call_script, "script_sod_companion_retinue_get_free_capacity", ":companion"),
        (assign, ":free_capacity", reg0),
        (troop_get_slot, ":order", ":companion", slot_troop_sod_retinue_strength_order),
        (assign, ":order_ok", 0),
        (try_begin),
          (call_script, "script_cf_sod_companion_retinue_accepts_strength_order", ":companion", ":order"),
          (assign, ":order_ok", 1),
        (try_end),
        (call_script, "script_sod_companion_retinue_get_target_size", ":companion"),
        (assign, ":target_size", reg0),
        (assign, ":recruit_target", ":target_size"),
        (try_begin),
          (eq, ":order", sod_retinue_strength_half),
          (val_sub, ":recruit_target", sod_retinue_half_strength_tolerance),
          (val_max, ":recruit_target", 0),
        (try_end),
        (call_script, "script_sod_companion_retinue_get_size", ":companion"),
        (assign, ":current_size", reg0),
        (try_begin),
          (eq, ":order_ok", 0),
          (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_no_trust, 0, 0),
        (else_try),
          (this_or_next|le, ":free_capacity", 0),
          (ge, ":current_size", ":recruit_target"),
          (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_no_capacity, 0, 0),
         (else_try),
           (call_script, "script_sod_companion_retinue_calculate_recruit_budget", ":companion"),
           (assign, ":budget", reg0),
           (try_begin),
             (le, ":budget", 0),
             (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_no_gold, 0, 0),
           (else_try),
             (assign, ":selected_troop", 0),
             (assign, ":selected_count", 0),
             (assign, ":selected_cost", 0),
             (call_script, "script_sod_companion_retinue_select_recruit_troop", ":companion"),
             (assign, ":preferred_troop", reg0),
             (party_get_num_companion_stacks, ":num_stacks", "p_temp_party"),
             (try_for_range, ":stack_no", 0, ":num_stacks"),
               (eq, ":selected_troop", 0),
               (party_stack_get_troop_id, ":stack_troop", "p_temp_party", ":stack_no"),
               (eq, ":stack_troop", ":preferred_troop"),
               (is_between, ":stack_troop", soldiers_begin, soldiers_end),
               (neg|troop_is_hero, ":stack_troop"),
               (call_script, "script_sod_troop_get_elite_tier", ":stack_troop"),
               (lt, reg0, sod_elite_tier_noble),
               (party_stack_get_size, ":stack_size", "p_temp_party", ":stack_no"),
               (gt, ":stack_size", 0),
               (call_script, "script_sod_companion_retinue_get_recruit_cost", ":stack_troop"),
               (assign, ":cost_per_recruit", reg0),
               (gt, ":cost_per_recruit", 0),
               (store_div, ":affordable", ":budget", ":cost_per_recruit"),
               (gt, ":affordable", 0),
              (assign, ":take", ":stack_size"),
              (val_min, ":take", ":affordable"),
              (val_min, ":take", ":free_capacity"),
              (store_sub, ":needed", ":recruit_target", ":current_size"),
              (val_min, ":take", ":needed"),
              (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_recruit_policy),
               (try_begin),
                 (eq, ":policy", sod_retinue_recruit_policy_cautious),
                 (val_min, ":take", 1),
               (else_try),
                 (eq, ":policy", sod_retinue_recruit_policy_balanced),
                 (val_min, ":take", 2),
               (else_try),
                 (eq, ":policy", sod_retinue_recruit_policy_eager),
                 (val_min, ":take", 3),
               (try_end),
               (gt, ":take", 0),
               (store_mul, ":cost", ":take", ":cost_per_recruit"),
               (assign, ":selected_troop", ":stack_troop"),
               (assign, ":selected_count", ":take"),
               (assign, ":selected_cost", ":cost"),
             (try_end),
             (try_for_range, ":stack_no", 0, ":num_stacks"),
               (eq, ":selected_troop", 0),
               (party_stack_get_troop_id, ":stack_troop", "p_temp_party", ":stack_no"),
               (is_between, ":stack_troop", soldiers_begin, soldiers_end),
               (neg|troop_is_hero, ":stack_troop"),
               (call_script, "script_sod_troop_get_elite_tier", ":stack_troop"),
               (lt, reg0, sod_elite_tier_noble),
               (party_stack_get_size, ":stack_size", "p_temp_party", ":stack_no"),
               (gt, ":stack_size", 0),
               (call_script, "script_sod_companion_retinue_get_recruit_cost", ":stack_troop"),
               (assign, ":cost_per_recruit", reg0),
               (gt, ":cost_per_recruit", 0),
               (store_div, ":affordable", ":budget", ":cost_per_recruit"),
               (gt, ":affordable", 0),
              (assign, ":take", ":stack_size"),
              (val_min, ":take", ":affordable"),
              (val_min, ":take", ":free_capacity"),
              (store_sub, ":needed", ":recruit_target", ":current_size"),
              (val_min, ":take", ":needed"),
              (troop_get_slot, ":policy", ":companion", slot_troop_sod_retinue_recruit_policy),
               (try_begin),
                 (eq, ":policy", sod_retinue_recruit_policy_cautious),
                 (val_min, ":take", 1),
               (else_try),
                 (eq, ":policy", sod_retinue_recruit_policy_balanced),
                 (val_min, ":take", 2),
               (else_try),
                 (eq, ":policy", sod_retinue_recruit_policy_eager),
                 (val_min, ":take", 3),
               (try_end),
               (gt, ":take", 0),
               (store_mul, ":cost", ":take", ":cost_per_recruit"),
               (assign, ":selected_troop", ":stack_troop"),
               (assign, ":selected_count", ":take"),
               (assign, ":selected_cost", ":cost"),
             (try_end),
             (try_begin),
               (neg|is_between, ":selected_troop", soldiers_begin, soldiers_end),
               (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_no_leftovers, 0, 0),
             (else_try),
               (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
               (lt, ":treasury", ":selected_cost"),
               (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_no_gold, ":selected_troop", 0),
             (else_try),
               (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
               (assign, ":retinue_party", reg0),
               (party_is_active, ":retinue_party"),
               (party_remove_members, "p_temp_party", ":selected_troop", ":selected_count"),
               (party_add_members, ":retinue_party", ":selected_troop", ":selected_count"),
               (val_sub, ":treasury", ":selected_cost"),
               (val_max, ":treasury", 0),
               (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, ":treasury"),
               (call_script, "script_sod_companion_retinue_note_post_battle_hire_result", ":companion", sod_retinue_battle_hire_hired, ":selected_troop", ":selected_count"),
               (call_script, "script_sod_company_accounts_record_company_growth", sod_company_growth_recruit, ":selected_count", ":selected_cost"),
               (str_store_troop_name, s20, ":companion"),
               (str_store_troop_name_by_count, s21, ":selected_troop", ":selected_count"),
               (assign, reg20, ":selected_count"),
              (display_message, "@{s20} has taken {reg20} {s21} from the freed stragglers under their command.", 0x99CCFF),
               (assign, reg0, ":selected_count"),
             (try_end),
           (try_end),
         (try_end),
       (try_end),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_process_post_battle_hires",
 [
   (assign, ":total_hired", 0),
   (try_begin),
     (eq, "$g_battle_result", 1),
     (party_get_num_companions, ":rescued_left", "p_temp_party"),
     (gt, ":rescued_left", 0),
     (try_for_range, ":companion", companions_begin, companions_end),
       (gt, ":rescued_left", 0),
       (call_script, "script_sod_companion_retinue_try_post_battle_hire", ":companion"),
       (try_begin),
         (gt, reg0, 0),
         (val_add, ":total_hired", reg0),
         (party_get_num_companions, ":rescued_left", "p_temp_party"),
       (try_end),
     (try_end),
   (try_end),
   (assign, reg0, ":total_hired"),
 ]),

("cf_sod_companion_retinue_can_merge_fallback",
 [
   (store_script_param, ":companion", 1),
   (is_between, ":companion", companions_begin, companions_end),
   (main_party_has_troop, ":companion"),
   (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
   (party_is_active, ":retinue_party"),
   (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
   (party_slot_eq, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
   (assign, ":safe", 1),
   (party_get_num_companion_stacks, ":num_stacks", ":retinue_party"),
   (try_for_range, ":stack_no", 0, ":num_stacks"),
     (party_stack_get_troop_id, ":stack_troop", ":retinue_party", ":stack_no"),
     (is_between, ":stack_troop", soldiers_begin, soldiers_end),
     (neg|troop_is_hero, ":stack_troop"),
     (party_count_members_of_type, ":player_has_type", "p_main_party", ":stack_troop"),
     (try_begin),
       (gt, ":player_has_type", 0),
       (assign, ":safe", 0),
     (try_end),
   (try_end),
   (eq, ":safe", 1),
 ]),

("sod_companion_retinue_clear_battle_store",
 [
   (store_script_param, ":companion", 1),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_get_slot, ":store_party", ":companion", slot_troop_sod_retinue_battle_store_party),
     (try_begin),
       (party_is_active, ":store_party"),
       (neq, ":store_party", "p_main_party"),
       (party_slot_eq, ":store_party", slot_party_type, spt_companion_retinue),
       (party_slot_eq, ":store_party", slot_party_sod_retinue_owner_troop, ":companion"),
       (remove_party, ":store_party"),
     (try_end),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_battle_store_party, 0),
   (try_end),
 ]),

("sod_companion_retinue_snapshot_for_battle",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":retinue_party", 2),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (party_is_active, ":retinue_party"),
     (party_get_num_companions, ":size", ":retinue_party"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_size, ":size"),
     (assign, reg0, ":size"),
   (try_end),
 ]),

("sod_companion_retinue_validate_battle_outcome",
 [
   (store_script_param, ":companion", 1),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (troop_get_slot, ":pre_size", ":companion", slot_troop_sod_retinue_last_size),
     (party_get_num_companions, ":post_size", ":retinue_party"),
     (try_begin),
       (gt, ":post_size", ":pre_size"),
       (store_sub, ":excess", ":post_size", ":pre_size"),
       (try_for_range, ":unused", 0, ":excess"),
         (gt, ":excess", 0),
         (call_script, "script_sod_companion_retinue_select_retinue_troop", ":companion", 0, 0),
         (assign, ":troop", reg0),
         (assign, ":available", reg1),
         (is_between, ":troop", soldiers_begin, soldiers_end),
         (assign, ":remove", ":available"),
         (val_min, ":remove", ":excess"),
         (gt, ":remove", 0),
         (party_remove_members, ":retinue_party", ":troop", ":remove"),
         (val_sub, ":excess", ":remove"),
       (try_end),
       (str_store_troop_name, s20, ":companion"),
       (display_message, "@Retinue battle repair: removed duplicate post-battle troops from {s20}'s command.", 0xFFCC66),
     (else_try),
       (lt, ":post_size", ":pre_size"),
       (store_sub, reg20, ":pre_size", ":post_size"),
       (str_store_troop_name, s20, ":companion"),
       (display_message, "@{s20}'s retinue lost {reg20} troop(s) killed, wounded, scattered, or captured in the battle.", 0x99CCFF),
     (try_end),
     (party_get_num_companions, ":final_size", ":retinue_party"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_size, ":final_size"),
     (assign, reg0, ":final_size"),
   (try_end),
 ]),

("sod_companion_retinue_join_current_battle_by_merge_fallback",
 [
   (assign, ":joined_count", 0),
   (assign, ":skipped_count", 0),
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (neq, ":retinue_party", "p_main_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (party_slot_eq, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
     (call_script, "script_sod_companion_retinue_get_size", ":companion"),
     (assign, ":retinue_size", reg0),
     (gt, ":retinue_size", 0),
     (try_begin),
       (call_script, "script_cf_sod_companion_retinue_can_merge_fallback", ":companion"),
       (call_script, "script_sod_companion_retinue_clear_battle_store", ":companion"),
       (set_spawn_radius, 0),
       (spawn_around_party, "p_main_party", "pt_sod_companion_retinue"),
       (assign, ":store_party", reg0),
       (gt, ":store_party", 0),
       (party_is_active, ":store_party"),
       (party_clear, ":store_party"),
       (party_set_slot, ":store_party", slot_party_type, spt_companion_retinue),
       (party_set_slot, ":store_party", slot_party_sod_retinue_owner_troop, ":companion"),
       (disable_party, ":store_party"),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_battle_store_party, ":store_party"),
       (call_script, "script_sod_companion_retinue_snapshot_for_battle", ":companion", ":retinue_party"),
       (party_get_num_companion_stacks, ":num_stacks", ":retinue_party"),
       (try_for_range, ":unused", 0, ":num_stacks"),
         (party_stack_get_troop_id, ":stack_troop", ":retinue_party", 0),
         (is_between, ":stack_troop", soldiers_begin, soldiers_end),
         (neg|troop_is_hero, ":stack_troop"),
         (party_stack_get_size, ":stack_size", ":retinue_party", 0),
         (gt, ":stack_size", 0),
         (party_add_members, ":store_party", ":stack_troop", ":stack_size"),
         (party_add_members, "p_main_party", ":stack_troop", ":stack_size"),
         (party_remove_members, ":retinue_party", ":stack_troop", ":stack_size"),
       (try_end),
       (val_add, ":joined_count", 1),
     (else_try),
       (val_add, ":skipped_count", 1),
     (try_end),
   (try_end),
   (assign, "$g_sod_retinue_battle_bridge_fallback_skipped", ":skipped_count"),
   (try_begin),
     (gt, ":joined_count", 0),
     (assign, "$g_sod_retinue_battle_bridge_active", 1),
     (assign, "$g_sod_retinue_battle_bridge_mode", 2),
     (assign, "$g_sod_retinue_battle_bridge_count", ":joined_count"),
   (try_end),
   (assign, reg0, ":joined_count"),
 ]),

("sod_companion_retinue_join_current_battle",
 [
   (assign, ":joined_count", 0),
   (assign, "$g_sod_retinue_battle_bridge_mode", 0),
   (assign, "$g_sod_retinue_battle_bridge_fallback_skipped", 0),
   (try_begin),
     (gt, "$g_sod_retinue_battle_bridge_force_merge", 0),
     (call_script, "script_sod_companion_retinue_join_current_battle_by_merge_fallback"),
     (assign, ":joined_count", reg0),
   (else_try),
     (try_for_range, ":companion", companions_begin, companions_end),
       (main_party_has_troop, ":companion"),
       (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
       (party_is_active, ":retinue_party"),
       (neq, ":retinue_party", "p_main_party"),
       (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
       (party_slot_eq, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
       (call_script, "script_sod_companion_retinue_get_size", ":companion"),
       (gt, reg0, 0),
       (call_script, "script_sod_companion_retinue_clear_battle_store", ":companion"),
       (call_script, "script_sod_companion_retinue_snapshot_for_battle", ":companion", ":retinue_party"),
       (party_quick_attach_to_current_battle, ":retinue_party", 0),
       (val_add, ":joined_count", 1),
     (try_end),
     (try_begin),
       (gt, ":joined_count", 0),
       (assign, "$g_sod_retinue_battle_bridge_mode", 1),
     (try_end),
   (try_end),
   (try_begin),
     (gt, ":joined_count", 0),
     (assign, "$g_sod_retinue_battle_bridge_active", 1),
     (assign, "$g_sod_retinue_battle_bridge_count", ":joined_count"),
   (else_try),
     (assign, "$g_sod_retinue_battle_bridge_count", 0),
   (try_end),
   (assign, reg0, ":joined_count"),
 ]),

("sod_companion_retinue_finish_merge_fallback",
 [
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (troop_get_slot, ":store_party", ":companion", slot_troop_sod_retinue_battle_store_party),
     (party_is_active, ":retinue_party"),
     (party_is_active, ":store_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (party_get_num_companion_stacks, ":num_stacks", ":store_party"),
     (try_for_range, ":unused", 0, ":num_stacks"),
       (party_stack_get_troop_id, ":stack_troop", ":store_party", 0),
       (is_between, ":stack_troop", soldiers_begin, soldiers_end),
       (neg|troop_is_hero, ":stack_troop"),
       (party_stack_get_size, ":original_count", ":store_party", 0),
       (party_count_members_of_type, ":survivors", "p_main_party", ":stack_troop"),
       (assign, ":restore", ":original_count"),
       (val_min, ":restore", ":survivors"),
       (try_begin),
         (gt, ":restore", 0),
         (party_remove_members, "p_main_party", ":stack_troop", ":restore"),
         (party_add_members, ":retinue_party", ":stack_troop", ":restore"),
       (try_end),
       (party_remove_members, ":store_party", ":stack_troop", ":original_count"),
     (try_end),
     (call_script, "script_sod_companion_retinue_clear_battle_store", ":companion"),
     (call_script, "script_sod_companion_retinue_validate_battle_outcome", ":companion"),
   (try_end),
 ]),

("sod_companion_retinue_finish_battle_bridge",
 [
   (try_begin),
     (gt, "$g_sod_retinue_battle_bridge_active", 0),
     (try_begin),
       (eq, "$g_sod_retinue_battle_bridge_mode", 2),
       (call_script, "script_sod_companion_retinue_finish_merge_fallback"),
     (try_end),
     (try_for_range, ":companion", companions_begin, companions_end),
       (main_party_has_troop, ":companion"),
       (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
       (party_is_active, ":retinue_party"),
       (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
       (party_slot_eq, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
       (try_begin),
         (eq, "$g_sod_retinue_battle_bridge_mode", 1),
         (call_script, "script_sod_companion_retinue_validate_battle_outcome", ":companion"),
       (try_end),
       (party_attach_to_party, ":retinue_party", "p_main_party"),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_anchor_party, "p_main_party"),
       (call_script, "script_sod_companion_retinue_get_size", ":companion"),
       (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
     (try_end),
   (try_end),
   (assign, "$g_sod_retinue_battle_bridge_active", 0),
   (assign, "$g_sod_retinue_battle_bridge_count", 0),
   (assign, "$g_sod_retinue_battle_bridge_mode", 0),
   (assign, "$g_sod_retinue_battle_bridge_fallback_skipped", 0),
   (assign, "$g_sod_retinue_battle_bridge_force_merge", 0),
 ]),

("sod_companion_retinue_handle_player_defeat",
 [
   (try_begin),
     (eq, "$g_sod_retinue_battle_bridge_mode", 2),
     (call_script, "script_sod_companion_retinue_finish_merge_fallback"),
   (try_end),
   (try_for_range, ":companion", companions_begin, companions_end),
     (main_party_has_troop, ":companion"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (party_is_active, ":retinue_party"),
     (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
     (party_slot_eq, ":retinue_party", slot_party_sod_retinue_owner_troop, ":companion"),
     (try_begin),
       (eq, "$g_sod_retinue_battle_bridge_mode", 1),
       (call_script, "script_sod_companion_retinue_validate_battle_outcome", ":companion"),
     (try_end),
     (party_get_num_companions, ":retinue_size", ":retinue_party"),
     (try_begin),
       (gt, ":retinue_size", 0),
       (party_clear, "p_temp_party"),
       (call_script, "script_party_add_party_companions", "p_temp_party", ":retinue_party"),
       (call_script, "script_party_remove_all_companions", ":retinue_party"),
       (distribute_party_among_party_group, "p_temp_party", "$g_enemy_party"),
     (try_end),
     (remove_party, ":retinue_party"),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_party, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_size, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_wage, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_morale, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_invoice, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_wage_reserve, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_shortage, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_supply_pressure, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_training_xp, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_training_hour, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_desertion_day, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_result, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_amount, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_troop, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_post_battle_policy, sod_retinue_post_battle_disabled),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_recruit_hour, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_upgrade_hour, 0),
     (call_script, "script_sod_companion_retinue_clear_battle_store", ":companion"),
   (try_end),
   (assign, "$g_sod_retinue_battle_bridge_active", 0),
   (assign, "$g_sod_retinue_battle_bridge_count", 0),
   (assign, "$g_sod_retinue_battle_bridge_mode", 0),
   (assign, "$g_sod_retinue_battle_bridge_fallback_skipped", 0),
   (assign, "$g_sod_retinue_battle_bridge_force_merge", 0),
 ]),

("sod_companion_retinue_process_daily",
 [
   (try_for_range, ":companion", companions_begin, companions_end),
     (try_begin),
      (main_party_has_troop, ":companion"),
      (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
      (call_script, "script_sod_companion_retinue_get_weekly_wage", ":companion"),
      (call_script, "script_sod_companion_retinue_update_supply_and_morale", ":companion"),
      (assign, ":retinue_morale", reg0),
      (call_script, "script_sod_companion_retinue_update_warning_state", ":companion"),
      (try_begin),
        (le, ":retinue_morale", 25),
        (troop_slot_eq, ":companion", slot_troop_sod_retinue_warning_state, sod_retinue_warning_over_capacity),
        (call_script, "script_sod_companion_retinue_apply_desertion_risk", ":companion", 1),
      (try_end),
      (try_begin),
        (le, ":retinue_morale", 20),
        (troop_get_slot, ":companion_warning_state", ":companion", slot_troop_companion_warning_state),
        (this_or_next|eq, ":companion_warning_state", sod_companion_warning_pending),
        (this_or_next|eq, ":companion_warning_state", sod_companion_warning_final),
        (eq, ":companion_warning_state", sod_companion_warning_broken),
        (call_script, "script_sod_companion_retinue_apply_desertion_risk", ":companion", 1),
      (try_end),
      (call_script, "script_sod_companion_retinue_process_strength_order", ":companion"),
      (call_script, "script_sod_companion_retinue_try_autorecruit", ":companion"),
      (call_script, "script_sod_companion_retinue_try_autoupgrade", ":companion"),
      (call_script, "script_sod_companion_retinue_apply_training", ":companion"),
     (else_try),
       (troop_slot_eq, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
       (call_script, "script_sod_companion_retinue_suspend_for_absence", ":companion", sod_retinue_departure_captured),
     (else_try),
       (call_script, "script_sod_companion_retinue_cleanup_for_departure", ":companion", sod_retinue_departure_cleanup),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_repair_all",
 [
   (try_for_range, ":companion", companions_begin, companions_end),
     (try_begin),
       (main_party_has_troop, ":companion"),
       (call_script, "script_sod_companion_retinue_ensure_party", ":companion"),
     (else_try),
       (troop_slot_eq, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
       (call_script, "script_sod_companion_retinue_suspend_for_absence", ":companion", sod_retinue_departure_captured),
     (else_try),
       (call_script, "script_sod_companion_retinue_cleanup_for_departure", ":companion", sod_retinue_departure_cleanup),
     (try_end),
   (try_end),
 ]),

("sod_companion_retinue_suspend_for_absence",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":reason", 2),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_clear_battle_store", ":companion"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (try_begin),
       (party_is_active, ":retinue_party"),
       (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
       (party_set_ai_behavior, ":retinue_party", ai_bhvr_hold),
       (party_attach_to_party, ":retinue_party", "p_main_party"),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_state, sod_retinue_state_suspended),
       (party_set_slot, ":retinue_party", slot_party_sod_retinue_anchor_party, "p_main_party"),
     (try_end),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_last_shortage, 0),
     (troop_set_slot, ":companion", slot_troop_sod_retinue_wage_reserve, 0),
     (assign, reg0, ":reason"),
   (try_end),
 ]),

("sod_companion_retinue_cleanup_for_departure",
 [
   (store_script_param, ":companion", 1),
   (store_script_param, ":mode", 2),
   (try_begin),
     (is_between, ":companion", companions_begin, companions_end),
     (call_script, "script_sod_companion_retinue_clear_battle_store", ":companion"),
     (troop_get_slot, ":retinue_party", ":companion", slot_troop_sod_retinue_party),
     (try_begin),
       (eq, ":mode", sod_retinue_departure_captured),
       (call_script, "script_sod_companion_retinue_suspend_for_absence", ":companion", ":mode"),
     (else_try),
       (party_is_active, ":retinue_party"),
       (party_slot_eq, ":retinue_party", slot_party_type, spt_companion_retinue),
       (assign, ":returned_total", 0),
       (assign, ":lost_total", 0),
       (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
       (troop_get_slot, ":companion_warning", ":companion", slot_troop_companion_warning_state),
       (assign, ":return_percent", 100),
       (try_begin),
         (eq, ":mode", sod_retinue_departure_angry),
         (try_begin),
           (this_or_next|lt, ":approval", 20),
           (eq, ":companion_warning", sod_companion_warning_broken),
           (assign, ":return_percent", 0),
         (else_try),
           (this_or_next|lt, ":approval", 35),
           (eq, ":companion_warning", sod_companion_warning_final),
           (assign, ":return_percent", 40),
         (else_try),
           (assign, ":return_percent", 75),
         (try_end),
       (try_end),
       (party_get_num_companion_stacks, ":num_stacks", ":retinue_party"),
       (try_for_range, ":unused", 0, ":num_stacks"),
         (party_get_num_companion_stacks, ":remaining_stacks", ":retinue_party"),
         (gt, ":remaining_stacks", 0),
         (party_stack_get_troop_id, ":stack_troop", ":retinue_party", 0),
         (party_stack_get_size, ":stack_size", ":retinue_party", 0),
         (is_between, ":stack_troop", soldiers_begin, soldiers_end),
         (neg|troop_is_hero, ":stack_troop"),
         (gt, ":stack_size", 0),
         (assign, ":return_amount", ":stack_size"),
         (try_begin),
           (eq, ":mode", sod_retinue_departure_angry),
           (store_mul, ":return_amount", ":stack_size", ":return_percent"),
           (val_div, ":return_amount", 100),
         (try_end),
         (party_get_free_companions_capacity, ":free_capacity", "p_main_party"),
         (val_min, ":return_amount", ":free_capacity"),
         (try_begin),
           (gt, ":return_amount", 0),
           (party_remove_members, ":retinue_party", ":stack_troop", ":return_amount"),
           (party_add_members, "p_main_party", ":stack_troop", ":return_amount"),
           (val_add, ":returned_total", ":return_amount"),
         (try_end),
         (party_count_members_of_type, ":still_in_retinue", ":retinue_party", ":stack_troop"),
         (try_begin),
           (gt, ":still_in_retinue", 0),
           (party_remove_members, ":retinue_party", ":stack_troop", ":still_in_retinue"),
           (val_add, ":lost_total", ":still_in_retinue"),
         (try_end),
       (try_end),
       (remove_party, ":retinue_party"),
       (try_begin),
         (this_or_next|gt, ":returned_total", 0),
         (gt, ":lost_total", 0),
         (str_store_troop_name, s20, ":companion"),
         (assign, reg20, ":returned_total"),
         (assign, reg21, ":lost_total"),
         (try_begin),
           (eq, ":mode", sod_retinue_departure_angry),
           (display_message, "@{s20}'s retinue breaks apart: {reg20} troop(s) return to you, while {reg21} follow the companion or scatter.", 0xCC9966),
         (else_try),
           (display_message, "@{s20}'s retinue is dissolved: {reg20} troop(s) return to your command, while {reg21} scatter for lack of room.", 0x99CCFF),
         (try_end),
       (try_end),
     (try_end),
     (troop_get_slot, ":treasury", ":companion", slot_troop_sod_retinue_treasury),
     (try_begin),
       (gt, ":treasury", 0),
       (neq, ":mode", sod_retinue_departure_captured),
       (assign, ":gold_returned", ":treasury"),
       (try_begin),
         (eq, ":mode", sod_retinue_departure_angry),
         (troop_get_slot, ":approval", ":companion", slot_troop_companion_approval),
         (try_begin),
           (lt, ":approval", 20),
           (assign, ":gold_returned", 0),
         (else_try),
           (lt, ":approval", 35),
           (val_div, ":gold_returned", 2),
         (else_try),
           (val_mul, ":gold_returned", 3),
           (val_div, ":gold_returned", 4),
         (try_end),
       (try_end),
       (try_begin),
         (gt, ":gold_returned", 0),
         (call_script, "script_troop_add_gold", "trp_player", ":gold_returned"),
       (try_end),
     (try_end),
     (try_begin),
       (eq, ":mode", sod_retinue_departure_captured),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_suspended),
     (else_try),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_party, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_capacity, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_state, sod_retinue_state_inactive),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_policy, sod_retinue_policy_balanced),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_size, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_wage, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_morale, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_warning_state, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_treasury, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_wage_reserve, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_shortage, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_supply_pressure, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_training_xp, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_training_hour, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_desertion_day, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_strength_order, sod_retinue_strength_none),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_recruit_policy, sod_retinue_recruit_policy_none),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_post_battle_policy, sod_retinue_post_battle_enabled),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_recruit_hour, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_upgrade_hour, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_invoice, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_result, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_amount, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_last_battle_hire_troop, 0),
       (troop_set_slot, ":companion", slot_troop_sod_retinue_battle_store_party, 0),
     (try_end),
   (try_end),
 ]),
]
