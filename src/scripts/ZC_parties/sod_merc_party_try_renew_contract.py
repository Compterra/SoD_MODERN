SCRIPTS = [
("sod_merc_party_try_renew_contract",
 [
   (store_script_param_1, ":cur_party"),

   (assign, ":assigned", 0),
   (try_begin),
     (party_is_active, ":cur_party"),
     (party_slot_eq, ":cur_party", slot_party_type, spt_ai_mercenaries),
     (party_get_slot, ":boss_troop", ":cur_party", slot_party_boss),
     (is_between, ":boss_troop", kingdom_heroes_begin, kingdom_heroes_end),
     (neq, ":boss_troop", "trp_player"),
     (troop_get_slot, ":boss_party", ":boss_troop", slot_troop_leaded_party),
     (gt, ":boss_party", 0),
     (store_troop_faction, ":boss_faction", ":boss_troop"),
     (neq, ":boss_faction", "fac_kingdom_6"),
     (faction_slot_eq, ":boss_faction", slot_faction_state, sfs_active),
     (party_get_slot, ":guild_faction", ":cur_party", slot_party_sod_merc_contract_guild),
     (try_begin),
       (neg|is_between, ":guild_faction", guilds_begin, guilds_end),
       (party_get_slot, ":guild_faction", ":cur_party", slot_party_orginal_faction),
     (try_end),
     (call_script, "script_sod_merc_market_calculate_renewal_price", ":cur_party"),
     (assign, ":renewal_price", reg0),
     (assign, ":term_days", reg1),
     (gt, ":renewal_price", 0),
     (faction_get_slot, ":budget", ":boss_faction", slot_faction_sod_merc_budget),
     (troop_get_slot, ":gold", ":boss_troop", slot_troop_wealth),
     (store_div, ":lord_share", ":renewal_price", 2),
     (ge, ":budget", ":lord_share"),
     (ge, ":gold", ":lord_share"),
     (val_sub, ":budget", ":lord_share"),
     (val_sub, ":gold", ":lord_share"),
     (faction_set_slot, ":boss_faction", slot_faction_sod_merc_budget, ":budget"),
     (troop_set_slot, ":boss_troop", slot_troop_wealth, ":gold"),

     (faction_get_slot, ":treasury", ":guild_faction", slot_faction_sod_merc_treasury),
     (val_add, ":treasury", ":renewal_price"),
     (faction_set_slot, ":guild_faction", slot_faction_sod_merc_treasury, ":treasury"),
     (store_current_day, ":cur_day"),
     (store_add, ":contract_end", ":cur_day", ":term_days"),
     (party_set_slot, ":cur_party", slot_party_merc_contract, ":contract_end"),
     (party_set_slot, ":cur_party", slot_party_sod_merc_contract_term_end, ":contract_end"),
     (party_set_slot, ":cur_party", slot_party_sod_merc_contract_employer, ":boss_faction"),
     (party_set_slot, ":cur_party", slot_party_sod_merc_contract_guild, ":guild_faction"),
     (party_set_slot, ":cur_party", slot_party_sod_merc_contract_value, ":renewal_price"),
     (party_set_slot, ":cur_party", slot_party_commander_party, ":boss_party"),
     (party_set_faction, ":cur_party", ":boss_faction"),
     (party_set_ai_object, ":cur_party", ":boss_party"),
     (party_set_ai_behavior, ":cur_party", ai_bhvr_escort_party),
     (assign, ":assigned", 1),
   (try_end),

   (assign, reg0, ":assigned"),
 ]),
]
