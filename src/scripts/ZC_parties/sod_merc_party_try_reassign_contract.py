SCRIPTS = [
("sod_merc_party_try_reassign_contract",
 [
   (store_script_param_1, ":cur_party"),

   (assign, ":assigned", 0),
   (try_begin),
     (party_is_active, ":cur_party"),
     (party_slot_eq, ":cur_party", slot_party_type, spt_ai_mercenaries),
     (party_get_slot, ":old_boss_troop", ":cur_party", slot_party_boss),
     (is_between, ":old_boss_troop", kingdom_heroes_begin, kingdom_heroes_end),
     (store_troop_faction, ":old_faction", ":old_boss_troop"),
     (neq, ":old_faction", "fac_kingdom_6"),
     (faction_slot_eq, ":old_faction", slot_faction_state, sfs_active),
     (call_script, "script_sod_merc_market_calculate_kingdom_demand", ":old_faction"),
     (faction_get_slot, ":demand_score", ":old_faction", slot_faction_sod_merc_demand_score),
     (ge, ":demand_score", 30),
     (party_get_slot, ":guild_faction", ":cur_party", slot_party_sod_merc_contract_guild),
     (try_begin),
       (neg|is_between, ":guild_faction", guilds_begin, guilds_end),
       (party_get_slot, ":guild_faction", ":cur_party", slot_party_orginal_faction),
     (try_end),
     (call_script, "script_sod_merc_market_calculate_renewal_price", ":cur_party"),
     (assign, ":renewal_price", reg0),
     (assign, ":term_days", reg1),
     (gt, ":renewal_price", 0),
     (try_for_range, ":new_boss", kingdom_heroes_begin, kingdom_heroes_end),
       (eq, ":assigned", 0),
       (neq, ":new_boss", ":old_boss_troop"),
       (store_troop_faction, ":new_boss_faction", ":new_boss"),
       (eq, ":old_faction", ":new_boss_faction"),
       (troop_get_slot, ":new_boss_party", ":new_boss", slot_troop_leaded_party),
       (gt, ":new_boss_party", 0),
       (try_begin),
         (faction_slot_eq, ":old_faction", slot_faction_leader, ":new_boss"),
         (assign, ":limit", 3),
       (else_try),
         (assign, ":limit", 1),
       (try_end),
       (assign, ":active_parties", 0),
       (try_for_parties, ":other_party"),
         (party_slot_eq, ":other_party", slot_party_type, spt_ai_mercenaries),
         (party_slot_eq, ":other_party", slot_party_boss, ":new_boss"),
         (val_add, ":active_parties", 1),
       (try_end),
       (store_sub, ":free_slots", ":limit", ":active_parties"),
       (gt, ":free_slots", 0),
       (faction_get_slot, ":budget", ":old_faction", slot_faction_sod_merc_budget),
       (troop_get_slot, ":gold", ":new_boss", slot_troop_wealth),
       (store_div, ":lord_share", ":renewal_price", 2),
       (ge, ":budget", ":lord_share"),
       (ge, ":gold", ":lord_share"),
       (val_sub, ":budget", ":lord_share"),
       (val_sub, ":gold", ":lord_share"),
       (faction_set_slot, ":old_faction", slot_faction_sod_merc_budget, ":budget"),
       (troop_set_slot, ":new_boss", slot_troop_wealth, ":gold"),

       (troop_get_slot, ":cur_banner", ":new_boss", slot_troop_banner_scene_prop),
       (try_begin),
         (gt, ":cur_banner", 0),
         (val_sub, ":cur_banner", banner_scene_props_begin),
         (val_add, ":cur_banner", banner_map_icons_begin),
         (party_set_banner_icon, ":cur_party", ":cur_banner"),
       (try_end),
       (try_begin),
         (troop_slot_eq, ":old_boss_troop", slot_troop_mercenaries, ":cur_party"),
         (troop_set_slot, ":old_boss_troop", slot_troop_mercenaries, -1),
       (try_end),
       (faction_get_slot, ":treasury", ":guild_faction", slot_faction_sod_merc_treasury),
       (val_add, ":treasury", ":renewal_price"),
       (faction_set_slot, ":guild_faction", slot_faction_sod_merc_treasury, ":treasury"),
       (store_current_day, ":cur_day"),
       (store_add, ":contract_end", ":cur_day", ":term_days"),
       (party_set_faction, ":cur_party", ":old_faction"),
       (party_set_slot, ":cur_party", slot_party_merc_contract, ":contract_end"),
       (party_set_slot, ":cur_party", slot_party_sod_merc_contract_term_end, ":contract_end"),
       (party_set_slot, ":cur_party", slot_party_sod_merc_contract_employer, ":old_faction"),
       (party_set_slot, ":cur_party", slot_party_sod_merc_contract_guild, ":guild_faction"),
       (party_set_slot, ":cur_party", slot_party_sod_merc_contract_value, ":renewal_price"),
       (party_set_slot, ":cur_party", slot_party_boss, ":new_boss"),
       (party_set_slot, ":cur_party", slot_party_commander_party, ":new_boss_party"),
       (troop_set_slot, ":new_boss", slot_troop_mercenaries, ":cur_party"),
       (party_set_ai_object, ":cur_party", ":new_boss_party"),
       (party_set_ai_behavior, ":cur_party", ai_bhvr_escort_party),
       (assign, ":assigned", 1),
     (try_end),
   (try_end),

   (assign, reg0, ":assigned"),
 ]),
]
