SCRIPTS = [
("sod_battle_enemy_surrender_check",
 [
   (assign, reg0, 0),
   (try_begin),
     (eq, "$battle_won", 0),
     (neq, "$g_enemy_surrenders", 1),
     (store_mission_timer_a, ":mission_time"),
     (ge, ":mission_time", 45),
     (assign, ":alive_allies", 0),
     (assign, ":alive_enemies", 0),
     (assign, ":fighting_enemies", 0),
     (assign, ":routed_enemies", 0),
     (assign, ":enemy_hero_alive", 0),
     (try_for_agents, ":agent_no"),
       (agent_is_human, ":agent_no"),
       (agent_is_alive, ":agent_no"),
       (agent_get_troop_id, ":troop_type", ":agent_no"),
       (try_begin),
         (agent_is_ally, ":agent_no"),
         (val_add, ":alive_allies", 1),
       (else_try),
         (val_add, ":alive_enemies", 1),
         (try_begin),
           (troop_is_hero, ":troop_type"),
           (assign, ":enemy_hero_alive", 1),
         (try_end),
         (try_begin),
           (agent_slot_eq, ":agent_no", slot_agent_is_hard_routed, 1),
           (val_add, ":routed_enemies", 1),
         (else_try),
           (val_add, ":fighting_enemies", 1),
         (try_end),
       (try_end),
     (try_end),
     (gt, ":alive_enemies", 0),
     (gt, ":alive_allies", 0),
     (eq, ":enemy_hero_alive", 0),
     (assign, ":surrender_score", 0),
     (try_begin),
       (store_mul, ":required_allies", ":alive_enemies", 3),
       (ge, ":alive_allies", ":required_allies"),
       (val_add, ":surrender_score", 2),
     (try_end),
     (try_begin),
       (gt, ":routed_enemies", 0),
       (store_mul, ":rout_pressure", ":fighting_enemies", 2),
       (ge, ":routed_enemies", ":rout_pressure"),
       (val_add, ":surrender_score", 2),
     (try_end),
     (try_begin),
       (lt, "$enemies_coh", 450),
       (gt, "$allies_coh", "$enemies_coh"),
       (val_add, ":surrender_score", 1),
     (try_end),
     (ge, ":surrender_score", 2),
     (assign, "$g_enemy_surrenders", 1),
     (assign, "$battle_won", 1),
     (assign, "$g_battle_result", 1),
     (display_message, "@The enemy line breaks. Those still standing throw down their arms.", 0x7ccd7c),
     (assign, reg0, 1),
   (try_end),
 ]),
]
