SCRIPTS = [
("sod_auto_sell_item_is_allowed",
 [
   (store_script_param_1, ":item_no"),
   (store_script_param_2, ":imod"),
   (store_script_param, ":price_limit", 3),

   (assign, reg0, 0),
   (assign, ":allowed", 0),
   (try_begin),
     (gt, ":item_no", 0),
     (call_script, "script_sod_auto_loot_item_is_protected", ":item_no"),
     (assign, ":is_protected", reg0),
     (try_begin),
       (eq, ":is_protected", 0),
       (item_get_type, ":item_type", ":item_no"),
       (neq, ":item_type", itp_type_goods),
       (neq, ":item_type", itp_type_book),
       (neq, ":item_type", itp_type_horse),
       (neq, ":item_type", itp_type_animal),
       (call_script, "script_get_item_cost_with_imod", ":item_no", ":imod"),
       (assign, ":item_value", reg0),
       (le, ":item_value", ":price_limit"),
       (assign, ":allowed", 1),
     (try_end),
   (try_end),
   (assign, reg0, ":allowed"),
 ]),
]
