SCRIPTS = [
("sod_describe_auto_loot_item_evaluation_to_s68",
 [
   (store_script_param_1, ":troop_no"),
   (store_script_param_2, ":item_no"),
   (store_script_param, ":imod", 3),
   (store_script_param, ":require_mount_compatible", 4),

   (try_begin),
     (lt, ":item_no", 0),
     (str_store_string, s68, "@empty slot"),
   (else_try),
     (call_script, "script_describe_item_with_imod", s1, ":item_no", ":imod"),
     (call_script, "script_sod_auto_loot_item_is_protected", ":item_no"),
     (try_begin),
       (eq, reg0, 1),
       (str_store_string, s68, "@{s1}: protected from automation"),
     (else_try),
       (eq, ":require_mount_compatible", 1),
       (item_slot_eq, ":item_no", slot_item_cant_use_on_horseback, 1),
       (str_store_string, s68, "@{s1}: rejected because it cannot be used on horseback"),
     (else_try),
       (call_script, "script_troop_can_use_item", ":troop_no", ":item_no", ":imod"),
       (eq, reg0, 0),
       (str_store_string, s68, "@{s1}: rejected because requirements are not met"),
     (else_try),
       (call_script, "script_get_item_score_with_imod", ":item_no", ":imod"),
       (assign, reg21, reg0),
       (call_script, "script_get_item_cost_with_imod", ":item_no", ":imod"),
       (assign, reg22, reg0),
       (str_store_string, s68, "@{s1}: usable, score {reg21}, modifier-adjusted value {reg22} denars"),
     (try_end),
   (try_end),
 ]),
("sod_describe_auto_loot_item_evaluation_to_s0",
 [
   (store_script_param_1, ":troop_no"),
   (store_script_param_2, ":item_no"),
   (store_script_param, ":imod", 3),
   (store_script_param, ":require_mount_compatible", 4),
   (call_script, "script_sod_describe_auto_loot_item_evaluation_to_s68", ":troop_no", ":item_no", ":imod", ":require_mount_compatible"),
   (str_store_string_reg, s0, s68),
 ]),
]
