# COST: light noble-house memory helpers and report text.
from header_operations import *
from module_constants import *
from module_troops import *

SCRIPTS = [
("sod_house_adjust_memory",
 [
   (store_script_param, ":troop_no", 1),
   (store_script_param, ":grievance_delta", 2),
   (store_script_param, ":loyalty_delta", 3),
   (store_script_param, ":claim_delta", 4),
   (try_begin),
     (eq, "$g_sod_house_politics_active", 1),
     (is_between, ":troop_no", heroes_begin, heroes_end),
     (troop_get_slot, ":house_id", ":troop_no", slot_troop_sod_house_id),
     (gt, ":house_id", 0),
     (store_current_day, "$g_sod_house_last_incident_day"),
     (assign, "$g_sod_house_last_incident_troop", ":troop_no"),
     (try_for_range, ":member_no", heroes_begin, heroes_end),
       (troop_slot_eq, ":member_no", slot_troop_sod_house_id, ":house_id"),
       (try_begin),
         (neq, ":grievance_delta", 0),
         (troop_get_slot, ":grievance", ":member_no", slot_troop_sod_house_grievance),
         (val_add, ":grievance", ":grievance_delta"),
         (val_clamp, ":grievance", 0, 101),
         (troop_set_slot, ":member_no", slot_troop_sod_house_grievance, ":grievance"),
       (try_end),
       (try_begin),
         (neq, ":loyalty_delta", 0),
         (troop_get_slot, ":loyalty", ":member_no", slot_troop_sod_house_loyalty),
         (val_add, ":loyalty", ":loyalty_delta"),
         (val_clamp, ":loyalty", 0, 101),
         (troop_set_slot, ":member_no", slot_troop_sod_house_loyalty, ":loyalty"),
       (try_end),
       (try_begin),
         (neq, ":claim_delta", 0),
         (troop_get_slot, ":claim", ":member_no", slot_troop_sod_house_claim_strength),
         (val_add, ":claim", ":claim_delta"),
         (val_clamp, ":claim", 0, 101),
         (troop_set_slot, ":member_no", slot_troop_sod_house_claim_strength, ":claim"),
       (try_end),
     (try_end),
   (try_end),
 ]),

("sod_house_note_lord_captured_by_player",
 [
   (store_script_param_1, ":troop_no"),
   (try_begin),
     (is_between, ":troop_no", kingdom_heroes_begin, kingdom_heroes_end),
     (call_script, "script_sod_house_adjust_memory", ":troop_no", 12, -3, 1),
     (call_script, "script_sod_strategy_advisor_note_house_incident", ":troop_no", 1),
   (try_end),
 ]),

("sod_house_note_lord_released_by_player",
 [
   (store_script_param_1, ":troop_no"),
   (try_begin),
     (is_between, ":troop_no", kingdom_heroes_begin, kingdom_heroes_end),
     (call_script, "script_sod_house_adjust_memory", ":troop_no", -8, 5, 0),
     (call_script, "script_sod_strategy_advisor_note_house_incident", ":troop_no", 2),
   (try_end),
 ]),

("sod_house_note_fief_change",
 [
   (store_script_param, ":center_no", 1),
   (store_script_param, ":old_lord", 2),
   (store_script_param, ":new_lord", 3),
   (try_begin),
     (eq, "$g_sod_house_politics_active", 1),
     (is_between, ":center_no", centers_begin, centers_end),
     (neq, ":old_lord", ":new_lord"),
     (assign, ":player_realm_transfer", 0),
     (try_begin),
       (eq, ":new_lord", "trp_player"),
       (assign, ":player_realm_transfer", 1),
     (else_try),
       (is_between, ":new_lord", kingdom_heroes_begin, kingdom_heroes_end),
       (store_troop_faction, ":new_lord_faction", ":new_lord"),
       (this_or_next|eq, ":new_lord_faction", "$players_kingdom"),
       (eq, ":new_lord_faction", "fac_player_supporters_faction"),
       (assign, ":player_realm_transfer", 1),
     (try_end),
     (try_begin),
       (is_between, ":old_lord", kingdom_heroes_begin, kingdom_heroes_end),
       (eq, ":new_lord", "trp_player"),
       (call_script, "script_sod_house_adjust_memory", ":old_lord", 18, -10, 4),
       (call_script, "script_change_player_relation_with_troop", ":old_lord", -15),
       (call_script, "script_sod_strategy_advisor_note_house_incident", ":old_lord", 3),
     (else_try),
       (is_between, ":old_lord", kingdom_heroes_begin, kingdom_heroes_end),
       (is_between, ":new_lord", kingdom_heroes_begin, kingdom_heroes_end),
       (eq, ":player_realm_transfer", 1),
       (call_script, "script_sod_house_adjust_memory", ":old_lord", 10, -6, 3),
       (call_script, "script_change_player_relation_with_troop", ":old_lord", -8),
       (call_script, "script_sod_strategy_advisor_note_house_incident", ":old_lord", 4),
     (try_end),
     (try_begin),
       (is_between, ":new_lord", kingdom_heroes_begin, kingdom_heroes_end),
       (eq, ":player_realm_transfer", 1),
       (call_script, "script_sod_house_adjust_memory", ":new_lord", -4, 8, 2),
       (call_script, "script_sod_strategy_advisor_note_house_incident", ":new_lord", 5),
     (try_end),
   (try_end),
 ]),

("sod_house_describe_noble_houses_to_s1",
 [
   (assign, ":realm_faction", "fac_player_supporters_faction"),
   (try_begin),
     (gt, "$players_kingdom", 0),
     (assign, ":realm_faction", "$players_kingdom"),
   (try_end),
   (str_store_faction_name, s2, ":realm_faction"),
   (str_store_string, s1, "@Noble house ledger for {s2}.^^"),
   (assign, ":listed", 0),
   (try_for_range, ":lord_no", kingdom_heroes_begin, kingdom_heroes_end),
     (store_troop_faction, ":lord_faction", ":lord_no"),
     (eq, ":lord_faction", ":realm_faction"),
     (troop_slot_eq, ":lord_no", slot_troop_sod_house_head, ":lord_no"),
     (call_script, "script_store_troop_name", s3, ":lord_no"),
     (troop_get_slot, reg3, ":lord_no", slot_troop_sod_house_loyalty),
     (troop_get_slot, reg4, ":lord_no", slot_troop_sod_house_grievance),
     (troop_get_slot, reg5, ":lord_no", slot_troop_sod_house_claim_strength),
     (call_script, "script_get_number_of_hero_centers", ":lord_no"),
     (assign, reg6, reg0),
     (troop_get_slot, ":spouse_no", ":lord_no", slot_troop_spouse),
     (try_begin),
       (is_between, ":spouse_no", heroes_begin, heroes_end),
       (call_script, "script_store_troop_name", s4, ":spouse_no"),
     (else_try),
       (str_store_string, s4, "@no recorded spouse"),
     (try_end),
     (try_begin),
       (troop_slot_ge, ":lord_no", slot_troop_prisoner_of_party, 0),
       (str_store_string, s5, "@prisoner"),
     (else_try),
       (str_store_string, s5, "@free"),
     (try_end),
     (str_store_string_reg, s97, s1),
     (str_store_string, s1, "@{s97}^{s3}: loyalty {reg3}, grievance {reg4}, claim {reg5}, fiefs {reg6}; spouse: {s4}; {s5}."),
     (val_add, ":listed", 1),
   (try_end),
   (try_begin),
     (eq, ":listed", 0),
     (str_store_string_reg, s97, s1),
     (str_store_string, s1, "@{s97}No noble houses in your realm are visible yet."),
   (try_end),
   (str_store_string_reg, s97, s1),
   (str_store_string, s1, "@{s97}^^Active claimants:"),
   (try_for_range, ":pretender", pretenders_begin, pretenders_end),
     (troop_get_slot, ":original_faction", ":pretender", slot_troop_original_faction),
     (is_between, ":original_faction", kingdoms_begin, kingdoms_end),
     (call_script, "script_store_troop_name", s3, ":pretender"),
     (str_store_faction_name, s4, ":original_faction"),
     (troop_get_slot, reg3, ":pretender", slot_troop_sod_house_claim_strength),
     (troop_get_slot, reg4, ":pretender", slot_troop_sod_pretender_claim_pressure),
     (troop_get_slot, reg5, ":pretender", slot_troop_sod_pretender_momentum),
     (troop_get_slot, ":foothold", ":pretender", slot_troop_sod_pretender_foothold_center),
     (try_begin),
       (is_between, ":foothold", centers_begin, centers_end),
       (str_store_party_name, s5, ":foothold"),
     (else_try),
       (troop_get_slot, ":support_base", ":pretender", slot_troop_support_base),
       (is_between, ":support_base", centers_begin, centers_end),
       (str_store_party_name, s5, ":support_base"),
     (else_try),
       (str_store_string, s5, "@no known foothold"),
     (try_end),
     (troop_get_slot, ":backer", ":pretender", slot_troop_sod_pretender_backer_lord),
     (try_begin),
       (is_between, ":backer", kingdom_heroes_begin, kingdom_heroes_end),
       (call_script, "script_store_troop_name", s6, ":backer"),
     (else_try),
       (str_store_string, s6, "@no declared noble backer"),
     (try_end),
     (try_begin),
       (eq, ":pretender", "$supported_pretender"),
       (str_store_string, s7, "@player-backed rebellion"),
     (else_try),
       (ge, reg4, sod_pretender_pressure_foothold),
       (str_store_string, s7, "@active underground cause"),
     (else_try),
       (ge, reg4, sod_pretender_pressure_stirring),
       (str_store_string, s7, "@stirring claimant cause"),
     (else_try),
       (str_store_string, s7, "@quiet claimant cause"),
     (try_end),
     (call_script, "script_sod_pretender_get_rebel_faction_to_reg", ":pretender"),
     (assign, ":rebel_faction", reg0),
     (try_begin),
       (is_between, ":rebel_faction", rebel_factions_begin, rebel_factions_end),
       (call_script, "script_sod_claimant_count_war_assets_to_regs", ":original_faction", ":rebel_faction"),
       (assign, reg6, reg0),
       (assign, reg7, reg1),
       (assign, reg8, reg2),
       (faction_get_slot, ":old_ruler", ":rebel_faction", slot_faction_sod_claimant_old_ruler),
       (try_begin),
         (is_between, ":old_ruler", kingdom_heroes_begin, kingdom_heroes_end),
         (call_script, "script_store_troop_name", s8, ":old_ruler"),
       (else_try),
         (str_store_string, s8, "@no recorded old ruler"),
       (try_end),
       (store_current_day, ":cur_day"),
       (faction_get_slot, ":started_day", ":rebel_faction", slot_faction_sod_civil_war_started_day),
       (try_begin),
         (gt, ":started_day", 0),
         (store_sub, reg9, ":cur_day", ":started_day"),
       (else_try),
         (assign, reg9, 0),
       (try_end),
       (try_begin),
         (le, reg6, 0),
         (ge, reg7, 1),
         (str_store_string, s9, "@likely rebel victory"),
       (else_try),
         (le, reg7, 0),
         (str_store_string, s9, "@fragile or failed rebel court"),
       (else_try),
         (gt, reg7, reg6),
         (str_store_string, s9, "@rebels gaining ground"),
       (else_try),
         (str_store_string, s9, "@loyalists hold advantage"),
       (try_end),
     (else_try),
       (assign, reg6, 0),
       (assign, reg7, 0),
       (assign, reg8, 0),
       (assign, reg9, 0),
       (str_store_string, s8, "@no recorded old ruler"),
       (str_store_string, s9, "@no rebel court"),
     (try_end),
     (str_store_string_reg, s97, s1),
     (str_store_string, s1, "@{s97}^{s3} for {s4}: claim {reg3}, pressure {reg4}, momentum {reg5}; foothold: {s5}; backer: {s6}; {s7}. Rebel fiefs {reg7}, loyalist fiefs {reg6}, rebel lord parties {reg8}, old ruler {s8}, war age {reg9}; {s9}."),
   (try_end),
   (assign, ":hot_house", -1),
   (assign, ":hot_grievance", -1),
   (try_for_range, ":lord_no", kingdom_heroes_begin, kingdom_heroes_end),
     (troop_slot_eq, ":lord_no", slot_troop_sod_house_head, ":lord_no"),
     (troop_get_slot, ":grievance", ":lord_no", slot_troop_sod_house_grievance),
     (gt, ":grievance", ":hot_grievance"),
     (assign, ":hot_grievance", ":grievance"),
     (assign, ":hot_house", ":lord_no"),
   (try_end),
   (try_begin),
     (is_between, ":hot_house", kingdom_heroes_begin, kingdom_heroes_end),
     (gt, ":hot_grievance", 0),
     (call_script, "script_store_troop_name", s6, ":hot_house"),
     (assign, reg7, ":hot_grievance"),
     (str_store_string_reg, s97, s1),
     (str_store_string, s1, "@{s97}^^Most aggrieved known house: {s6}, grievance {reg7}."),
   (try_end),
 ]),

("sod_strategy_advisor_describe_house_politics_to_s1",
 [
   (assign, ":hot_house", -1),
   (assign, ":hot_grievance", -1),
   (assign, ":loyal_house", -1),
   (assign, ":best_loyalty", -1),
   (try_for_range, ":lord_no", kingdom_heroes_begin, kingdom_heroes_end),
     (troop_slot_eq, ":lord_no", slot_troop_sod_house_head, ":lord_no"),
     (troop_get_slot, ":grievance", ":lord_no", slot_troop_sod_house_grievance),
     (troop_get_slot, ":loyalty", ":lord_no", slot_troop_sod_house_loyalty),
     (try_begin),
       (gt, ":grievance", ":hot_grievance"),
       (assign, ":hot_grievance", ":grievance"),
       (assign, ":hot_house", ":lord_no"),
     (try_end),
     (try_begin),
       (gt, ":loyalty", ":best_loyalty"),
       (assign, ":best_loyalty", ":loyalty"),
       (assign, ":loyal_house", ":lord_no"),
     (try_end),
   (try_end),
   (try_begin),
     (is_between, "$g_sod_house_last_incident_troop", kingdom_heroes_begin, kingdom_heroes_end),
     (call_script, "script_store_troop_name", s2, "$g_sod_house_last_incident_troop"),
     (assign, reg2, "$g_sod_house_last_incident_day"),
     (str_store_string, s1, "@The last noble-house incident concerned {s2}, recorded on day {reg2}. "),
   (else_try),
     (str_store_string, s1, "@No noble-house incident has been recorded yet. "),
   (try_end),
   (try_begin),
     (is_between, ":hot_house", kingdom_heroes_begin, kingdom_heroes_end),
     (call_script, "script_store_troop_name", s3, ":hot_house"),
     (assign, reg3, ":hot_grievance"),
     (str_store_string_reg, s97, s1),
     (str_store_string, s1, "@{s97}The sharpest grievance belongs to {s3}, at {reg3}. "),
   (try_end),
   (try_begin),
     (is_between, ":loyal_house", kingdom_heroes_begin, kingdom_heroes_end),
     (call_script, "script_store_troop_name", s4, ":loyal_house"),
     (assign, reg4, ":best_loyalty"),
     (str_store_string_reg, s97, s1),
     (str_store_string, s1, "@{s97}The firmest loyalty belongs to {s4}, at {reg4}. Houses are not family trees yet; they are memory, obligation, and danger made visible."),
   (try_end),
   (assign, ":strongest_pretender", 0),
   (assign, ":strongest_pressure", -1),
   (try_for_range, ":pretender", pretenders_begin, pretenders_end),
     (troop_get_slot, ":pressure", ":pretender", slot_troop_sod_pretender_claim_pressure),
     (gt, ":pressure", ":strongest_pressure"),
     (assign, ":strongest_pressure", ":pressure"),
     (assign, ":strongest_pretender", ":pretender"),
   (try_end),
   (try_begin),
     (is_between, ":strongest_pretender", pretenders_begin, pretenders_end),
     (ge, ":strongest_pressure", sod_pretender_pressure_stirring),
     (call_script, "script_store_troop_name", s5, ":strongest_pretender"),
     (troop_get_slot, ":original_faction", ":strongest_pretender", slot_troop_original_faction),
     (str_store_faction_name, s6, ":original_faction"),
     (assign, reg5, ":strongest_pressure"),
     (str_store_string_reg, s97, s1),
     (str_store_string, s1, "@{s97} {s5}'s claim in {s6} is no longer idle; pressure stands at {reg5}."),
   (try_end),
   (str_store_string, s10, "@{s1}"),
   (call_script, "script_sod_claimant_describe_active_wars_to_s1"),
   (str_store_string_reg, s97, s1),
   (str_store_string, s1, "@{s10} {s97}"),
 ]),

("sod_strategy_advisor_note_house_incident",
 [
   (store_script_param, ":troop_no", 1),
   (store_script_param, ":incident", 2),
   (try_begin),
     (eq, "$g_sod_house_politics_active", 1),
     (is_between, ":troop_no", kingdom_heroes_begin, kingdom_heroes_end),
     (troop_get_slot, ":grievance", ":troop_no", slot_troop_sod_house_grievance),
     (ge, ":grievance", 45),
     (store_current_day, ":cur_day"),
     (store_sub, ":days_since", ":cur_day", "$g_sod_house_last_advisor_warning_day"),
     (this_or_next|eq, "$g_sod_house_last_advisor_warning_day", 0),
     (ge, ":days_since", 7),
     (assign, "$g_sod_house_last_advisor_warning_day", ":cur_day"),
     (call_script, "script_store_troop_name", s2, ":troop_no"),
     (assign, reg2, ":grievance"),
     (try_begin),
       (eq, ":incident", 1),
       (str_store_string, s3, "@captivity"),
     (else_try),
       (eq, ":incident", 2),
       (str_store_string, s3, "@mercy"),
     (else_try),
       (this_or_next|eq, ":incident", 3),
       (eq, ":incident", 4),
       (str_store_string, s3, "@lost land"),
     (else_try),
       (str_store_string, s3, "@new favor"),
     (try_end),
     (display_message, "@Cassian notes that {s2}'s house is carrying grievance {reg2} after {s3}. Grudges become politics when left untended.", 0xFFCC66),
   (try_end),
 ]),
]
