# COST: medium
SCRIPTS = [
("sod_artifact_can_lord_take_item",
 [
   (store_script_param_1, ":captor"),
   (store_script_param_2, ":item_no"),
   (store_script_param, ":item_modifier", 3),
   (assign, reg0, 0),
   (assign, reg1, -1),

   (item_get_type, ":item_type", ":item_no"),
   (assign, ":slot_begin", ek_item_0),
   (assign, ":slot_end", ek_item_3 + 1),
   (try_begin),
     (eq, ":item_type", itp_type_head_armor),
     (assign, ":slot_begin", ek_head),
     (assign, ":slot_end", ek_head + 1),
   (else_try),
     (eq, ":item_type", itp_type_body_armor),
     (assign, ":slot_begin", ek_body),
     (assign, ":slot_end", ek_body + 1),
   (else_try),
     (eq, ":item_type", itp_type_foot_armor),
     (assign, ":slot_begin", ek_foot),
     (assign, ":slot_end", ek_foot + 1),
   (else_try),
     (eq, ":item_type", itp_type_hand_armor),
     (assign, ":slot_begin", ek_gloves),
     (assign, ":slot_end", ek_gloves + 1),
   (else_try),
     (eq, ":item_type", itp_type_horse),
     (assign, ":slot_begin", ek_horse),
     (assign, ":slot_end", ek_horse + 1),
   (try_end),

   (try_for_range, ":slot", ":slot_begin", ":slot_end"),
     (eq, reg0, 0),
     (troop_get_inventory_slot, ":current_item", ":captor", ":slot"),
     (try_begin),
       (lt, ":current_item", 0),
       (assign, reg0, 1),
       (assign, reg1, ":slot"),
     (else_try),
       (item_get_slot, ":current_flags", ":current_item", slot_item_artifact_flags),
       (le, ":current_flags", 0),
       (call_script, "script_get_item_score_with_imod", ":item_no", ":item_modifier"),
       (assign, ":new_score", reg0),
       (troop_get_inventory_slot_modifier, ":current_imod", ":captor", ":slot"),
       (call_script, "script_get_item_score_with_imod", ":current_item", ":current_imod"),
       (assign, ":old_score", reg0),
       (try_begin),
         (ge, ":new_score", ":old_score"),
         (assign, reg0, 1),
         (assign, reg1, ":slot"),
       (try_end),
     (try_end),
   (try_end),

   (try_begin),
     (eq, reg0, 0),
     (troop_get_inventory_capacity, ":inv_cap", ":captor"),
     (try_for_range, ":slot", first_inventory_slot, ":inv_cap"),
       (eq, reg0, 0),
       (troop_get_inventory_slot, ":current_item", ":captor", ":slot"),
       (lt, ":current_item", 0),
       (assign, reg0, 1),
       (assign, reg1, ":slot"),
     (try_end),
   (try_end),
 ]),
]
