# COST: trivial
SCRIPTS = [
("sod_artifact_get_progress",
 [
   (store_script_param_1, ":item_no"),
   (store_script_param_2, ":item_modifier"),
   (assign, reg0, 0),
   (assign, reg1, 0),
   (assign, reg2, 25),
   (try_begin),
     (gt, ":item_no", 0),
     (call_script, "script_sod_artifact_get_modifier_block", ":item_modifier"),
     (store_mul, ":offset", reg0, artifact_progress_stride),
     (store_add, ":kills_slot", slot_item_artifact_progress_begin, ":offset"),
     (store_add, ":milestone_slot", ":kills_slot", 1),
     (item_get_slot, reg0, ":item_no", ":kills_slot"),
     (item_get_slot, reg1, ":item_no", ":milestone_slot"),
     (try_begin),
       (ge, reg1, 4),
       (assign, reg2, 700),
     (else_try),
       (ge, reg1, 3),
       (assign, reg2, 700),
     (else_try),
       (ge, reg1, 2),
       (assign, reg2, 300),
     (else_try),
       (ge, reg1, 1),
       (assign, reg2, 100),
     (else_try),
       (assign, reg2, 25),
     (try_end),
   (try_end),
 ]),
]
