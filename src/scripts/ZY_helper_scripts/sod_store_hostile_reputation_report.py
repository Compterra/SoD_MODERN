# COST: light
SCRIPTS = [
("sod_store_hostile_reputation_report",
 [
   (assign, reg1, "$g_sod_hostile_mercy_count"),
   (assign, reg2, "$g_sod_hostile_recruit_count"),
   (assign, reg3, "$g_sod_hostile_paid_toll_count"),
   (assign, reg4, "$g_sod_hostile_intimidation_count"),
   (assign, reg5, "$g_sod_hostile_shakedown_count"),
   (assign, reg6, "$g_sod_hostile_informant_count"),
   (assign, reg7, "$g_sod_hostile_deserter_destroyed_count"),
   (assign, reg8, "$g_sod_hostile_refugee_help_count"),
   (assign, reg9, "$g_sod_hostile_regional_noncombat_count"),
   (assign, reg10, "$g_sod_hostile_regional_combat_count"),
   (assign, reg11, "$g_sod_hostile_prisoner_count"),
   (assign, reg12, "$g_sod_hostile_reputation_last_day"),
   (try_begin),
     (gt, "$g_sod_hostile_reputation_last_center", 0),
     (str_store_party_name, s8, "$g_sod_hostile_reputation_last_center"),
   (else_try),
     (str_store_string, s8, "@no specific center"),
   (try_end),
   (try_begin),
     (gt, "$g_sod_hostile_reputation_last_faction", 0),
     (str_store_faction_name, s9, "$g_sod_hostile_reputation_last_faction"),
   (else_try),
     (str_store_string, s9, "@no specific faction"),
   (try_end),
   (assign, ":dominant", 0),
   (assign, ":dominant_score", 0),
   (try_begin),
     (gt, "$g_sod_hostile_mercy_count", ":dominant_score"),
     (assign, ":dominant_score", "$g_sod_hostile_mercy_count"),
     (assign, ":dominant", 1),
   (try_end),
   (try_begin),
     (gt, "$g_sod_hostile_paid_toll_count", ":dominant_score"),
     (assign, ":dominant_score", "$g_sod_hostile_paid_toll_count"),
     (assign, ":dominant", 2),
   (try_end),
   (try_begin),
     (gt, "$g_sod_hostile_intimidation_count", ":dominant_score"),
     (assign, ":dominant_score", "$g_sod_hostile_intimidation_count"),
     (assign, ":dominant", 3),
   (try_end),
   (try_begin),
     (gt, "$g_sod_hostile_shakedown_count", ":dominant_score"),
     (assign, ":dominant_score", "$g_sod_hostile_shakedown_count"),
     (assign, ":dominant", 4),
   (try_end),
   (try_begin),
     (gt, "$g_sod_hostile_deserter_destroyed_count", ":dominant_score"),
     (assign, ":dominant_score", "$g_sod_hostile_deserter_destroyed_count"),
     (assign, ":dominant", 5),
   (try_end),
   (try_begin),
     (eq, ":dominant", 1),
     (str_store_string, s6, "@Road reputation: merciful negotiator."),
     (str_store_string, s7, "@Deserters and hungry bands may test your mercy before testing your line."),
   (else_try),
     (eq, ":dominant", 2),
     (str_store_string, s6, "@Road reputation: payer of tolls."),
     (str_store_string, s7, "@Bandits may open with silver before steel, but they will also smell weakness if you indulge them too often."),
   (else_try),
     (eq, ":dominant", 3),
     (str_store_string, s6, "@Road reputation: frightening banner."),
     (str_store_string, s7, "@Weak raiders may scatter, while proud ones may attack simply to prove they are not afraid."),
   (else_try),
     (eq, ":dominant", 4),
     (str_store_string, s6, "@Road reputation: robber of robbers."),
     (str_store_string, s7, "@Outlaws know you make them pay. Expect fewer clean bargains and more grudges."),
   (else_try),
     (eq, ":dominant", 5),
     (str_store_string, s6, "@Road reputation: deserter killer."),
     (str_store_string, s7, "@Deserters will be harder to recruit and more likely to fight like cornered men."),
   (else_try),
     (str_store_string, s6, "@Road reputation: unformed."),
     (str_store_string, s7, "@The roads do not yet agree on what kind of captain you are."),
   (try_end),
   (try_begin),
     (gt, "$g_sod_hostile_regional_noncombat_count", "$g_sod_hostile_regional_combat_count"),
     (str_store_string, s10, "@Recent road settlements lean toward bargains, dispersals, and quiet endings."),
   (else_try),
     (lt, "$g_sod_hostile_regional_noncombat_count", "$g_sod_hostile_regional_combat_count"),
     (str_store_string, s10, "@Recent road settlements lean toward steel and graves."),
   (else_try),
     (gt, "$g_sod_hostile_regional_combat_count", 0),
     (str_store_string, s10, "@Your recent road record is split between bargains and battle."),
   (else_try),
     (str_store_string, s10, "@Your recent road record is still too thin to read."),
   (try_end),
 ]),
]
