# COST: low
SCRIPTS = [
("sod_threat_board_describe_offer",
 [
   (store_script_param_1, ":offer_index"),

   (assign, ":slot_no", slot_quest_sod_threat_offer_1),
   (try_begin),
     (eq, ":offer_index", 2),
     (assign, ":slot_no", slot_quest_sod_threat_offer_2),
   (else_try),
     (eq, ":offer_index", 3),
     (assign, ":slot_no", slot_quest_sod_threat_offer_3),
   (try_end),
   (quest_get_slot, ":archetype", "qst_regional_threat_contract", ":slot_no"),
   (call_script, "script_sod_threat_board_get_archetype", ":archetype"),
    (assign, ":tier", reg(1)),
    (assign, ":deadline", reg(3)),

   (call_script, "script_sod_threat_board_calculate_reward", ":archetype"),
    (assign, ":gold", reg(0)),
    (assign, ":xp", reg(1)),
    (assign, reg(1), ":tier"),
    (assign, reg(2), ":gold"),
    (assign, reg(3), ":deadline"),
    (assign, reg(4), ":xp"),
    (store_mul, ":suggested_strength", ":tier", 12),
    (val_add, ":suggested_strength", 8),
    (assign, reg(5), ":suggested_strength"),

   (try_begin),
     (eq, ":archetype", sod_threat_archetype_river_pirates),
     (str_store_string, s2, "@River pirates"),
     (str_store_string, s3, "@river bandits"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_coastal_smugglers),
     (str_store_string, s2, "@Coastal smugglers"),
     (str_store_string, s3, "@sea raiders"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_army_deserters),
     (str_store_string, s2, "@Army deserters"),
     (str_store_string, s3, "@deserters"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_noble_deserters),
     (str_store_string, s2, "@Noble deserters"),
     (str_store_string, s3, "@veteran deserters"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_relic_thieves),
     (str_store_string, s2, "@Relic thieves"),
     (str_store_string, s3, "@mountain bandits"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_tomb_robbers),
     (str_store_string, s2, "@Tomb robbers"),
     (str_store_string, s3, "@ravaging bandits"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_rogue_company),
     (str_store_string, s2, "@Rogue company"),
     (str_store_string, s3, "@mercenaries"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_guild_traitors),
     (str_store_string, s2, "@Guild traitors"),
     (str_store_string, s3, "@elite sellswords"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_cattle_raiders),
     (str_store_string, s2, "@Cattle raiders"),
     (str_store_string, s3, "@raiders"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_herd_rustlers),
     (str_store_string, s2, "@Herd rustlers"),
     (str_store_string, s3, "@forest bandits"),
   (else_try),
     (eq, ":archetype", sod_threat_archetype_invader_scouts),
     (str_store_string, s2, "@Invader scouts"),
     (str_store_string, s3, "@Serpent Host scouts"),
   (else_try),
     (str_store_string, s2, "@Raiding captain"),
     (str_store_string, s3, "@Black Company raiders"),
   (try_end),
   (str_store_string, s1, "@Tier {reg1} {s2}: expected {s3}; recommended force {reg5}+ fit troops; reward {reg2} denars and {reg4} XP; {reg3} days."),
 ]),
]
