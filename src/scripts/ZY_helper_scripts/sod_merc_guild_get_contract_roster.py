# COST: trivial; resolved once when an AI mercenary company is formed.
# Player hire menus keep their chosen mix. AI companies receive a job-aware
# mix so a contract is visible on the map without granting a new elite tier.
SCRIPTS = [
("sod_merc_guild_get_contract_roster",
 [
   (store_script_param_1, ":guild_faction"),
   (store_script_param, ":demand_type", 2),

   (assign, ":t1_1", 0),
   (assign, ":t1_2", 0),
   (assign, ":noble", 0),
   (assign, ":proportion", 0),
   (assign, ":specialist", 0),
   (assign, ":specialist_per_noble", 0),
   (try_begin),
     (call_script, "script_cf_sod_faction_is_merc_guild", ":guild_faction"),
     (call_script, "script_sod_merc_guild_get_roster", ":guild_faction"),
     (assign, ":t1_1", reg0),
     (assign, ":t1_2", reg1),
     (assign, ":noble", reg2),
     (assign, ":proportion", reg3),

     (try_begin),
       (eq, ":guild_faction", "fac_sod_merc_guild1"),
       # Existing Black Army cavalry remains, but security jobs show more foot and fewer crushers.
       (assign, ":specialist", black_army_tier_1_unit_3),
       (assign, ":specialist_per_noble", 4),
       (try_begin),
         (eq, ":demand_type", sod_merc_contract_role_patrol),
         (assign, ":proportion", 12),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_escort),
         (assign, ":proportion", 14),
         (assign, ":specialist_per_noble", 3),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_supply_column),
         (assign, ":proportion", 11),
         (assign, ":specialist_per_noble", 1),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_garrison_support),
         (assign, ":proportion", 18),
         (assign, ":specialist_per_noble", 2),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_special_world_activity),
         (assign, ":proportion", 14),
         (assign, ":specialist_per_noble", 3),
       (try_end),
     (else_try),
       (eq, ":guild_faction", "fac_sod_merc_guild2"),
       # Footmen feed the pike branch; crossbows dominate supply protection.
       (try_begin),
         (eq, ":demand_type", sod_merc_contract_role_patrol),
         (assign, ":proportion", 13),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_escort),
         (assign, ":proportion", 14),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_supply_column),
         (assign, ":proportion", 10),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_garrison_support),
         (assign, ":proportion", 16),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_field_company),
         (assign, ":proportion", 18),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_special_world_activity),
         (assign, ":proportion", 14),
       (try_end),
     (else_try),
       (eq, ":guild_faction", "fac_sod_merc_guild3"),
       # Spearmen become the visible core when the Guard is protecting a community.
       (try_begin),
         (eq, ":demand_type", sod_merc_contract_role_patrol),
         (assign, ":proportion", 11),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_escort),
         (assign, ":proportion", 10),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_supply_column),
         (assign, ":proportion", 10),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_garrison_support),
         (assign, ":proportion", 8),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_field_company),
         (assign, ":proportion", 14),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_special_world_activity),
         (assign, ":proportion", 9),
       (try_end),
     (else_try),
       (eq, ":guild_faction", "fac_sod_merc_guild4"),
       # Volvas are a mobile screen; armsmen hold hearths and walls.
       (try_begin),
         (eq, ":demand_type", sod_merc_contract_role_patrol),
         (assign, ":proportion", 16),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_escort),
         (assign, ":proportion", 13),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_supply_column),
         (assign, ":proportion", 8),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_garrison_support),
         (assign, ":proportion", 6),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_field_company),
         (assign, ":proportion", 11),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_special_world_activity),
         (assign, ":proportion", 14),
       (try_end),
     (else_try),
       (eq, ":guild_faction", "fac_sod_merc_guild5"),
       # The Host brings its cavalry screen to routes, but dismounts into a deliberately weak garrison mix.
       (try_begin),
         (eq, ":demand_type", sod_merc_contract_role_patrol),
         (assign, ":proportion", 19),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_escort),
         (assign, ":proportion", 20),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_supply_column),
         (assign, ":proportion", 11),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_garrison_support),
         (assign, ":proportion", 5),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_field_company),
         (assign, ":proportion", 17),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_special_world_activity),
         (assign, ":proportion", 16),
       (try_end),
     (else_try),
       (eq, ":guild_faction", "fac_sod_merc_guild6"),
       # A Slaver special operation has handlers, while unsuitable civic work exposes its coerced base.
       (try_begin),
         (eq, ":demand_type", sod_merc_contract_role_patrol),
         (assign, ":proportion", 14),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_escort),
         (assign, ":proportion", 11),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_supply_column),
         (assign, ":proportion", 5),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_garrison_support),
         (assign, ":proportion", 4),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_field_company),
         (assign, ":proportion", 17),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_special_world_activity),
         (assign, ":proportion", 20),
       (try_end),
     (else_try),
       (eq, ":guild_faction", "fac_sod_merc_guild7"),
       # Boar riders concentrate on frontier assignments; normal infantry remains for field work.
       (try_begin),
         (eq, ":demand_type", sod_merc_contract_role_patrol),
         (assign, ":proportion", 8),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_escort),
         (assign, ":proportion", 10),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_supply_column),
         (assign, ":proportion", 4),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_garrison_support),
         (assign, ":proportion", 18),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_field_company),
         (assign, ":proportion", 12),
       (else_try),
         (eq, ":demand_type", sod_merc_contract_role_special_world_activity),
         (assign, ":proportion", 8),
       (try_end),
     (try_end),
   (try_end),

   (assign, reg0, ":t1_1"),
   (assign, reg1, ":t1_2"),
   (assign, reg2, ":noble"),
   (assign, reg3, ":proportion"),
   (assign, reg4, ":specialist"),
   (assign, reg5, ":specialist_per_noble"),
 ]),
]
