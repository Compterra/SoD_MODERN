# COST: O(1) per dialogue call; no loops.
SCRIPTS = [
("sod_store_lord_first_line_to_s12",
 [
   (store_script_param_1, ":lord_troop"),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":lord_troop", kingdom_heroes_begin, kingdom_heroes_end),
     (troop_slot_eq, ":lord_troop", slot_troop_occupation, slto_kingdom_hero),
     (troop_get_slot, ":reputation", ":lord_troop", slot_lord_reputation_type),
     (call_script, "script_troop_get_player_relation", ":lord_troop"),
     (assign, ":relation", reg0),
     (store_troop_faction, ":lord_faction", ":lord_troop"),
     (store_relation, ":faction_relation", ":lord_faction", "fac_player_faction"),
     (try_begin),
       (gt, "$supported_pretender", 0),
       (str_store_troop_name, s13, "$supported_pretender"),
       (str_store_string, s12, "@Rebellion makes every greeting a test of nerve. If you bring words for {s13}'s cause, spend them carefully."),
     (else_try),
       (gt, "$players_kingdom", 0),
       (eq, ":lord_faction", "$players_kingdom"),
       (faction_slot_eq, ":lord_faction", slot_faction_leader, ":lord_troop"),
       (str_store_string, s12, "@You stand before the one whose banner weighs on your road. If this is counsel, give it plainly; if it is service, make it useful."),
     (else_try),
       (eq, ":lord_faction", "fac_player_supporters_faction"),
       (neg|faction_slot_eq, "fac_player_supporters_faction", slot_faction_leader, ":lord_troop"),
       (str_store_string, s12, "@My liege. Your realm has more needs than hours, but a loyal lord can still choose which fire to stamp first."),
     (else_try),
       (gt, "$players_kingdom", 0),
       (eq, ":lord_faction", "$players_kingdom"),
       (ge, ":relation", 0),
       (str_store_string, s12, "@We serve under the same banner for now. Whether by oath, contract, or necessity, that should make this conversation useful."),
     (else_try),
       (le, ":relation", -20),
       (try_begin),
         (this_or_next|eq, ":reputation", lrep_quarrelsome),
         (eq, ":reputation", lrep_debauched),
         (str_store_string, s12, "@You again. I had hoped the road would have better taste than to bring you here. Speak, and be worth the irritation."),
       (else_try),
         (this_or_next|eq, ":reputation", lrep_goodnatured),
         (eq, ":reputation", lrep_upstanding),
         (str_store_string, s12, "@I know enough of you to be wary, {playername}. Still, courtesy costs less than blood. Speak plainly and we may keep it that way."),
       (else_try),
         (str_store_string, s12, "@Our business is not friendly business, {playername}. Choose your words as though someone will remember them."),
       (try_end),
     (else_try),
       (ge, ":relation", 20),
       (try_begin),
         (this_or_next|eq, ":reputation", lrep_goodnatured),
         (eq, ":reputation", lrep_upstanding),
         (str_store_string, s12, "@Good. A face I do not have to weigh like bad coin. Tell me what brings you here, friend."),
       (else_try),
         (eq, ":reputation", lrep_martial),
         (str_store_string, s12, "@You ride with purpose. Good. Men who waste words tend to waste daylight as well."),
       (else_try),
         (eq, ":reputation", lrep_cunning),
         (str_store_string, s12, "@Ah, {playername}. When capable people come calling, I listen for the bargain hidden under the greeting."),
       (else_try),
         (str_store_string, s12, "@You are welcome enough here, {playername}. Say what needs saying."),
       (try_end),
     (else_try),
       (lt, ":faction_relation", 0),
       (try_begin),
         (eq, ":reputation", lrep_martial),
         (str_store_string, s12, "@You ride under a hostile shadow. If this is parley, make it sharp and clean."),
       (else_try),
         (eq, ":reputation", lrep_cunning),
         (str_store_string, s12, "@Enemies who ask to talk usually want either time or witnesses. Which are you buying?"),
       (else_try),
         (str_store_string, s12, "@Your banner and mine are not friends. Still, words may save men that steel would spend."),
       (try_end),
     (else_try),
       (eq, ":reputation", lrep_martial),
       (str_store_string, s12, "@Speak while the horses breathe. If this is business, let it have a point."),
     (else_try),
       (eq, ":reputation", lrep_quarrelsome),
       (str_store_string, s12, "@You have my attention. Try not to prove that I was careless in granting it."),
     (else_try),
       (eq, ":reputation", lrep_selfrighteous),
       (str_store_string, s12, "@Many come asking favors. Fewer come bearing duties. Which sort are you?"),
     (else_try),
       (eq, ":reputation", lrep_cunning),
       (str_store_string, s12, "@Every visit has a price, even when no coin changes hands. Let us hear yours."),
     (else_try),
       (eq, ":reputation", lrep_debauched),
       (str_store_string, s12, "@If this is grave news, make it amusing. If it is amusing news, make it profitable."),
     (else_try),
       (this_or_next|eq, ":reputation", lrep_goodnatured),
       (eq, ":reputation", lrep_upstanding),
       (str_store_string, s12, "@You are received, {playername}. If the matter is honest, bring it into the light."),
     (else_try),
       (str_store_string, s12, "@You have my attention. Make the matter worth the interruption."),
     (try_end),
     (assign, reg0, 1),
   (try_end),
 ]),

("sod_store_mayor_social_weather_to_s12",
 [
   (store_script_param_1, ":center_no"),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":center_no", centers_begin, centers_end),
     (party_get_slot, ":besieged_by", ":center_no", slot_center_is_besieged_by),
     (party_get_slot, ":prosperity", ":center_no", slot_town_prosperity),
     (party_get_slot, ":security_pressure", ":center_no", slot_center_sod_security_pressure),
     (party_get_slot, ":accumulated_rents", ":center_no", slot_center_accumulated_rents),
     (party_get_slot, ":active_courier", ":center_no", slot_center_sod_active_tax_courier),
     (party_get_slot, ":courier_losses", ":center_no", slot_center_sod_tax_courier_losses),
     (party_get_slot, ":relation", ":center_no", slot_center_player_relation),
     (try_begin),
       (ge, ":besieged_by", 0),
       (str_store_string, s12, "@The town can pay men, or feed families. It cannot keep doing both forever. If this is business, make it clean."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":security_pressure", 20),
       (str_store_string, s12, "@The watch reports read like unpaid debts: small troubles, counted daily, becoming large by habit. What needs the town seal now?"),
       (assign, reg0, 1),
     (else_try),
       (gt, ":active_courier", 0),
       (party_is_active, ":active_courier"),
       (str_store_string, s12, "@A tax courier has already left our gate. Until that chest reaches its lord, every clerk in town acts as though the road itself owes us a receipt."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":courier_losses", 2),
       (str_store_string, s12, "@We have lost too many tax couriers for anyone to call it misfortune. The ledgers are starting to read like casualty lists."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":accumulated_rents", 2000),
       (str_store_string, s12, "@The tax chests are getting heavy, and heavy chests make nervous roads. I would rather count receipts than hoofprints."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":relation", -5),
       (str_store_string, s12, "@The town will hear your business, {playername}. Whether it thanks me for opening the door is another ledger entirely."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":prosperity", 25),
       (str_store_string, s12, "@Every loaf in this town has three hands reaching for it. If this is business, make it clean."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":prosperity", 75),
       (str_store_string, s12, "@The markets are loud, the tax books are fat, and every guild clerk thinks himself a minister. Speak before they invent another form."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":relation", 20),
       (str_store_string, s12, "@You have helped this town enough that even the clerks argue more softly when your name appears. What business needs the seal today?"),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),

("sod_store_village_elder_social_weather_to_s12",
 [
   (store_script_param_1, ":village_no"),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":village_no", villages_begin, villages_end),
     (party_get_slot, ":village_state", ":village_no", slot_village_state),
     (party_get_slot, ":looter_pressure", ":village_no", slot_center_sod_looter_raid_pressure),
     (party_get_slot, ":volunteers", ":village_no", slot_center_volunteer_troop_amount),
     (party_get_slot, ":relation", ":village_no", slot_center_player_relation),
     (try_begin),
       (eq, ":village_state", svs_being_raided),
       (str_store_string, s12, "@There is smoke in every answer I give today. If you have strength to lend, spend it before the fields are ash."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":village_state", svs_looted),
       (str_store_string, s12, "@We are still counting what was taken. Grain can be measured. Courage is harder to put back in a storehouse."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":village_state", svs_recovering),
       (str_store_string, s12, "@The roofs are being patched, the fences raised again, and the children have stopped whispering quite so much. That is what passes for good news here."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":looter_pressure", 60),
       (str_store_string, s12, "@The road is being watched by men who own nothing but hunger and knives. We speak softly because sound travels."),
       (assign, reg0, 1),
     (else_try),
       (gt, ":looter_pressure", 0),
       (str_store_string, s12, "@There is trouble near the lanes. Not enough to empty the village yet, enough to make every errand a prayer."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":relation", -5),
       (str_store_string, s12, "@The village will answer you, {sir/madam}. Whether it trusts the answer it gives is another matter."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":volunteers", 0),
       (str_store_string, s12, "@The young ones are needed close to home today. A village can empty itself into armies only so many times before winter notices."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":relation", 20),
       (str_store_string, s12, "@You are known here, and not merely by name. The village listens differently when a friend asks."),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),

("sod_store_goods_merchant_social_weather_to_s12",
 [
   (store_script_param_1, ":center_no"),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":center_no", centers_begin, centers_end),
     (party_get_slot, ":prosperity", ":center_no", slot_town_prosperity),
     (party_get_slot, ":population", ":center_no", slot_center_sod_local_population),
     (party_get_slot, ":health", ":center_no", slot_center_sod_local_health),
     (party_get_slot, ":security_pressure", ":center_no", slot_center_sod_security_pressure),
     (party_get_slot, ":relation", ":center_no", slot_center_player_relation),
     (try_begin),
       (ge, ":security_pressure", 25),
       (str_store_string, s12, "@Caravans are counting guards before coin. When roads grow nervous, every honest sack starts costing like contraband."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":health", 30),
       (str_store_string, s12, "@Half the market asks for clean water and the other half asks who touched the barrels. Trade gets delicate when sickness learns the alleys."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":prosperity", 25),
       (str_store_string, s12, "@People still buy, but now they buy by the handful and apologize to the scales. Tell me what you need before someone else needs it worse."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":population", 800),
       (str_store_string, s12, "@The town feels thin. Fewer hands at the stalls, fewer boots on the stones, fewer mistakes a merchant can afford."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":relation", -5),
       (str_store_string, s12, "@I will sell to you because coin spends even when trust does not. But the scale stays where both of us can see it."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":prosperity", 75),
       (str_store_string, s12, "@Good season for trade. Bad season for patience. Everyone with a purse thinks the market should bow first."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":relation", 20),
       (str_store_string, s12, "@A reliable buyer is rarer than a cheap road. For you, I can speak plainly about price before the market starts performing."),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),

("sod_store_tavernkeeper_social_weather_to_s12",
 [
   (store_script_param_1, ":center_no"),
   (assign, reg0, 0),
   (try_begin),
     (is_between, ":center_no", centers_begin, centers_end),
     (party_get_slot, ":prosperity", ":center_no", slot_town_prosperity),
     (party_get_slot, ":health", ":center_no", slot_center_sod_local_health),
     (party_get_slot, ":security_pressure", ":center_no", slot_center_sod_security_pressure),
     (party_get_slot, ":accumulated_rents", ":center_no", slot_center_accumulated_rents),
     (party_get_slot, ":relation", ":center_no", slot_center_player_relation),
     (try_begin),
       (ge, ":security_pressure", 25),
       (str_store_string, s12, "@Room's louder than usual tonight. When roads go bad, men drink like the door is listening."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":health", 30),
       (str_store_string, s12, "@I am watering the ale less and boiling the cups more. That tells you enough about the town's mood."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":accumulated_rents", 2000),
       (str_store_string, s12, "@Tax talk has been walking table to table with its boots on. Folk notice when coin leaves town in chests."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":relation", -5),
       (str_store_string, s12, "@The room knows you, and not every whisper is kind. Still, coin buys ale before it buys forgiveness."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":prosperity", 25),
       (str_store_string, s12, "@Thin purses make quiet rooms. Even the songs are being nursed by the cup."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":prosperity", 75),
       (str_store_string, s12, "@Good coin in the room tonight. Bad judgment too, if the hour gets late enough."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":relation", 20),
       (str_store_string, s12, "@Your name lands well in this room. That does not make the ale cheaper, but it does make the stories more honest."),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),

("sod_store_guild_master_social_weather_to_s12",
 [
   (store_script_param_1, ":guild_master"),
   (assign, reg0, 0),
   (try_begin),
     (gt, ":guild_master", 0),
     (store_troop_faction, ":guild_faction", ":guild_master"),
     (faction_get_slot, ":guild_base", ":guild_faction", slot_guild_base),
     (gt, ":guild_base", 0),
     (party_get_slot, ":security_pressure", ":guild_base", slot_center_sod_security_pressure),
     (party_get_slot, ":prosperity", ":guild_base", slot_town_prosperity),
     (party_get_slot, ":accumulated_rents", ":guild_base", slot_center_accumulated_rents),
     (store_relation, ":relation", ":guild_faction", "fac_player_faction"),
     (try_begin),
       (lt, ":relation", 0),
       (str_store_string, s12, "@Contracts remember enemies longer than soldiers do. If you came for work, speak like coin can still improve your manners."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":security_pressure", 25),
       (str_store_string, s12, "@Road danger is becoming market weather. Escorts ask double, caravans leave late, and every captain at my table claims prudence is a billable skill."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":accumulated_rents", 2000),
       (str_store_string, s12, "@Tax chests have been moving loudly. When coin travels under guard, mercenaries start hearing opportunity in every hoofbeat."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":prosperity", 25),
       (str_store_string, s12, "@Poor markets make sharp employers. Everyone wants protection, few want to admit what protection costs."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":prosperity", 75),
       (str_store_string, s12, "@A fat market makes brave merchants and expensive mistakes. Good season for guild work, if you can tell profit from bait."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":relation", 20),
       (str_store_string, s12, "@Your name opens this table more easily than most. The company listens when reliable partners bring business."),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),

("sod_store_castle_patrol_authority_to_s12",
 [
   (store_script_param_1, ":patrol_party"),
   (assign, reg0, 0),
   (try_begin),
     (party_slot_eq, ":patrol_party", slot_party_sod_support_type, sod_support_type_castle_patrol),
     (call_script, "script_sod_store_castle_patrol_dialog_context", ":patrol_party"),
     (party_get_slot, ":role", ":patrol_party", slot_party_sod_patrol_role),
     (party_get_slot, ":quality", ":patrol_party", slot_party_sod_patrol_quality),
     (party_get_slot, ":status", ":patrol_party", slot_party_sod_patrol_status),
     (store_faction_of_party, ":patrol_faction", ":patrol_party"),
     (store_relation, ":relation", ":patrol_faction", "fac_player_supporters_faction"),
     (try_begin),
       (lt, ":relation", 0),
       (str_store_string, s12, "@Our authority is the seal of {s1}, the warrant of {s4}, and the fact that hostile banners do not get to wander this road uncounted. Contraband, scouts, false pilgrims, unpaid blades: we stop what we can prove and remember what we cannot."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_village_shield),
       (str_store_string, s12, "@We hold village law here: no stolen cattle, no pressed peasants, no grain wagons slipping away under a noble excuse. {s4} signs the warrant, but the fields near {s2} are what we are really guarding."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_border_harasser),
       (str_store_string, s12, "@This is border work. We read hoofprints like signatures and treat unmarked baggage as a confession waiting for witnesses. {s1} wants enemies delayed, exposed, and worried."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_caravan_screen),
       (str_store_string, s12, "@Caravan law is simple: manifests match wagons, escorts match names, and sealed chests stay sealed until a proper clerk breaks them. Smugglers hate us because we make profit answer questions."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_campaign_screen),
       (str_store_string, s12, "@Campaign authority is less polite than town law. We count armed companies, watch supply trails, and stop any baggage train that smells like an army pretending to be trade."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_emergency_relief),
       (str_store_string, s12, "@Relief orders outrank courtesy. We carry {s1}'s seal for roads where delay costs lives, so contraband matters less than blocked bridges, stolen carts, and men who keep aid from reaching {s2}."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":status", sod_castle_patrol_status_damaged),
       (str_store_string, s12, "@Our warrant is bruised, not broken. Wounded patrols look harder at packs, papers, and promises because one missed blade can finish what the last fight started."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":quality", 80),
       (str_store_string, s12, "@We are not toll men. {s4} gave us authority to keep the road legible: who rides, who pays, who hides steel under wool, and who carries goods that belong to someone quieter."),
       (assign, reg0, 1),
     (else_try),
       (str_store_string, s12, "@The warrant is plain enough: keep {s2} passable, count suspicious companies, and turn back contraband before it becomes bandit pay or enemy supply. Honest travelers lose only a moment."),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),

("sod_store_nonplayer_patrol_first_line_to_s12",
 [
   (store_script_param_1, ":patrol_party"),
   (assign, reg0, 0),
   (try_begin),
     (party_slot_eq, ":patrol_party", slot_party_sod_support_type, sod_support_type_castle_patrol),
     (call_script, "script_sod_player_can_command_castle_patrol", ":patrol_party"),
     (eq, reg0, 0),
     (call_script, "script_sod_store_castle_patrol_dialog_context", ":patrol_party"),
     (party_get_slot, ":role", ":patrol_party", slot_party_sod_patrol_role),
     (party_get_slot, ":status", ":patrol_party", slot_party_sod_patrol_status),
     (party_get_slot, ":quality", ":patrol_party", slot_party_sod_patrol_quality),
     (store_faction_of_party, ":patrol_faction", ":patrol_party"),
     (store_relation, ":relation", ":patrol_faction", "fac_player_supporters_faction"),
     (try_begin),
       (lt, ":relation", 0),
       (eq, ":role", sod_castle_patrol_role_border_harasser),
       (str_store_string, s12, "@Hold there. {s1} sent us across this border to make enemies count their teeth before they speak. If your business is honest, make it short."),
       (assign, reg0, 1),
     (else_try),
       (lt, ":relation", 0),
       (str_store_string, s12, "@Hands where we can see them. {s4} has us watching {s2}, and your banner is not loved on this road."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":status", sod_castle_patrol_status_damaged),
       (str_store_string, s12, "@We have taken knocks enough today. {s1}'s seal still holds, but wounded men hear hoofbeats differently. State your business."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":status", sod_castle_patrol_status_returning),
       (str_store_string, s12, "@We are turning back to {s1}. Reports weigh more than pride when a patrol has done its work."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_village_shield),
       (str_store_string, s12, "@Slowly now. We are the shield between {s2} and every hungry blade that thinks peasants bleed quietly."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_caravan_screen),
       (str_store_string, s12, "@Keep to the visible road. We are counting wagons, hoofprints, and men who pretend not to follow either."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_campaign_screen),
       (str_store_string, s12, "@This is campaign work, not toll work. We watch movement, dust, and mistakes. Speak before you become one of them."),
       (assign, reg0, 1),
     (else_try),
       (eq, ":role", sod_castle_patrol_role_emergency_relief),
       (str_store_string, s12, "@Make way if you can. We ride with relief orders from {s1}, and someone ahead has run out of time."),
       (assign, reg0, 1),
     (else_try),
       (ge, ":quality", 80),
       (str_store_string, s12, "@Halt at a respectful distance. {s1} does not send green boys for this watch. Name yourself and your road."),
       (assign, reg0, 1),
     (else_try),
       (str_store_string, s12, "@Hold. {s1} patrol, {s3}. We watch {s2}, and we have learned to dislike surprises."),
       (assign, reg0, 1),
     (try_end),
   (try_end),
 ]),
]
