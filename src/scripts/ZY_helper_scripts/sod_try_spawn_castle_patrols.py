# COST: castles * local scan
SCRIPTS = [
("sod_try_spawn_castle_patrols",
 [
   (try_for_range, ":castle_no", castles_begin, castles_end),
     (call_script, "script_sod_get_castle_patrol_capacity", ":castle_no"),
     (gt, reg2, 0),
     (call_script, "script_sod_get_castle_patrol_demand", ":castle_no"),
     (assign, ":demand", reg0),
     (assign, ":role", reg1),
     (assign, ":target", reg2),
     (assign, ":urgency", reg3),
     (ge, ":demand", 25),
     (store_faction_of_party, ":faction_no", ":castle_no"),
     (call_script, "script_sod_count_faction_castle_patrols", ":faction_no"),
     (assign, ":faction_active", reg0),
     (call_script, "script_sod_get_faction_castle_patrol_cap", ":faction_no"),
     (assign, ":faction_cap", reg0),
     (try_begin),
       (ge, ":faction_active", ":faction_cap"),
       (this_or_next|eq, ":role", sod_castle_patrol_role_border_harasser),
       (eq, ":role", sod_castle_patrol_role_road),
       (assign, ":role", sod_castle_patrol_role_village_shield),
     (else_try),
       (ge, ":faction_active", ":faction_cap"),
       (neq, ":role", sod_castle_patrol_role_emergency_relief),
       (neq, ":role", sod_castle_patrol_role_village_shield),
       (assign, ":role", sod_castle_patrol_role_village_shield),
     (try_end),
     (try_begin),
       (eq, ":role", sod_castle_patrol_role_village_shield),
       (call_script, "script_sod_select_castle_patrol_target_for_role", ":castle_no", ":role"),
       (assign, ":target", reg0),
     (try_end),
     (call_script, "script_cf_sod_create_castle_patrol", ":castle_no", ":target", ":faction_no", ":role", ":urgency", -1, ":demand"),
   (try_end),
 ]),
]
