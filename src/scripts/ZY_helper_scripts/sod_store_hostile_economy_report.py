# COST: party scan
SCRIPTS = [
("sod_store_hostile_economy_report",
 [
   (assign, reg21, 0), # generic looters
   (assign, reg22, 0), # specialist bandits
   (assign, reg23, 0), # deserters total
   (assign, reg24, 0), # tagged pressure parties
   (assign, reg25, 0), # prisoner-breakout parties
   (assign, reg26, 0), # active hostile total
   (assign, reg27, sod_spawn_cap_generic_bandits),
   (assign, reg28, 0), # specialist cap total
   (val_add, reg28, sod_spawn_cap_mountain_bandits),
   (val_add, reg28, sod_spawn_cap_forest_bandits),
   (val_add, reg28, sod_spawn_cap_sea_raiders),
   (val_add, reg28, sod_spawn_cap_steppe_bandits),
   (val_add, reg28, sod_spawn_cap_boar_raiders),
   (val_add, reg28, sod_spawn_cap_boar_desert_bands),
   (assign, reg29, num_max_deserters),
   (str_store_string, s11, "@No pressure-origin party located recently."),
   (try_for_parties, ":party_no"),
     (party_is_active, ":party_no"),
     (party_get_template_id, ":template", ":party_no"),
     (try_begin),
       (eq, ":template", "pt_bandits"),
       (val_add, reg21, 1),
       (val_add, reg26, 1),
     (else_try),
       (this_or_next|eq, ":template", "pt_mountain_bandits"),
       (this_or_next|eq, ":template", "pt_forest_bandits"),
       (this_or_next|eq, ":template", "pt_steppe_bandits"),
       (this_or_next|eq, ":template", "pt_sea_raiders"),
       (this_or_next|eq, ":template", "pt_boar_clan_fighters"),
       (eq, ":template", "pt_boar_clan_fighters_desert"),
       (val_add, reg22, 1),
       (val_add, reg26, 1),
     (else_try),
       (this_or_next|eq, ":template", "pt_deserters"),
       (this_or_next|eq, ":template", "pt_sod_deserters"),
       (eq, ":template", "pt_sod_merc_deserters"),
       (val_add, reg23, 1),
       (val_add, reg26, 1),
     (try_end),
     (party_get_slot, ":sponsor_center", ":party_no", slot_party_sod_threat_sponsor_center),
     (try_begin),
       (is_between, ":sponsor_center", centers_begin, centers_end),
       (val_add, reg24, 1),
       (str_store_party_name, s11, ":sponsor_center"),
     (try_end),
     (party_get_slot, ":prisoner_origin", ":party_no", slot_party_sod_prisoner_origin),
     (try_begin),
       (is_between, ":prisoner_origin", centers_begin, centers_end),
       (party_get_slot, ":prisoner_destination", ":party_no", slot_party_sod_prisoner_destination),
       (is_between, ":prisoner_destination", centers_begin, centers_end),
       (eq, ":template", "pt_bandits"),
       (val_add, reg25, 1),
     (try_end),
   (try_end),
   (try_begin),
     (le, reg21, 3),
     (str_store_string, s12, "@Looter signs are scattered and local."),
   (else_try),
     (le, reg21, 12),
     (str_store_string, s12, "@Looters are stirring around troubled settlements."),
   (else_try),
     (str_store_string, s12, "@Looters are becoming common enough to worry the roads."),
   (try_end),
   (try_begin),
     (le, reg22, 4),
     (str_store_string, s13, "@The harder bandit clans are quiet."),
   (else_try),
     (le, reg22, 12),
     (str_store_string, s13, "@Regional bandits are active in their usual country."),
   (else_try),
     (str_store_string, s13, "@Specialist raiders are thick on the frontiers."),
   (try_end),
   (try_begin),
     (le, reg23, 2),
     (str_store_string, s14, "@Desertion talk is faint."),
   (else_try),
     (le, reg23, 6),
     (str_store_string, s14, "@Deserter bands are being reported by scouts."),
   (else_try),
     (str_store_string, s14, "@Too many armed runaways are loose for comfort."),
   (try_end),
   (try_begin),
     (le, reg24, 2),
     (str_store_string, s15, "@Most hostile bands look opportunistic rather than rooted."),
   (else_try),
     (le, reg24, 8),
     (str_store_string, s15, "@Several hostile bands can be traced to local hardship."),
   (else_try),
     (str_store_string, s15, "@The roads are showing systemic local breakdown, not mere robbery."),
   (try_end),
   (try_begin),
     (le, reg25, 0),
     (str_store_string, s16, "@No fresh prisoner-breakout band is being whispered about."),
   (else_try),
     (le, reg25, 2),
     (str_store_string, s16, "@There are rumors of escaped prisoners turning outlaw."),
   (else_try),
     (str_store_string, s16, "@Prisoner breakouts are feeding the outlaw problem."),
   (try_end),
 ]),
]
