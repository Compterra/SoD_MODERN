# COST: light
from header_operations import *
from module_constants import *

SCRIPTS = [
("sod_treason_select_plea_reaction",
 [
   (store_script_param_1, ":lord_troop"),
   (assign, ":reaction", 1),
   (try_begin),
     (is_between, ":lord_troop", kingdom_heroes_begin, kingdom_heroes_end),
     (store_troop_faction, ":lord_faction", ":lord_troop"),
     (troop_get_slot, ":reputation", ":lord_troop", slot_lord_reputation_type),
     (troop_get_slot, ":lord_renown", ":lord_troop", slot_troop_renown),
     (troop_get_slot, ":player_renown", "trp_player", slot_troop_renown),
     (call_script, "script_troop_get_player_relation", ":lord_troop"),
     (assign, ":relation", reg0),
     (assign, ":is_king", 0),
     (try_begin),
       (faction_slot_eq, ":lord_faction", slot_faction_leader, ":lord_troop"),
       (assign, ":is_king", 1),
     (try_end),
     (store_random_in_range, ":roll", 0, 100),
     (try_begin),
       (this_or_next|eq, ":reputation", lrep_goodnatured),
       (eq, ":reputation", lrep_upstanding),
       (try_begin),
         (eq, ":is_king", 0),
         (ge, ":relation", 15),
         (lt, ":roll", 55),
         (assign, ":reaction", 0),
       (else_try),
         (assign, ":reaction", 1),
       (try_end),
     (else_try),
       (eq, ":reputation", lrep_martial),
       (try_begin),
         (eq, ":is_king", 1),
         (assign, ":reaction", 3),
       (else_try),
         (assign, ":reaction", 1),
       (try_end),
     (else_try),
       (this_or_next|eq, ":reputation", lrep_quarrelsome),
       (eq, ":reputation", lrep_debauched),
       (assign, ":reaction", 2),
     (else_try),
       (eq, ":reputation", lrep_selfrighteous),
       (assign, ":reaction", 3),
     (else_try),
       (eq, ":reputation", lrep_cunning),
       (assign, ":reaction", 1),
     (else_try),
       (try_begin),
         (lt, ":roll", 25),
         (assign, ":reaction", 0),
       (else_try),
         (lt, ":roll", 60),
         (assign, ":reaction", 1),
       (else_try),
         (lt, ":roll", 82),
         (assign, ":reaction", 2),
       (else_try),
         (assign, ":reaction", 3),
       (try_end),
     (try_end),

     (try_begin),
       (lt, ":relation", -15),
       (this_or_next|eq, ":reaction", 0),
       (eq, ":reaction", 1),
       (assign, ":reaction", 2),
     (else_try),
       (ge, ":relation", 20),
       (this_or_next|eq, ":reaction", 2),
       (eq, ":reaction", 3),
       (assign, ":reaction", 1),
     (try_end),
     (try_begin),
       (eq, ":is_king", 1),
       (eq, ":reaction", 0),
       (assign, ":reaction", 3),
     (try_end),
     (store_add, ":renown_margin", ":player_renown", 75),
     (try_begin),
       (lt, ":renown_margin", ":lord_renown"),
       (eq, ":reaction", 1),
       (assign, ":reaction", 2),
     (else_try),
       (gt, ":player_renown", ":lord_renown"),
       (eq, ":reaction", 2),
       (assign, ":reaction", 1),
     (try_end),
   (try_end),
   (val_clamp, ":reaction", 0, 4),
   (troop_set_slot, ":lord_troop", slot_prisoner_agreed, ":reaction"),
 ]),

("sod_treason_select_final_words",
 [
   (store_script_param_1, ":lord_troop"),
   (assign, ":reaction", 1),
   (try_begin),
     (is_between, ":lord_troop", kingdom_heroes_begin, kingdom_heroes_end),
     (store_troop_faction, ":lord_faction", ":lord_troop"),
     (troop_get_slot, ":reputation", ":lord_troop", slot_lord_reputation_type),
     (troop_get_slot, ":lord_renown", ":lord_troop", slot_troop_renown),
     (troop_get_slot, ":player_renown", "trp_player", slot_troop_renown),
     (call_script, "script_troop_get_player_relation", ":lord_troop"),
     (assign, ":relation", reg0),
     (assign, ":is_king", 0),
     (try_begin),
       (faction_slot_eq, ":lord_faction", slot_faction_leader, ":lord_troop"),
       (assign, ":is_king", 1),
     (try_end),
     (store_random_in_range, ":roll", 0, 100),
     (try_begin),
       (this_or_next|eq, ":reputation", lrep_goodnatured),
       (eq, ":reputation", lrep_upstanding),
       (try_begin),
         (ge, ":relation", 10),
         (lt, ":roll", 45),
         (assign, ":reaction", 0),
       (else_try),
         (assign, ":reaction", 1),
       (try_end),
     (else_try),
       (eq, ":reputation", lrep_martial),
       (assign, ":reaction", 3),
     (else_try),
       (eq, ":reputation", lrep_cunning),
       (assign, ":reaction", 3),
     (else_try),
       (eq, ":reputation", lrep_selfrighteous),
       (assign, ":reaction", 4),
     (else_try),
       (this_or_next|eq, ":reputation", lrep_quarrelsome),
       (eq, ":reputation", lrep_debauched),
       (try_begin),
         (lt, ":roll", 55),
         (assign, ":reaction", 2),
       (else_try),
         (assign, ":reaction", 4),
       (try_end),
     (else_try),
       (try_begin),
         (lt, ":roll", 20),
         (assign, ":reaction", 0),
       (else_try),
         (lt, ":roll", 45),
         (assign, ":reaction", 1),
       (else_try),
         (lt, ":roll", 67),
         (assign, ":reaction", 2),
       (else_try),
         (lt, ":roll", 86),
         (assign, ":reaction", 3),
       (else_try),
         (assign, ":reaction", 4),
       (try_end),
     (try_end),

     (try_begin),
       (eq, ":is_king", 1),
       (lt, ":reaction", 3),
       (assign, ":reaction", 3),
     (try_end),
     (try_begin),
       (lt, ":relation", -20),
       (lt, ":reaction", 2),
       (assign, ":reaction", 2),
     (else_try),
       (ge, ":relation", 20),
       (gt, ":reaction", 1),
       (assign, ":reaction", 1),
     (try_end),
     (store_add, ":renown_margin", ":player_renown", 75),
     (try_begin),
       (lt, ":renown_margin", ":lord_renown"),
       (eq, ":reaction", 1),
       (assign, ":reaction", 2),
     (else_try),
       (gt, ":player_renown", ":lord_renown"),
       (eq, ":reaction", 2),
       (assign, ":reaction", 3),
     (try_end),
   (try_end),
   (val_clamp, ":reaction", 0, 5),
   (troop_set_slot, ":lord_troop", slot_prisoner_agreed, ":reaction"),
 ]),

("sod_treason_apply_spared_outcome",
 [
   (store_script_param_1, ":lord_troop"),
   (try_begin),
     (is_between, ":lord_troop", kingdom_heroes_begin, kingdom_heroes_end),
     (troop_get_slot, ":reaction", ":lord_troop", slot_prisoner_agreed),
     (store_troop_faction, ":lord_faction", ":lord_troop"),
     (troop_get_slot, ":reputation", ":lord_troop", slot_lord_reputation_type),
     (call_script, "script_troop_get_player_relation", ":lord_troop"),
     (assign, ":relation", reg0),
     (assign, ":relation_change", 1),
     (try_begin),
       (eq, ":reaction", 0),
       (assign, ":relation_change", 3),
     (else_try),
       (eq, ":reaction", 1),
       (assign, ":relation_change", 1),
     (else_try),
       (eq, ":reaction", 2),
       (assign, ":relation_change", -2),
     (else_try),
       (eq, ":reaction", 3),
       (assign, ":relation_change", -1),
     (try_end),
     (try_begin),
       (ge, ":relation", 20),
       (val_add, ":relation_change", 1),
     (else_try),
       (lt, ":relation", -20),
       (val_sub, ":relation_change", 1),
     (try_end),
     (try_begin),
       (this_or_next|eq, ":reputation", lrep_goodnatured),
       (eq, ":reputation", lrep_upstanding),
       (val_add, ":relation_change", 1),
     (else_try),
       (this_or_next|eq, ":reputation", lrep_quarrelsome),
       (eq, ":reputation", lrep_debauched),
       (val_sub, ":relation_change", 1),
     (try_end),
     (try_begin),
       (faction_slot_eq, ":lord_faction", slot_faction_leader, ":lord_troop"),
       (val_sub, ":relation_change", 1),
     (try_end),
     (val_clamp, ":relation_change", -4, 5),
     (call_script, "script_change_player_relation_with_troop", ":lord_troop", ":relation_change"),
     (call_script, "script_change_player_honor", 1),
     (call_script, "script_sod_nemesis_note_lord_resolution", ":lord_troop", sod_nemesis_lord_resolution_mercy),
     (call_script, "script_store_troop_name", s21, ":lord_troop"),
     (assign, reg21, ":relation_change"),
     (try_begin),
       (ge, ":relation_change", 0),
       (display_message, "@You stay the sentence. {s21} remains your prisoner, but mercy softens the grudge by {reg21}.", 0x99CCFF),
     (else_try),
       (display_message, "@You stay the sentence. {s21} remains your prisoner, but the trial leaves a sharper private grievance.", 0xFFCC66),
     (try_end),
     (troop_set_slot, ":lord_troop", slot_prisoner_agreed, 0),
   (try_end),
 ]),
]
