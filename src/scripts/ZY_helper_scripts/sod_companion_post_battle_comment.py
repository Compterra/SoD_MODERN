# COST: low
SCRIPTS = [
("sod_companion_post_battle_comment",
 [
   (store_script_param_1, ":comment_type"),
   (store_script_param_2, ":weight"),
   (val_max, ":weight", 1),
   (store_current_day, ":cur_day"),
   (try_begin),
     (eq, "$g_sod_last_post_battle_comment_day", ":cur_day"),
     (eq, "$g_sod_last_post_battle_comment_type", ":comment_type"),
   (else_try),
     (assign, "$g_sod_last_post_battle_comment_day", ":cur_day"),
     (assign, "$g_sod_last_post_battle_comment_type", ":comment_type"),
     (try_begin),
     (eq, ":comment_type", 1), # costly victory
     (call_script, "script_sod_companion_dispatch_player_action", sod_companion_action_costly_battle, ":weight"),
     (try_begin),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc12"),
       (display_message, "@Jeremus moves among the wounded after the victory. 'A field can be won and still ask mercy of us.'", 0xFFCC66),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc10"),
       (display_message, "@Bunduk counts the missing voices before anyone counts the spoils. 'We held. Now pay attention to who paid for it.'", 0xFFCC66),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc8"),
       (display_message, "@Matheld wipes blood from her shield and says the line stood, but it should not be spent cheaply again.", 0xFFCC66),
     (try_end),
   (else_try),
     (eq, ":comment_type", -1), # defeat
     (call_script, "script_sod_companion_dispatch_player_action", sod_companion_action_battle_defeat, ":weight"),
     (call_script, "script_sod_companion_dispatch_player_action", sod_companion_action_retreat_or_fail, 1),
     (try_begin),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc14"),
       (display_message, "@Lezalit says nothing until the men can hear him. 'Defeat is a lesson only if command has the courage to read it.'", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc10"),
       (display_message, "@Bunduk keeps his voice low after the defeat. 'The common men will forgive fear sooner than excuses.'", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc3"),
       (display_message, "@Ymira tends the worst hurt first. She does not accuse you, which somehow makes the silence heavier.", 0xCC6666),
     (try_end),
   (else_try),
     (eq, ":comment_type", 2), # commander duel won
     (call_script, "script_sod_companion_dispatch_player_action", sod_companion_action_commander_duel_won, ":weight"),
     (try_begin),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc13"),
       (display_message, "@Nizar is already shaping the duel into verse. For once, the soldiers do not tell him to stop.", 0x99CCFF),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc8"),
       (display_message, "@Matheld nods toward the fallen enemy commander. 'That is how a challenge earns its breath.'", 0x99CCFF),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc14"),
       (display_message, "@Lezalit watches the ranks steady after the duel. 'A clean victory travels faster than an order.'", 0x99CCFF),
     (try_end),
   (else_try),
     (eq, ":comment_type", -2), # commander duel lost
     (call_script, "script_sod_companion_dispatch_player_action", sod_companion_action_commander_duel_lost, ":weight"),
     (try_begin),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc10"),
       (display_message, "@Bunduk watches the ranks flinch after the duel. 'They saw more than one man fall there.'", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc3"),
       (display_message, "@Ymira reaches for bandages before pride can speak. The company notices which hand moved first.", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc14"),
       (display_message, "@Lezalit marks the damage in the line, not the wound. 'We repair discipline now, or fear writes the next order.'", 0xCC6666),
     (try_end),
   (else_try),
     (eq, ":comment_type", 3), # morale collapse
     (call_script, "script_sod_companion_dispatch_player_action", sod_companion_action_morale_collapse, ":weight"),
     (try_begin),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc10"),
       (display_message, "@Bunduk hears the break in the line before the officers name it. 'Men do not run from one bad moment. They run from a chain of them.'", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc8"),
       (display_message, "@Matheld looks back over the broken ground. 'A shield wall can bend. When it forgets why it stands, it dies.'", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc14"),
       (display_message, "@Lezalit calls the collapse by its name and starts rebuilding the watch before rumor can do it worse.", 0xCC6666),
     (try_end),
   (else_try),
     (eq, ":comment_type", 4), # mutiny breaks into battle
     (call_script, "script_sod_companion_dispatch_player_action", sod_companion_action_mutiny_battle, ":weight"),
     (try_begin),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc10"),
       (display_message, "@Bunduk looks toward the men who chose steel over petition. 'This is what happens when a camp stops believing it can be heard.'", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc3"),
       (display_message, "@Ymira turns away from the first mutineer to fall. There is no triumph in her face.", 0xCC6666),
     (else_try),
       (call_script, "script_cf_sod_companion_in_main_party", "trp_npc14"),
       (display_message, "@Lezalit tightens the loyal ranks. 'A mutiny is still a battle, but command should remember how it was invited.'", 0xCC6666),
     (try_end),
     (try_end),
   (try_end),
 ]),
]
