# COST: low
SCRIPTS = [
("sod_store_bandit_hideout_clue",
 [
   (store_script_param, ":party_no", 1),
   (assign, ":template", "$g_encountered_party_template"),
   (try_begin),
     (gt, ":party_no", 0),
     (party_get_template_id, ":template", ":party_no"),
   (try_end),
   (str_store_string, s19, "@the back roads"),
   (str_store_string, s20, "@Watch the low farms, dry wells, and broken watch-posts. Outlaws gather where lawful patrols turn back."),
   (try_begin),
     (eq, ":template", "pt_mountain_bandits"),
     (str_store_party_name, s19, "p_mountain_bandit_spawn_point"),
     (str_store_string, s20, "@Look above the goat tracks in {s19}. Smoke there means cooking fires, not shepherds."),
   (else_try),
     (eq, ":template", "pt_forest_bandits"),
     (str_store_party_name, s19, "p_forest_bandit_spawn_point"),
     (str_store_string, s20, "@Search {s19} where the cart-path narrows. Cut bark and fresh snares mark their sleeping ground."),
   (else_try),
     (eq, ":template", "pt_steppe_bandits"),
     (str_store_party_name, s19, "p_steppe_bandit_spawn_point"),
     (str_store_string, s20, "@Ride wide in {s19}. Their camp moves with the grass, but their remount herd leaves a trail."),
   (else_try),
     (eq, ":template", "pt_sea_raiders"),
     (str_store_party_name, s19, "p_sea_raider_spawn_point_1"),
     (str_store_string, s20, "@Follow {s19} at dawn. Boats beach where gulls circle and no village bell answers."),
   (else_try),
     (party_slot_eq, ":party_no", slot_party_sod_threat_active_quest, "qst_regional_threat_contract"),
     (party_get_slot, ":origin_center", ":party_no", slot_party_sod_threat_sponsor_center),
     (gt, ":origin_center", 0),
     (str_store_party_name, s19, ":origin_center"),
     (str_store_string, s20, "@Start near {s19}. Men working a contract return to the same wells, fences, and frightened cousins."),
   (try_end),
 ]),
]
