SIMPLE_TRIGGERS = [
(24,
  [
    (try_begin),
      (eq, "$g_sod_hide_messages", -2),
      (set_show_messages, 0),
    (try_end),

    (try_for_range, ":center_no", centers_begin, centers_end),
      (party_get_slot, ":center_population", ":center_no", slot_center_sod_local_population),
      (try_begin),
        (party_slot_eq, ":center_no", slot_party_type, spt_village),
        (this_or_next|lt, ":center_population", village_pop_min),
        (             gt, ":center_population", village_pop_max),
        (val_clamp, ":center_population", village_pop_min, village_pop_max+1),
        (party_set_slot, ":center_no", slot_center_sod_local_population, ":center_population"),
        (str_store_party_name_link, s3, ":center_no"),
        (try_begin),
          (eq, "$g_sod_debug", 1),
          (display_message, "@Pop limit reached at {s3}", debug_color),
        (try_end),
      (else_try),
        (party_slot_eq, ":center_no", slot_party_type, spt_town),
        (this_or_next|lt, ":center_population", town_pop_min),
        (             gt, ":center_population", town_pop_max),
        (val_clamp, ":center_population", town_pop_min, town_pop_max+1),
        (party_set_slot, ":center_no", slot_center_sod_local_population, ":center_population"),
        (str_store_party_name_link, s3, ":center_no"),
        (try_begin),
          (eq, "$g_sod_debug", 1),
          (display_message, "@Pop limit required at {s3}", debug_color),
        (try_end),
      (try_end),
    (try_end),

    (try_begin),
      (eq, "$g_sod_hide_messages", -2),
      (set_show_messages, 1),
    (try_end),
  ]),
]
