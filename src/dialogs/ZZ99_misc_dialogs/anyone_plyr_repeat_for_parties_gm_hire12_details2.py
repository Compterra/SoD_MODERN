DIALOGS = [
[anyone|plyr|repeat_for_parties, "gm_hire12_details2",
   [
     (store_repeat_object, ":party_no"),
     (store_faction_of_party, ":party_faction", ":party_no"),
     (assign, ":continue", 0),
     (try_begin),
       (eq, "$temp4", spai_holding_center),
       (try_begin),
         (this_or_next|party_slot_eq, ":party_no", slot_party_type, spt_castle),
         (party_slot_eq, ":party_no", slot_party_type, spt_town),
         (eq, ":party_faction", "$players_kingdom"),
         (assign, ":continue", 1),
       (try_end),
     (else_try),
       (eq, "$temp4", spai_patrolling_around_center),
       (try_begin),
#         (eq, ":party_faction", "$players_kingdom"),
         (is_between, ":party_no", centers_begin, centers_end),
         (assign, ":continue", 1),
       (try_end),
     (try_end),
     (eq, ":continue", 1),
     (str_store_party_name, s1, ":party_no")],
   "{s1}", "gm_hire12",
   [
     (store_repeat_object, "$temp6"),
     ]],
]
